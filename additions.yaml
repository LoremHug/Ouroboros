"""Post-migration additions: nodes and edges added during sessions.

This file is the source of truth for everything created via add_node.py /
add_edge.py. migrate.py applies it on top of the .txt parse.

Format:
    edges:
      - source: N_X
        target: N_Y
        label: snake_case_descriptor
        edge_status: D | S
        justification: |
          Why this connection holds, in plain text.
        why_forced: |
          What alternative is impossible and why.

    nodes:
      - id: N_NewNode
        title: ...
        layer: observers | structure | ...
        status: DEMONSTRATED | STRONG | CONDITIONAL | OPERATIONAL | STUB
        anchors: 3
        a_infinity: false
        summary: One-line CLAIM.
        why_status: Why this status holds.
        not_misinterpretations: Misreadings to reject.
        is_placeholder: false

Edits here are committed and become the canonical source of post-migration work.
"""

edges:
  - source: N_BPIEngagement
    target: N_GrammarTrap
    label: bpi_compression_manifests_as_grammar_artifacts
    edge_status: D
    justification: |
      BPI has K(O)<K(F) per Observer Containment Lemma (N112), forcing
      finite-capacity compression onto whatever substrate the BPI operates in.
      When that substrate is language, the compression manifests as the
      subject-verb-object grammar topology that N_GrammarTrap formulates —
      persistent objects + actions between them are the compression artifact
      class produced by language-using BPIs.
    why_forced: |
      Alternative requires either (a) BPI without compression — denies
      K(O)<K(F), contradicts N112; or (b) compression that doesn't manifest
      in language — but for any BPI whose I/O channel is language, the
      compression IS the grammar topology. N_GrammarTrap's claim that
      "grammar is ontological by structure" identifies precisely this class
      of artifact. No structurally coherent alternative.

  - source: N_GrammarTrap
    target: N_OntologyGate
    label: gate_intercepts_grammar_artifact_class
    edge_status: D
    justification: |
      N_OntologyGate's predicate list ("what is X?", "what exists?",
      "what is X made of?", "what is the nature of X?", "what is fundamental?")
      is exactly the catalogue of question-forms that subject-verb-object
      grammar produces under R1 reification. The gate exists as the
      operational protocol that intercepts these patterns at the language
      interface and translates them into structural form.
    why_forced: |
      Two structural directions check the edge: (a) without grammar trap,
      ontological questions don't arise — but the gate's own predicates are
      precisely the question-forms grammar produces (R1 + R4 catalogued in
      N_EpistemicTraps Traps 1-4); (b) gate operates without targeting
      grammar artifacts — but inspection of its predicate list shows it
      targets only R1/R4-loaded language patterns. The gate's predicate set
      IS the closure-condition of grammar-trap-generated questions; no
      alternative formulation closes without contradiction.

  - source: DEF
    target: N165
    label: definition_requires_ergodicity
    edge_status: D
    justification: |
      DEF defines A₀ = argmin Z as the unique stable transitioning that
      Banach contraction maps to its fixed point. Banach guarantees a fixed
      point only if iterations cover the relevant phase space (no permanent
      hidden basins). N165 (ergodicity theorem) provides exactly this
      coverage guarantee: the Reality Protocol Markov chain reaches the
      unique Gibbs state from any initial state via Perron-Frobenius. Without
      ergodicity, DEF's convergence claim is incomplete.
    why_forced: |
      Alternative requires either (a) A₀ converges without ergodicity — but
      then some basin is permanently hidden, contradicting K(O)<K(F) which
      already requires that the observer's accessible substrate is fully
      explored under the Z-cost gradient (per Observer Containment N112);
      or (b) DEF's fixed-point claim is weakened — but then A₀(A₀)=A₀ stops
      being structural inevitability and becomes a hypothesis. No
      structurally coherent alternative.

  - source: N_Triangulation
    target: N067
    label: triangulation_forces_spectral_dim_3
    edge_status: D
    justification: |
      Triangulation forces Z to have exactly 3 components. The same π₁(L(3,1))
      = Z₃ acting on S³ that produces the 3-fold Z-decomposition also forces
      the spectral dimension formula d_s(r) = 2 + 2r/(r + r_0) via RG fixed
      point analysis (η = 0 exact for the linear A₀ operator). At the unique
      half-transition scale r_0, d_s(r_0) = 3. The same "3" appears in both
      coordinates because both follow from one topological fact.
    why_forced: |
      Alternative requires either (a) d_s(r_0) ≠ 3 — but RG analysis with
      η = 0 (Nelson 1966; Wilson-Fisher) gives the unique formula d_s(r) =
      2 + 2r/(r + r_0); substitution at r = r_0 yields exactly 3 with no
      free parameter; or (b) triangulation has Z components ≠ 3 — but
      N_Triangulation establishes the triangulation principle (3 minimal
      Z-components for unique localisation in transition-space) as forced
      by definition of minimality. Both routes converge on 3, and the
      convergence is itself structural, not coincidental.

  - source: N067
    target: N_Triangulation
    label: spectral_dim_3_confirms_triangulation
    edge_status: D
    justification: |
      Independent topological derivation (sec:spectral-dim-prob via L(3,1)
      Hopf fibration, RG fixed point with η = 0) yields d_s(r_0) = 3 with no
      reference to Z-decomposition. This is a coordinate confirmation of
      the triangulation count: two independent calculations (Z-component
      count from triangulation, spectral dimension from RG) yielding the
      same integer 3 is the structural signature of one fact in two
      coordinates.
    why_forced: |
      The convergence is forced because both calculations descend from
      π₁(L(3,1)) = Z₃: the Z-decomposition via the Z₃ covering action
      (N_ZGaugeDecomposition), the spectral dimension via the Z₃ fibre-
      freezing in the Hopf fibration. Were the values different, that would
      signal inconsistent topology between the two coordinate expressions.
      Their identity at value 3 is forced by the same underlying topological
      structure.

  - source: N_ZGaugeDecomposition
    target: N129
    label: Z_components_are_algebra_summands
    edge_status: D
    justification: |
      The three Z-components (Z_struct, Z_therm, Z_hidden) of
      N_ZGaugeDecomposition correspond bijectively to the three summands of
      the spectral triple algebra A_F = ℂ ⊕ ℍ ⊕ M_3(ℂ) derived in N129.
      Both partitions descend from the same Z_3 action: π₁(L(3,1)) = Z_3 acts
      on H_F via triality (Z_3-character of SU(3) representations), and
      Center(SU(3)) = Z_3 also indexes the Z-components via the covering
      structure. Triality-0 (leptons, SU(3) singlets) ↔ ℂ summand; Triality-1
      (quarks, SU(3) triplets) ↔ M_3(ℂ) summand; the unique spin structure
      of L(3,1) equips chiral doublets with the ℍ summand.
    why_forced: |
      Alternative requires the algebra A_F to have a different number of
      summands than 3 — but Krajewski (1998) classification + Wedderburn's
      theorem on real C*-algebras + KO-dimension 6 + three Z_3 sectors +
      Poincaré duality + minimality jointly force exactly ℂ ⊕ ℍ ⊕ M_3(ℂ)
      with no remaining freedom. Or alternative requires Z to have a
      different number of components — but N_Triangulation forces exactly
      three (by triangulation principle). Both partitions converge on three
      because both descend from π₁(L(3,1)) = Z_3 — different coordinate
      expressions of one Z_3 covering action.

  - source: N129
    target: N_ZGaugeDecomposition
    label: algebra_summands_are_Z_components
    edge_status: D
    justification: |
      Reverse direction of the same identification: the three summands of
      A_F derived in N129 (via Krajewski + Wedderburn from L(3,1) topology)
      are the three Z-components of N_ZGaugeDecomposition. The structural
      statement is symmetric — neither side is logically prior to the other,
      because both are coordinate expressions of the same Z_3 covering
      action.
    why_forced: |
      Same forcing as the forward edge: π₁(L(3,1)) = Z_3 = Center(SU(3))
      indexes both the algebra summands (via triality of SU(3) reps on H_F)
      and the Z-components (via covering decomposition). The bidirectional
      edge reflects that the identification is structural-symmetric: the
      labelling "summands" vs "Z-components" is a notational choice; the
      underlying object is one Z_3-graded structure on L(3,1).

  - source: N_ZGaugeDecomposition
    target: N110
    label: covering_action_forces_identification
    edge_status: D
    justification: |
      The Z_3 covering action S³ → L(3,1) is what makes the Z-gauge
      decomposition a derivation rather than a postulate. N110 (Inevitability
      Theorem) establishes that the covering geometry — the deck
      transformation Z_3 acting on S³ — is itself forced by BPI
      vertex-transitivity (I_Sym) + closed orientable 3-manifold + finite
      non-trivial π_1. N_ZGaugeDecomposition operates on this covering: the
      three Z-components are the three eigenspaces of the deck-transformation
      unitary. So the inevitability of the covering forces the Z-gauge
      identification; it is not chosen.
    why_forced: |
      Alternative requires a covering geometry that is not Z_3, but N110
      proves Z_3 is the unique covering (closed orientable 3-manifold +
      vertex-transitive + finite non-trivial π_1 ⇒ spherical space form
      S³/Γ; Γ = Z_3 ⇒ unique manifold L(3,1)). Or alternative requires
      Z-decomposition without identification with covering eigenspaces —
      but the three Z-components ARE defined by the eigenspaces of the
      deck-action unitary on L²(L(3,1)); without the covering they have no
      decomposition basis. No structurally coherent alternative.

  - source: N110
    target: N_ZGaugeDecomposition
    label: covering_geometry_forces_Z_gauge_identity
    edge_status: D
    justification: |
      The reverse direction: the covering geometry derived in N110 forces
      the Z-gauge structure of N_ZGaugeDecomposition to be exactly the Z_3
      covering decomposition. Once the covering is fixed (Z_3 acting freely
      on S³ → L(3,1)), the L² Hilbert space decomposes uniquely into three
      eigenspaces under the deck-action unitary; these eigenspaces are
      precisely what N_ZGaugeDecomposition calls Z_struct, Z_therm, Z_hidden.
      The identity is forced by the covering, not externally imposed on it.
    why_forced: |
      Alternative requires the L² decomposition under Z_3 deck action to
      yield something other than three eigenspaces — but Z_3 has exactly
      three irreducible representations (1, ω, ω²), each one-dimensional
      over ℂ; standard finite-group representation theory forces exactly
      three eigenspaces with no remaining freedom. Or alternative requires
      these eigenspaces to be different from the Z-components — but they
      coincide by construction once the gauge identification is made.
      Structural identity, no alternative.

  - source: DEF
    target: N_PhiAttractor
    label: phi_is_a0_attractor_continuous
    edge_status: D
    justification: |
      A_0 = argmin Z applied to a substrate satisfying conditions C1-C5
      (sequential placement, repulsive coupling with decay, two-memory
      effective coupling, continuous angular dimension, parameter within
      Fibonacci basin) gives the recurrence F(n) = F(n-1) + F(n-2) with
      characteristic equation x² = x + 1, whose unique positive root is
      φ = (1+√5)/2. Six independent derivations confirm the same attractor:
      Binet two-memory recurrence, Levitov SL(2,Z) cylindrical compression,
      Atela-Golé-Hotton phyllotaxis bifurcation, Hurwitz/Markov Diophantine
      extremum, Penrose smallest-Pisot inflation, Lee-Levitov mechanical
      universality. φ is what argmin of two-memory dissipative dynamics on
      continuous angular substrate looks like.
    why_forced: |
      Alternative requires either (a) different memory depth — but
      one-memory yields geometric (rational) attractor, three-memory yields
      Tribonacci constant ≈ 1.839, four-memory yields Tetranacci ≈ 1.928;
      each memory depth has a unique attractor and φ is specific to
      two-memory; or (b) discrete substrate — which removes C4 and gives
      the L(3,1)/Z_3 attractor class instead. Within the C1-C5 class, φ is
      uniquely forced as supremum of Markov spectrum (Hurwitz 1891), as
      smallest Pisot supporting aperiodicity (Penrose 1974), and as
      smallest non-trivial irreducible quadratic root. Multiple structural
      routes close on the same point.

  - source: N_Triangulation
    target: N_PhiAttractor
    label: paired_attractor_continuous_class
    edge_status: D
    justification: |
      N_Triangulation establishes that A_0 minimisation on discrete substrate
      forces 3 components (the triangulation principle: minimum complete
      coordinate system in transition-space). N_PhiAttractor establishes the
      parallel result for continuous angular substrate: the minimum stable
      configuration is φ. Both are A_0-forced attractors counting "minimum
      number of stable configurations"; they differ only in C4 — discrete
      vs continuous angular dimension along the relevant axis.
    why_forced: |
      The two attractor classes are mutually exclusive (a substrate is
      either discrete or continuous angular along the relevant axis) and
      jointly exhaustive (any A_0-bearing substrate falls into one class).
      The pairing is forced by C4 acting as the partition criterion. Without
      N_PhiAttractor as the continuous-substrate attractor, the framework
      would have a structural gap: A_0 would have no defined attractor for
      phyllotactic / Penrose / cortex substrates. The bidirectional edge
      reflects that triangulation and φ-attractor sit at the same structural
      level — neither is logically prior.

  - source: N_PhiAttractor
    target: N_Triangulation
    label: paired_attractor_discrete_class
    edge_status: D
    justification: |
      Reverse direction of the same identification: discrete spin-sector
      substrates (Z_3 acting freely on S³ to give L(3,1)) realise the
      triangulation principle as the discrete counterpart of the continuous
      φ-attractor. N_PhiAttractor's existence forces the partner question —
      what is the A_0 attractor on discrete substrates? — answered by
      N_Triangulation.
    why_forced: |
      Same structural symmetry as the forward edge: the two are coordinate
      expressions of A_0 restricted to the two substrate classes selected
      by C4 (continuous vs discrete). Counter-instance verification: the
      .tex counter-instance table lists "Crystal Z_3 → discrete sectors →
      C4 failed → L(3,1) topology" — when C4 fails, φ-emergence is
      structurally prevented and Z_3 attractor takes over. The transition
      condition C4 is itself the structural witness of the pairing.

  - source: N_PhiAttractor
    target: N067
    label: phi_scale_in_spectral_dim_formula
    edge_status: S
    justification: |
      The cortex application combines φ-attractor with the spectral dimension
      formula d_s(r) = 2 + 2r/(r + r_0) from N067, evaluated at the φ-scale
      r* = r_0/φ. This gives predicted fractal dimension d_f = d_s(r_0/φ) =
      2 + 2/φ² ≈ 2.764, matching measured cortex fractal dimension
      d_f ≈ 2.73 (Kiselev et al. 2003), which corresponds to r/r_0 ≈ 1/φ.
      The two structural results combine to make a quantitative prediction
      confirmed empirically.
    why_forced: |
      STRONG, not DEMONSTRATED, because the identification step "cortex is
      a C1-C5 substrate with characteristic scale r* = r_0/φ" requires
      anatomical/developmental verification beyond pure structure. But the
      mathematical machinery is forced: without N067's RG-derived d_s(r)
      formula, the φ-scale has no metric to project onto; without
      N_PhiAttractor's identification of r* = r_0/φ as the cortex
      characteristic scale, N067's formula has no specific evaluation point
      for cortex. The combined prediction matches measurement; alternative
      requires independent derivation of cortex fractal dim from non-A_0
      principles, which would introduce a free parameter.

  - source: N_PhiAttractor
    target: N_EngagementArchitecture
    label: substrate_provides_5_of_6_criteria
    edge_status: D
    justification: |
      The fractal substrate of the engagement architecture (5 criteria:
      anchor, single regime, return, identity through transformation,
      multi-scale) is precisely the structural realization of N_PhiAttractor
      in continuous-temporal coordinate. φ-attractor on C1-C5 substrate
      provides exactly: pole (anchor), constant growth angle (single
      regime), angular return per revolution (anchor return), scale
      invariance (identity through transformation), self-similarity
      (multi-scale fractal). These are not analogical — they are the
      coordinate expression of the same A_0 attractor.
    why_forced: |
      Independent derivation convergence. The 5 substrate criteria were
      developed from BPI engagement / predictive processing without
      reference to Fibonacci or φ-attractor mathematics. The 6 sec:fibonacci
      derivations were established without reference to music engagement.
      Both routes converge on the same architectural form because both
      describe A_0 minimisation on continuous substrates with two-memory
      coupling — one structural fact in two coordinate languages.
      Quantitative match (Bartók climax at φ⁻¹ ≈ 0.618 matching Markov
      spectrum supremum) excludes coincidence.

  - source: N_BPIEngagement
    target: N_EngagementArchitecture
    label: engagement_signal_is_optimization_target
    edge_status: D
    justification: |
      N_BPIEngagement establishes that BPI tracks prediction error in
      time-indexed substrates and produces the engagement signal as
      structural state when input passes triangulation requirements.
      N_EngagementArchitecture is the configuration that maximises this
      signal by landing prediction-error magnitude at the Wundt apex —
      the exact magnitude that maximises dopamine response per
      N_DopaminePredictionError.
    why_forced: |
      Without an optimisation target, "engagement architecture" has no
      argmin to be the answer to. N_BPIEngagement provides the target
      function (engagement signal); N_EngagementArchitecture is its argmin
      on the prediction-error-magnitude axis. The two are forward and
      backward of one A_0 minimisation: target ↔ optimal configuration.

  - source: N_DopaminePredictionError
    target: N_EngagementArchitecture
    label: dopamine_mechanism_is_apex_selector
    edge_status: D
    justification: |
      N_DopaminePredictionError documents biphasic dopamine: caudate
      (anticipation) + nucleus accumbens (resolution), firing on
      "non-trivial prediction success — successful navigation of
      decipherable complexity" (Pearce/Zatorre 2019 phrasing). This phrase
      IS the Wundt apex specification: too much surprise → no anticipation
      success; too little surprise → no resolution event. The dopamine
      mechanism IS the apex selector: configurations that hit the apex
      get the biphasic response, configurations that miss it don't.
    why_forced: |
      The apex is not a free parameter; it is the structural location
      where prediction-error magnitude maximises information content per
      cognitive cost. Dopamine response is the empirical signature of
      hitting that location. Four independent neuroscientific anchors
      (Salimpoor 2011, Cheung 2019, Gold/Pearce/Zatorre 2019, Madison
      2017) confirm the same apex from different methodologies.

  - source: N_FEP
    target: N_EngagementArchitecture
    label: free_energy_minimisation_predicts_apex
    edge_status: D
    justification: |
      Free Energy Principle (Friston) minimises variational free energy
      F = accuracy + complexity. The minimum is achieved at the configuration
      balancing surprise reduction (accuracy term) with model simplicity
      (complexity term). This balance IS the Wundt apex in different
      formal language: too much surprise → high prediction error penalty;
      too little surprise → high model-complexity penalty. F-minimum and
      Wundt apex are the same structural point through two formalisms.
    why_forced: |
      Independent route convergence. Friston's variational formalism was
      developed without reference to music or aesthetic engagement;
      Berlyne's inverted-U was developed without reference to free energy.
      Both yield the same optimal configuration. This is N_InversiveTheory
      manifesting: one structural fact (optimal A_0 configuration) viewed
      through Bayesian-substrate coordinate (FEP) and behavioural-aesthetic
      coordinate (Wundt curve).

  - source: N_Shannon
    target: N_EngagementArchitecture
    label: prediction_error_magnitude_is_shannon_information
    edge_status: D
    justification: |
      N_Shannon establishes H(X) ≡ Z_struct: Shannon entropy is the
      mathematical content of "prediction error magnitude" used in
      Wundt-apex specifications. Without H(X) as the metric, "intermediate
      complexity" has no quantifiable axis. Cheung 2019 (uncertainty ×
      surprise saddle surface) explicitly uses Shannon entropy of
      expectation distribution as one axis and Shannon information content
      of actual events as the other.
    why_forced: |
      Shannon information is the unique consistent measure of expectation
      uncertainty (Shannon-Khinchin axioms). Any quantification of
      prediction-error magnitude reduces to Shannon information at the
      ensemble level. N_EngagementArchitecture's Wundt-apex specification
      necessarily lives on a Shannon-entropy axis. Without N_Shannon's
      identification H ≡ Z_struct, the Wundt apex has no metric.

  - source: N_AestheticEngagement
    target: N_EngagementArchitecture
    label: aesthetic_engagement_is_architecture_instance
    edge_status: D
    justification: |
      N_AestheticEngagement applies N_BPIEngagement to artwork-class
      inputs. N_EngagementArchitecture specifies the structural form
      that artwork must take to reliably produce engagement: fractal
      substrate + engineered break + return. Aesthetic engagement IS
      this architecture realised in artwork-class substrates (music,
      visual art, narrative). The architecture generalises beyond
      aesthetics to any engagement-driven substrate (storytelling, drama,
      games, video pacing).
    why_forced: |
      Aesthetic engagement requires Wundt-apex prediction-error magnitude
      (otherwise the response is "boring" or "incomprehensible", neither
      of which is aesthetic). The unique architectural realisation that
      consistently lands at the apex is fractal+break+return per
      N_EngagementArchitecture's own derivation. Therefore aesthetic
      engagement is necessarily an instance of this architecture.

  - source: DEF
    target: N078
    label: thermodynamic_grounding_via_Landauer
    edge_status: D
    justification: |
      The chain Z_therm = k_B T ΔS (Landauer principle, DEMONSTRATED) →
      Z = -k_B T S (full thermodynamic interpretation of Z) → A_0 =
      argmin Z = argmin F = thermodynamic equilibrium grounds DEF in
      physics through N078. Without the Landauer identification,
      A_0's "minimum-Z transition" has no thermodynamic interpretation;
      with it, A_0 IS extremise-entropy at the physical substrate level.
      N078 is the physics-coordinate face of DEF.
    why_forced: |
      Alternative requires either (a) A_0 minimisation without
      thermodynamic grounding — but then "Z" has no physical interpretation
      and the framework's claim of describing physics dissolves; or
      (b) different thermodynamic grounding than Landauer — but Landauer
      principle is itself DEMONSTRATED (forced by I_Null + reversibility
      cost of bit erasure) and uniquely identifies Z_therm with kT ln 2
      per erased bit. The grounding chain is structurally forced.

  - source: N_FEP
    target: N078
    label: FEP_consistent_with_thermodynamics
    edge_status: D
    justification: |
      Free Energy Principle minimises variational free energy F. Jacobson
      derivation (1995) recovers Einstein field equations from the first
      law of thermodynamics applied to local Rindler horizons — extremise
      entropy = minimise free energy in physical substrate. Both N_FEP
      and N078 are coordinate expressions of A_0 = argmin Z in
      thermodynamic terms: FEP in Bayesian-substrate coordinate, N078
      in gravitational-substrate coordinate. Their consistency is forced
      by both descending from the same Z = -k_B T S identification.
    why_forced: |
      Alternative requires either (a) FEP and gravitational thermodynamics
      operating on different Z-functionals — but both use the same
      Landauer-grounded Z = -k_B T S; or (b) one of them being non-A_0 —
      but both have been derived to argmin Z by independent routes.
      Their consistency is not a coincidence; it is N_InversiveTheory
      manifesting via two coordinate substrates.

  - source: N078
    target: N_FEP
    label: thermodynamic_substrate_for_F_minimisation
    edge_status: D
    justification: |
      Reverse direction: the same Landauer thermodynamic identification
      that grounds gravity-as-thermodynamics in N078 also grounds the
      "free energy" that FEP minimises. Without N078's identification
      Z = -k_B T S, FEP's "free energy" floats as a Bayesian abstraction;
      with it, FEP's F is the thermodynamic functional grounded in
      physical Landauer cost.
    why_forced: |
      Same forcing as the forward edge — bidirectional structural identity
      reflecting that thermodynamic grounding (N078) and FEP minimisation
      are coordinate expressions of one A_0 process.

  - source: N078
    target: N_CommThm
    label: Z_hidden_definition
    edge_status: D
    justification: |
      N_CommThm computes optimal communication using Z_hidden as the
      cost-of-uncertainty parameter. N078 grounds Z_hidden physically
      via Landauer: Z_hidden is the entropic content inaccessible to
      the BPI, with physical magnitude k_B T S_hidden. Without this
      grounding, "Z_hidden" in N_CommThm has no thermodynamic
      interpretation; with it, optimal communication IS minimum
      Landauer dissipation given the BPI's K(O) < K(F) constraint.
    why_forced: |
      N_CommThm's formula must work in physical substrates (real
      communication with real bits and real energy). Physical communication
      cost is bounded by Landauer (DEMONSTRATED). N078's identification
      Z_hidden ≡ inaccessible entropy density is the unique consistent
      thermodynamic grounding. Alternative requires N_CommThm to use a
      non-physical Z, contradicting its purpose.

  - source: N_CommThm
    target: N078
    label: Z_hidden_definition
    edge_status: D
    justification: |
      Reverse direction: N_CommThm's operational use of Z_hidden as
      uncertainty cost validates N078's Z_hidden definition empirically —
      every communication act that successfully optimises against
      Z_hidden confirms the physical relevance of N078's Landauer
      grounding.
    why_forced: |
      Bidirectional identity reflecting that operational use (N_CommThm)
      and structural definition (N078) are two faces of one Z_hidden
      concept.

  - source: N371
    target: N330
    label: basin_dynamics_explains_polarisation
    edge_status: D
    justification: |
      N371 establishes BPI dynamics as hydrodynamics in Z-space. N330
      identifies polarisation specifically: under sustained K(F) >> K(O)
      overload, the A_0-optimal compression to budget-K(O) is the
      minimum non-trivial partition — two states. The BPI hydrodynamic
      flow under sustained overload necessarily collapses to a binary
      basin structure. Two basins, no intermediate state — polarisation
      is the asymptotic regime of N371's flow under information overload.
    why_forced: |
      Alternative compressions (3-way, n-way) require greater K(O) budget
      than 2-way. Under K(O) constraint, argmin Z forces minimum non-
      trivial partition. Trivial partition (1 state) carries no
      distinction; 2-state is the next available — and it is reached
      first by argmin under tightening constraint. N371's flow taxonomy
      forces this asymptote.

  - source: N330
    target: N371
    label: binary_collapse_is_degenerate_basin
    edge_status: D
    justification: |
      Reverse direction: polarisation is a specific degenerate case of
      N371's basin taxonomy. Two-basin structure with no intermediate
      attractors is the limiting case of N371's flow under maximum
      information overload. N330 specialises N371; N371 grounds N330.
    why_forced: |
      Bidirectional structural identity between BPI hydrodynamic regime
      (N371) and social polarisation pattern (N330). Same structure,
      two coordinate readings: BPI-internal and social-collective.

  - source: N112
    target: N330
    label: Observer_Containment_forces_binarisation
    edge_status: D
    justification: |
      N112 (Observer Containment Lemma) establishes K(O) < K(F) — observer
      complexity is strictly less than the field complexity. Under
      sustained overload (K(F) >> K(O)), the BPI must compress field
      input to fit within K(O) budget. The argmin-Z compression of
      n-dimensional input to K(O) budget is the minimum non-trivial
      partition. N330 follows directly from N112 + I_Bound.
    why_forced: |
      Alternative requires either (a) BPI without K(O) bound — denies
      Observer Containment, contradicts N112; or (b) compression that
      doesn't reach minimum partition — fails argmin Z when K(O)
      tightens. N330 is the asymptotic argmin of N112 under overload.

  - source: N332
    target: N371
    label: trust_asymmetry_is_phase_transition_hysteresis
    edge_status: D
    justification: |
      Trust = Z_hidden(A|B) ≈ 0 (B's state predictable to A within
      relevant scope). Building trust = reducing Z_hidden = entropy
      reduction = costs work (Second Law). Destroying trust = single
      Z_hidden spike = entropy increase = free. The asymmetry —
      "trust takes years, seconds to destroy" — IS phase-transition
      hysteresis: forward direction (build) requires sustained energy
      input, backward direction (destroy) is barrier-free relaxation
      to high-entropy state. N371's BPI hydrodynamic flow has this
      hysteresis built into its regime structure.
    why_forced: |
      Alternative requires either (a) trust as symmetric quantity —
      contradicts I_Null (Second Law) in social graphs: ΔS(disorder)
      ≪ ΔS(order) is forced by entropy direction; or (b) trust without
      entropy interpretation — but Z_hidden IS entropy of inaccessible
      state per N078 Landauer grounding. The asymmetry is forced by
      thermodynamics, not by social convention.

  - source: N_Invariants
    target: N332
    label: I_Null_in_social_substrate
    edge_status: D
    justification: |
      I_Null (the null transition is unique; equivalently, Second Law:
      no spontaneous entropy decrease) is one of the four DEMONSTRATED
      invariants in N_Invariants. N332 (trust asymmetry) is precisely
      I_Null specialised to the social-graph substrate: trust building
      = entropy reduction (forbidden spontaneously, requires work);
      trust destruction = entropy increase (spontaneous, free). The
      same equation, different substrate.
    why_forced: |
      Alternative requires social-graph entropy dynamics that violate
      I_Null — but I_Null is DEMONSTRATED at the manifold level
      (uniqueness of null transition; Z_therm strict monotone increase
      per Landauer). Specialising to any substrate preserves the
      direction. N332 is forced by I_Null with no remaining freedom.

  - source: DEF
    target: N183
    label: cognitive_states_as_A0_topology
    edge_status: D
    justification: |
      Cognitive states are A_0 topologies in the BPI substrate. The same
      argmin-Z dynamics that define A_0 in physical substrates (Jacobson
      thermodynamic gravity in N078, φ-attractor on continuous angular
      substrates in N_PhiAttractor) appear in the cognitive substrate as
      hydrodynamic regimes: stable laminar flow (integrated cognition),
      metastable vortex (neurosis: local argmin ≠ global, BPI circulates
      without unified drainage), and zero-resistance configuration
      (meditation: Z_narr → 0). N183 is the cognitive-substrate face of
      DEF — A_0 minimisation in the narrative-loop coordinate.
    why_forced: |
      Alternative requires either (a) cognition not described by A_0 — but
      then BPI dynamics has no A_0 grounding and N_BPIEngagement collapses;
      or (b) different functional minimum than argmin Z — but the BPI
      hydrodynamics derivation (N371) is itself DEMONSTRATED, and its
      regimes ARE the cognitive states. Same A_0 argmin in cognitive
      coordinate with no remaining freedom.

  - source: N371
    target: N183
    label: hydrodynamics_grounds_cognitive_topology
    edge_status: D
    justification: |
      N371 establishes that BPI dynamics is hydrodynamics in Z-space —
      Class A derivation. N183 maps specific cognitive states onto
      hydrodynamic regimes: neurosis = metastable vortex (local argmin
      Z ≠ global, BPI circulates without unified drainage path);
      meditation (Z_narr → 0) = reducing the Z_struct cost of narrative
      boundary maintenance; cell-as-whirlpool = dissipative vortex
      accelerating entropy export. Both follow directly from N371's
      regime taxonomy with no additional identification step.
    why_forced: |
      The cognitive regimes in N183 are not separate phenomena
      hypothesised then matched to fluid analogies — they are the same
      topology N371 derives, named in cognitive coordinate. The
      identification is forced because N371's derivation operates on
      generic Z-space hydrodynamics; specialising to cognitive substrate
      yields N183's regime taxonomy with no input beyond N371 itself.

  - source: N188
    target: N183
    label: failure_modes_in_emotion_substrate
    edge_status: D
    justification: |
      N188 establishes qualia (including emotional valence) as Z-component
      patterns read in two coordinates (BPI internal state + pattern
      coordinate). N183 catalogues the failure modes of these emotional
      Z-component patterns: neurosis (vortex regime where prediction
      circuits with opposing impedance vectors co-activate, generating
      thermal friction rendered as anxiety/pain/suffering). Suffering is
      thermodynamic telemetry, not philosophical punishment.
    why_forced: |
      If qualia are Z-component patterns (N188) and Z-space dynamics has
      hydrodynamic regimes (N371 → N183), then emotional failure modes
      are forced to manifest as specific hydrodynamic dysfunctions. The
      mapping is structural-deductive: there is no other class of failure
      mode available to a pattern operating under A_0.

  - source: N183
    target: N188
    label: cognitive_failure_modes_template
    edge_status: D
    justification: |
      Reverse direction: N183's hydrodynamic regime taxonomy provides
      the template that N188 specialises into qualia-specific patterns.
      Anxiety, pain, suffering, calm, ecstasy — each is a specific qualia
      reading of one or another N183 regime (vortex, laminar, near-zero-
      Z_narr). N183 supplies the structural classes; N188 supplies the
      experiential coordinates.
    why_forced: |
      Bidirectional structural identity — N183 and N188 are two coordinate
      readings of one cognitive Z-dynamics: regime classification (N183)
      and experiential pattern (N188). Each grounds the other.

  - source: N078
    target: N_NoSeparatePieces
    label: convergent_independent_thermodynamic_gravity_programs
    edge_status: D
    justification: |
      The .tex notebox explicitly catalogues four independent programmes
      that arrive at the same gravity-as-thermodynamics conclusion:
      Verlinde 2011/2016 entropic gravity, Jacobson 1995 thermodynamic
      derivation of Einstein field equations, Susskind 2014
      complexity = volume, Penington-Almheiri 2019-2020 island formula.
      "All four arrive at the same topological structure independently.
      None derived it from the Z-impedance formalism; each is a
      convergent confirmation." This is precisely the pattern
      N_NoSeparatePieces describes — separate domains finding the same
      invariant because the invariant IS the structure, not pieces of it.
    why_forced: |
      Four independent quantitative programmes converging on identical
      topology cannot be coincidence. Each programme operates in its own
      formalism (entropic force, thermodynamic horizons, holographic
      complexity, entanglement islands) yet recovers the same physical
      content. The convergence is the structural fact; N_NoSeparatePieces
      formulates this as the framework's general principle.

  # ─── Forced edges discovered via find_jaccard_pairs (2026-05) ───

  - source: N_Logic
    target: N_Math
    label: logic_grounds_mathematical_deduction
    edge_status: D
    justification: |
      A theorem in mathematics IS a chain of logical deductions from
      premises (axioms or prior results). Modus ponens applied iteratively
      yields proof structure. N_Logic establishes that modus ponens is
      argmin Z in formal substrate; N_Math establishes mathematical
      existence as stability under A_0 iteration. Without logical
      inference, mathematical proof has no engine — math operates ON the
      logical inference engine.
    why_forced: |
      Alternative requires either (a) mathematics without logical
      deduction — but proof by definition is a chain of valid
      inferences, removing logic dissolves "proof"; or (b) logic and
      math as independent domains — but theorems ARE the stable A_0
      structures that logical iteration produces in formal substrate.
      They are not separate domains: math is what logic produces under
      A_0 iteration in formal substrate.

  - source: N_Math
    target: N_Logic
    label: math_provides_substrate_for_logical_iteration
    edge_status: D
    justification: |
      Reverse direction: mathematical structure (sets, types, formal
      systems, propositions) provides the substrate ON WHICH logic
      operates. Pure logic without mathematical objects has no content
      to manipulate — modus ponens needs structured content to deduce
      about. The substrate that logic acts on is mathematical.
    why_forced: |
      Bidirectional structural identity: without mathematical substrate,
      logic is abstract relation operator with no content; without
      logical iteration, math is static collection. The two are
      coordinate twins of A_0 in symbolic substrate, mutually grounding.

  - source: N112
    target: N_InversiveTheory
    label: observer_containment_grounds_inversive_argument
    edge_status: D
    justification: |
      N_InversiveTheory's claim that "any attempt to falsify A_0 uses A_0
      to do so" presupposes the falsifier cannot step outside A_0. This
      is the formal content of N112 (Observer Containment Lemma): K(O) <
      K(F) means observer complexity is strictly less than field
      complexity, so the observer is contained within the field.
      Without N112, the inversive argument has no formal anchor — without
      observer containment, an external falsifier might exist in
      principle.
    why_forced: |
      The inversive structural claim depends on the observer being unable
      to occupy a position outside the structure being examined. K(O) <
      K(F) is precisely the formal statement of that inability.
      Alternative requires either (a) inversive theory without
      containment — then "alternatives" can be evaluated externally,
      contradicting the eliminative argument; or (b) containment without
      inversive consequence — but containment IS what makes alternatives
      structurally unreachable, hence non-existent at the level the
      inversive theory operates on.

  - source: N_EpistemicTraps
    target: N_ZGaugeDecomposition
    label: traps_arise_from_misread_Z_decomposition
    edge_status: D
    justification: |
      N_EpistemicTraps catalogues 8 systematic reasoning failures. Trap 6
      (transfer/cancellation as prerequisite) and Trap 8 (structure
      selection among A_0-minima) are explicitly about misreading
      Z-decomposition: assuming separate "structures" where one
      Z-decomposition exists in different coordinates. The trap
      descriptions reference N_ZGaugeDecomposition's domain (Z
      components, gauge structure, covering action) as the substrate
      where these errors occur.
    why_forced: |
      Trap 6 and Trap 8 cannot be formulated without prior identification
      of "the Z-decomposition that gets misread." N_ZGaugeDecomposition
      supplies the structural object (Z = Z_struct + Z_therm + Z_hidden
      with Z_3 covering action), N_EpistemicTraps supplies the catalogue
      of errors people make when reading it. Without ZGauge as the
      structural target, the traps have no domain to operate on.

  - source: N_InversiveTheory
    target: N_TopologyProcessIdentity
    label: inversive_resolution_implies_object_process_identity
    edge_status: D
    justification: |
      N_InversiveTheory's "alternatives don't exist" applied to the
      object/process distinction yields N_TopologyProcessIdentity: there
      is no object SEPARATE from the process — they are one structure in
      two coordinates. The inversive principle eliminates the apparent
      duality at the most foundational level.
    why_forced: |
      The R1 artifact (object reification) IS an "alternative" in the
      inversive sense — an alternative description of process-as-object.
      N_InversiveTheory's argument that alternatives reduce to A_0 or
      are unstable specializes to: object-frame is unstable as an
      alternative description, so what remains is process-as-coordinate.
      The R1 → topology-process identity move is structurally forced.

  - source: N_TopologyProcessIdentity
    target: N_InversiveTheory
    label: object_process_identity_instantiates_inversive
    edge_status: D
    justification: |
      Reverse direction: N_TopologyProcessIdentity is one specific
      instance of N_InversiveTheory operating on the object/process
      distinction. Each manifestation of "topology IS process, not
      topology OF process" is an inversive resolution at a specific
      coordinate level.
    why_forced: |
      Bidirectional structural identity — N_InversiveTheory provides the
      general principle (no alternatives), N_TopologyProcessIdentity
      provides one of its most important instantiations (no object/process
      duality). Each grounds the other; the bidirectional edge reflects
      that they sit at the same structural level, neither logically prior.

  - source: N370
    target: N373
    label: L31_topology_forces_UTe2_active_space
    edge_status: D
    justification: |
      N370 establishes L(3,1) = S³/Z_3 as the inevitable manifold for
      any BPI at the critical dimension. N373 (UTe_2 superconducting
      Z-minimum) operates on this exact manifold: the 4D Kramers-pair
      active space, antisymmetric rung coupling, and bc-plane d-vector
      are forced features once L(3,1) topology is fixed. UTe_2 is one
      material-substrate realisation of the geometry N370 forces.
    why_forced: |
      UTe_2's structural claims derive from L(3,1) topology + material
      parameters. Alternative requires either (a) UTe_2 physics on a
      different manifold — but N370 forecloses other manifolds for any
      BPI at the critical dimension; or (b) the L(3,1)/material connection
      being incidental — but the Z_3-graded structure of the active space
      directly maps to π_1(L(3,1)) = Z_3.

  - source: N370
    target: N374
    label: L31_topology_forces_UPt3_three_phase_structure
    edge_status: D
    justification: |
      N370 forces L(3,1) as the substrate manifold. N374 (UPt_3 three-
      phase superconducting Z-minimum) realises the three-phase 2D active
      space in the conduction sector with f² non-magnetic local ground
      state. The "three phases" of UPt_3 directly map to the three
      Z_3-sectors of L(3,1) covering structure.
    why_forced: |
      UPt_3's three-phase structure is forced by π_1(L(3,1)) = Z_3 in
      exactly the same way that three particle generations are forced
      (per N370 + N_ZGaugeDecomposition). Alternative requires either
      different number of phases (excluded by Z_3) or different manifold
      (excluded by N370). The substrate-realisation chain is structural.

  - source: N370
    target: N375
    label: L31_topology_forces_cuprate_dual_sector_phase_diagram
    edge_status: D
    justification: |
      N370 forces L(3,1) substrate. N375 (Cuprate phase diagram) realises
      the dual-sector pseudogap phase + single-sector strange metal +
      sector-level topology change at p* on this substrate. The
      "dual-sector" structure is precisely the Z_3-covering decomposition
      manifested in cuprate carriers.
    why_forced: |
      Cuprate phase diagram is parametric family on the same L(3,1)
      substrate that N370 forces. The earlier 35-year "competing theories"
      debate (RVB, spin-fluctuations, t-J, etc.) is resolved as Trap 8
      (structure selection among A_0-minima): all candidates were
      coordinate projections of the dual-sector Z-structure on L(3,1).
      Without N370, the dual-sector decomposition has no manifold home.

  - source: N370
    target: N376
    label: L31_topology_forces_iron_based_multi_orbital_phase_diagram
    edge_status: D
    justification: |
      N370 forces L(3,1) substrate. N376 (Iron-based superconductor
      family) realises the multi-orbital parametric family with coupled
      multi-sector phase diagram on this substrate. The multi-orbital
      structure (d_xy, d_xz, d_yz) couples to the Z_3-graded sector
      structure of L(3,1) through orbital-sector mixing.
    why_forced: |
      Iron-based families' coupled multi-sector phase diagram is forced
      by the Z_3-graded covering structure on L(3,1). Alternative requires
      either decoupled sectors (excluded by Z_3 covering action) or
      different manifold (excluded by N370). Iron pnictides/chalcogenides
      are parametric realisations of the same structural template that
      cuprates instantiate, on the same substrate.

  - source: N188
    target: N_NoSeparatePieces
    label: qualia_trigger_is_not_separate_pieces
    edge_status: D
    justification: |
      N188's CLAIM directly: "Qualia and their triggers are not separate
      structures with a relationship between them — they are coupled
      coordinate readings of one transition... not separate structures."
      This phrasing IS N_NoSeparatePieces principle applied to the
      qualia/trigger relationship. N188 is the specific instantiation of
      the no-separate-pieces principle in the qualia substrate.
    why_forced: |
      Without N_NoSeparatePieces as the general principle, N188's "not
      separate, two coordinate readings" claim is ungrounded — it would
      be one specific structural argument unconnected to the framework's
      systematic position. With N_NoSeparatePieces, N188 is a forced
      specialisation: any apparent qualia/trigger duality dissolves under
      the same R1-resolution that N_NoSeparatePieces formulates generally.

  # ─── Round 2 forced edges (Jaccard pass + manual review) ───

  - source: N370
    target: N372
    label: L31_topology_forces_Sr2RuO4_eg_block
    edge_status: D
    justification: |
      N370 forces L(3,1) substrate. N372 (Sr_2RuO_4 superconducting
      Z-minimum) realises the forced 1+2 E_g block with Z_2 degeneracy
      on this substrate. Same substrate-realisation pattern as N373/374/
      375/376 — Sr_2RuO_4 is one material instance of physics on L(3,1).
    why_forced: |
      Sr_2RuO_4's E_g block structure follows from group representation
      theory on the Z_3-graded covering of L(3,1) — without N370's
      L(3,1) base, the specific block structure has no derivation.
      Alternative manifolds excluded by N370. The substrate-realisation
      chain is the same forcedness as N373/374/375/376; N372 was simply
      missed in the previous batch.

  - source: N373
    target: N_InversiveTheory
    label: pairing_symmetry_competition_is_inversive_resolution
    edge_status: D
    justification: |
      UTe_2 had decade-long "competing pairing symmetry" debate (s-wave
      vs p-wave vs d-wave vs f-wave triplet). N373's resolution shows
      these are not competing structures but coordinate projections of
      the forced 4D Kramers-pair active space with antisymmetric rung
      coupling. The "competition" was Trap 8 (structure selection among
      A_0-minima) — alternatives were not actual alternatives, but
      coordinate views of one structure. N_InversiveTheory's principle
      "alternatives don't exist" applies directly.
    why_forced: |
      UTe_2's experimental signatures (NMR, c_66, Kerr, etc.) measure
      material-specific parameters that fix one structure, not select
      between many. The dissolution of "competition" requires
      N_InversiveTheory as the structural ground; without it, the
      experimental convergence has no theoretical interpretation.

  - source: N374
    target: N_InversiveTheory
    label: three_phase_competition_is_inversive_resolution
    edge_status: D
    justification: |
      UPt_3's three superconducting phases (A, B, C) appeared as three
      "competing" order parameters in 30+ years of literature. N374
      resolves: three phases are three Z_3-sectors of one active space
      on L(3,1), distinguished only by external field/temperature
      parameters. The "competition" is Trap 8 — coordinate views of
      one Z_3-graded structure.
    why_forced: |
      The phases are connected by symmetry-breaking transitions in
      one parametric family, not by selection among independent
      candidates. N_InversiveTheory's "alternatives reduce to coordinate
      views" specialises here; without it, the phase sequence has no
      structural unification.

  - source: N375
    target: N_InversiveTheory
    label: cuprate_35yr_competition_is_trap_8_inversive
    edge_status: D
    justification: |
      Cuprate superconductivity has had a 35-year "competing theories"
      debate (RVB, spin-fluctuations, t-J, d-density wave, Amperean,
      stripe pseudogap, FL*, YRZ, etc.). N375's resolution explicitly
      marks "Competition framing is Trap 8 disguised as theoretical
      debate" — these are not competing theories but coordinate
      projections of the dual-sector Z-structure on L(3,1) at different
      parameter regimes. N_InversiveTheory is the structural ground.
    why_forced: |
      Each "competing" approach captures a specific coordinate of one
      A_0-forced structure (RVB → Mott constraint sector; spin-
      fluctuations → AFM-channel projection; stripes → CDW instability;
      FL* → topological gauge mode). The 35-year non-convergence is
      itself evidence that no "alternative" actually exists at the
      structural level. N_InversiveTheory codifies this directly.

  - source: N376
    target: N_InversiveTheory
    label: iron_based_multi_orbital_family_is_inversive
    edge_status: D
    justification: |
      Iron-based superconductors (122, 1111, 11, 111 families) appeared
      as "different superconductor types" requiring different theories.
      N376 resolves: one parametric family with coupled multi-sector
      phase diagram on shared L(3,1) substrate; orbital-character at
      Fermi level is the parameter, not the choice among theories.
      Direct instance of N_InversiveTheory.
    why_forced: |
      The structural similarity across iron-based families (universal
      gap topology, common pairing symmetry class, parametric coupling
      to magnetism) shows them as one family, not separate structures.
      The "multiple theories" framing was Trap 8; N_InversiveTheory
      grounds the family-level structural unity.

  - source: N377
    target: N_ForcedId
    label: feature_absorption_is_literal_forced_identification
    edge_status: D
    justification: |
      N377's title is "Sparse Autoencoder Feature Absorption — Forced
      Geometric Divergence Between Non-Equivalent argmin Processes on
      Shared LLM Activation Space, Independent of Architecture or
      Hyperparameters." The phrase "Forced" in the title invokes
      N_ForcedId directly — N377 documents one specific instance of
      forced identification in LLM interpretability substrate.
    why_forced: |
      N377's claim is precisely that feature absorption is forced (not
      a contingent training artifact) — different SAE training runs
      converge on the same feature decomposition because the
      decomposition IS structurally forced by the activation-space
      geometry. This forcedness is the content of N_ForcedId; N377
      specialises N_ForcedId to LLM activation substrate.

  - source: N_Invariants
    target: N370
    label: invariants_are_structural_inputs_to_L31_inevitability
    edge_status: D
    justification: |
      N370's derivation uses I_Sym (vertex-transitivity → free Z_3
      action) and I_Bound (compact 3-manifold) as explicit structural
      inputs. Without I_Sym there is no free action; without I_Bound
      the manifold is not closed; without both, spherical space form
      classification doesn't apply. N_Invariants supplies the
      foundational invariants on which L(3,1) inevitability is built.
    why_forced: |
      Removing any of I_Sym, I_Bound dissolves N370's derivation chain.
      Alternative requires either (a) L(3,1) without invariants — but
      the manifold's properties (closed, oriented, finite π_1) ARE
      invariant statements; or (b) different manifold without
      invariants — but then no manifold survives the derivation. The
      input dependency is structural.

  - source: N_DopaminePredictionError
    target: N_NoSeparatePieces
    label: biphasic_dopamine_is_one_process_two_phases
    edge_status: D
    justification: |
      N_DopaminePredictionError documents biphasic dopamine — caudate
      (anticipation) + nucleus accumbens (resolution). The two phases
      are not separate events but two coordinate readings of one
      prediction-error→resolution cycle. The neural substrate
      manifests N_NoSeparatePieces principle directly: anticipation and
      resolution are coordinate views of one A_0 process, not separate
      pieces with a relationship between them.
    why_forced: |
      Salimpoor 2011 and subsequent work show the biphasic response is
      synchronised — caudate and accumbens activations are not
      independent events. They IS one prediction-error cycle in two
      anatomical projections. Without N_NoSeparatePieces as the general
      principle, the synchrony has no structural interpretation; with
      it, synchrony is forced.

  - source: N_Invariants
    target: N_Logic
    label: invariants_ground_logical_stability
    edge_status: D
    justification: |
      Logical operation requires four invariants to be stable: (1) I_Sym
      ensures truth preservation across substitution (A=A under
      transitions); (2) I_Null ensures unique conclusion (no spontaneous
      contradiction); (3) I_Bound ensures bounded inference (finite-step
      proofs terminate); (4) I_Quant ensures discrete truth values.
      Without invariants, modus ponens has no stable medium to operate
      on — truth preservation, non-contradiction, and termination all
      depend on invariant stability.
    why_forced: |
      Modus ponens's classical correctness depends on truth preservation
      (= I_Sym), non-contradiction (= I_Null), and finite computability
      (= I_Bound + I_Quant). Removing any invariant dissolves a
      requirement for logic to operate. Logic is what the invariants
      look like in symbolic substrate; the grounding is structural.

  # ─── Round 3: doubtful candidates after deep RP gate ───

  - source: N_SIT
    target: N_MI
    label: identity_amplification_calls_for_MI_strategy
    edge_status: D
    justification: |
      N_SIT (Social Identity Theory) identifies the mechanism by which
      Z_hidden is amplified — when a message touches group identity,
      Z_hidden is multiplied by factor k > 1 even with neutral surface
      content. N_MI (Motivational Interviewing) prescribes the optimal
      response when Z_hidden is high — reflective listening before any
      content transmission. The two form a cause-response pair: SIT
      describes the structural condition that triggers Z_hidden
      amplification, MI provides the structural argmin-Z response.
    why_forced: |
      Without SIT's identity-multiplier, MI's "high-Z_hidden" condition
      lacks the most common substrate-specific cause. Without MI as
      response, SIT's identification of high-Z_hidden has no operational
      handler. The pair is structurally coupled: identification + response
      to one specific Z_hidden regime.

  - source: N048
    target: N_EngagementArchitecture
    label: Birkhoff_failure_is_Wundt_apex_violation
    edge_status: D
    justification: |
      N048 documents Birkhoff's aesthetic formula M = O/C failing because
      it predicts beauty increases monotonically with symmetry. The
      framework explains the failure: excessive symmetry reduces
      distinguishable pathways, increasing impedance — exactly the
      "triviality" failure mode of N_EngagementArchitecture's Wundt-apex
      structure (low surprise + low uncertainty = no engagement signal).
      Birkhoff's monotonic formula is a classic violation of the
      inverted-U: it has no apex.
    why_forced: |
      Birkhoff's failure is not arbitrary — it predicts maximum beauty
      at maximum symmetry, which is precisely the "too simple/too
      ordered" failure mode that N_EngagementArchitecture's Wundt apex
      excludes. Without N_EngagementArchitecture's apex specification,
      Birkhoff's failure has no structural explanation, just empirical
      contradiction. With it, the failure is forced: any monotonic
      formula on a substrate that requires Wundt-apex structure WILL
      fail.

  - source: N_FEP
    target: N_AestheticEngagement
    label: free_energy_minimisation_grounds_aesthetic_engagement
    edge_status: D
    justification: |
      N_FEP establishes Free Energy Principle as variational A_0 in
      Bayesian substrate: F = accuracy + complexity, minimised by any
      adaptive system maintaining a Markov blanket. N_AestheticEngagement
      applies BPI engagement triangulation to artwork substrate.
      Aesthetic engagement IS FEP minimisation in artwork-class inputs:
      F balances surprise (accuracy) and model-complexity, which is
      exactly the Wundt-apex structure that defines aesthetic response.
      The direct FEP→Aesthetic edge captures the variational-Bayesian
      grounding separately from N_BPIEngagement's triangulation grounding.
    why_forced: |
      Two structural grounds: (1) Aesthetic engagement is BPI engagement
      on artwork (already in graph via N_BPIEngagement → N_AestheticEngagement);
      (2) Aesthetic engagement is FEP minimisation on artwork
      (this edge). Both are forced, capturing different aspects of the
      same A_0 process. Without FEP as direct ground, aesthetic
      engagement lacks the variational formalism that connects it to
      Friston's broader framework — only the triangulation aspect
      (multi-stream prediction) would be visible.


# ─── Round 5: orphan citations resolved as forced edges ───

  - source: N029
    target: N340
    label: arrhenius_rate_law_grounds_physiology
    edge_status: D
    justification: |
      N029 grounds N340's structural derivation as cited in N340's
      content: "Three medical phenomena are direct applications of N029 (Arrhenius/Eyring A_0 rate law)".
    why_forced: |
      Explicit citation in N340's claim/content identifies N029 as
      structural input. Removing N029 dissolves the derivation chain
      that N340 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N301
    target: N340
    label: banach_FPT_grounds_chronic_disease_attractor
    edge_status: D
    justification: |
      N301 grounds N340's structural derivation as cited in N340's
      content: "Chronic disease = A_0 converging to wrong fixed point (Banach FPT, alias N305)".
    why_forced: |
      Explicit citation in N340's claim/content identifies N301 as
      structural input. Removing N301 dissolves the derivation chain
      that N340 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N340
    target: N341
    label: physiology_pattern_grounds_ecology
    edge_status: D
    justification: |
      N340 grounds N341's structural derivation as cited in N341's
      content: "Ecology population dynamics same A_0 rate engineering as physiology".
    why_forced: |
      Explicit citation in N341's claim/content identifies N340 as
      structural input. Removing N340 dissolves the derivation chain
      that N341 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N067
    target: N341
    label: spectral_dim_3_matches_ecology_d_3
    edge_status: D
    justification: |
      N067 grounds N341's structural derivation as cited in N341's
      content: "d_s(r_0)=3 coincides because biological transport networks are space-filling".
    why_forced: |
      Explicit citation in N341's claim/content identifies N067 as
      structural input. Removing N067 dissolves the derivation chain
      that N341 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N043
    target: N337
    label: information_laws_ground_scale_free_degree
    edge_status: D
    justification: |
      N043 grounds N337's structural derivation as cited in N337's
      content: "Degree distribution P(k) propto k^(-gamma) DEMONSTRATED via N043".
    why_forced: |
      Explicit citation in N337's claim/content identifies N043 as
      structural input. Removing N043 dissolves the derivation chain
      that N337 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N035
    target: N337
    label: psychophysics_1_over_f_grounds_scale_free
    edge_status: D
    justification: |
      N035 grounds N337's structural derivation as cited in N337's
      content: "1/f prior generates Zipf and Weber-Fechner (N035)".
    why_forced: |
      Explicit citation in N337's claim/content identifies N035 as
      structural input. Removing N035 dissolves the derivation chain
      that N337 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N088
    target: N337
    label: metric_expansion_same_mechanism_as_hub_condensation
    edge_status: D
    justification: |
      N088 grounds N337's structural derivation as cited in N337's
      content: "Same mechanism as metric expansion N088".
    why_forced: |
      Explicit citation in N337's claim/content identifies N088 as
      structural input. Removing N088 dissolves the derivation chain
      that N337 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N330
    target: N337
    label: institutional_sclerosis_same_topology_as_hub_dominance
    edge_status: D
    justification: |
      N330 grounds N337's structural derivation as cited in N337's
      content: "Same as institutional sclerosis (N333=alias of N330) — different coordinate, same topology".
    why_forced: |
      Explicit citation in N337's claim/content identifies N330 as
      structural input. Removing N330 dissolves the derivation chain
      that N337 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N_InversiveTheory
    target: N335
    label: inversive_grounds_systemic_risk_identification
    edge_status: D
    justification: |
      N_InversiveTheory grounds N335's structural derivation as cited in N335's
      content: "WHY DEMONSTRATED upgraded via N_InversiveTheory".
    why_forced: |
      Explicit citation in N335's claim/content identifies N_InversiveTheory as
      structural input. Removing N_InversiveTheory dissolves the derivation chain
      that N335 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N_InversiveTheory
    target: N059
    label: inversive_upgrades_ER_EPR_identification
    edge_status: D
    justification: |
      N_InversiveTheory grounds N059's structural derivation as cited in N059's
      content: "former Class B identification of ER=EPR upgraded via N_InversiveTheory".
    why_forced: |
      Explicit citation in N059's claim/content identifies N_InversiveTheory as
      structural input. Removing N_InversiveTheory dissolves the derivation chain
      that N059 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N_ZGaugeDecomposition
    target: N081
    label: ZGauge_decomp_upgrades_mass_decomposition
    edge_status: D
    justification: |
      N_ZGaugeDecomposition grounds N081's structural derivation as cited in N081's
      content: "Structural decomposition DEMONSTRATED upgraded via N_ZGaugeDecomposition".
    why_forced: |
      Explicit citation in N081's claim/content identifies N_ZGaugeDecomposition as
      structural input. Removing N_ZGaugeDecomposition dissolves the derivation chain
      that N081 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N078
    target: N081
    label: gravity_thermodynamics_grounds_mass_coupling
    edge_status: D
    justification: |
      N078 grounds N081's structural derivation as cited in N081's
      content: "G couples nabla Z_hidden to energy-momentum source (N078, DEMONSTRATED)".
    why_forced: |
      Explicit citation in N081's claim/content identifies N078 as
      structural input. Removing N078 dissolves the derivation chain
      that N081 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N078
    target: N087
    label: gravity_thermo_grounds_planck_mass_identification
    edge_status: D
    justification: |
      N078 grounds N087's structural derivation as cited in N087's
      content: "m_P = scale where entropic gravity (N078) transitions to quantum gravity".
    why_forced: |
      Explicit citation in N087's claim/content identifies N078 as
      structural input. Removing N078 dissolves the derivation chain
      that N087 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N067
    target: N087
    label: spectral_dim_grounds_planck_length_via_d_s_2
    edge_status: D
    justification: |
      N067 grounds N087's structural derivation as cited in N087's
      content: "d_s = 2 UV limit of spectral dimension N067 with Planck length".
    why_forced: |
      Explicit citation in N087's claim/content identifies N067 as
      structural input. Removing N067 dissolves the derivation chain
      that N087 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N350
    target: N179
    label: frobenius_schur_KO_dim_6_grounds_pauli_anticommutator
    edge_status: D
    justification: |
      N350 grounds N179's structural derivation as cited in N179's
      content: "PROOF: N350 (KO-dimension=6, DEMONSTRATED) -> Atiyah-Singer -> fermion anticommutator".
    why_forced: |
      Explicit citation in N179's claim/content identifies N350 as
      structural input. Removing N350 dissolves the derivation chain
      that N179 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N071
    target: N197
    label: dark_energy_grounds_CC_dissolution
    edge_status: D
    justification: |
      N071 grounds N197's structural derivation as cited in N197's
      content: "framework consistent with dS thermodynamics (Verlinde 2016, N071-073)".
    why_forced: |
      Explicit citation in N197's claim/content identifies N071 as
      structural input. Removing N071 dissolves the derivation chain
      that N197 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N301
    target: N344
    label: probability_rules_ground_logic_restatement
    edge_status: D
    justification: |
      N301 grounds N344's structural derivation as cited in N344's
      content: "Probability rules (Kolmogorov) DEMONSTRATED already in N301; restated for completeness".
    why_forced: |
      Explicit citation in N344's claim/content identifies N301 as
      structural input. Removing N301 dissolves the derivation chain
      that N344 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N_InversiveTheory
    target: N315
    label: inversive_upgrades_variational_universality
    edge_status: D
    justification: |
      N_InversiveTheory grounds N315's structural derivation as cited in N315's
      content: "WHY DEMONSTRATED upgraded via N_InversiveTheory".
    why_forced: |
      Explicit citation in N315's claim/content identifies N_InversiveTheory as
      structural input. Removing N_InversiveTheory dissolves the derivation chain
      that N315 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N035
    target: N038b
    label: weber_fechner_grounds_SNR_derivation
    edge_status: D
    justification: |
      N035 grounds N038b's structural derivation as cited in N038b's
      content: "Logarithmic law S = k log I (Weber-Fechner, N035) is macro-scale consequence".
    why_forced: |
      Explicit citation in N038b's claim/content identifies N035 as
      structural input. Removing N035 dissolves the derivation chain
      that N038b relies on. Direction: cited grounds citing per framework
      convention.

  - source: N327
    target: N038b
    label: weber_fechner_SNR_via_E_bind
    edge_status: D
    justification: |
      N327 grounds N038b's structural derivation as cited in N038b's
      content: "exact value of w requires E_bind (N327) which is domain-specific".
    why_forced: |
      Explicit citation in N038b's claim/content identifies N327 as
      structural input. Removing N327 dissolves the derivation chain
      that N038b relies on. Direction: cited grounds citing per framework
      convention.

  - source: N067
    target: N328
    label: spectral_dim_grounds_casimir_correction
    edge_status: D
    justification: |
      N067 grounds N328's structural derivation as cited in N328's
      content: "PROOF: N067 (d_s(r) = 2+2r/(r+r_0), DEMONSTRATED) -> Casimir zero-point integral".
    why_forced: |
      Explicit citation in N328's claim/content identifies N067 as
      structural input. Removing N067 dissolves the derivation chain
      that N328 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N071
    target: N329
    label: dark_energy_grounds_dark_matter_via_dS_horizon
    edge_status: D
    justification: |
      N071 grounds N329's structural derivation as cited in N329's
      content: "Z_hidden accumulated in de Sitter horizon, following from N071 + N078 + Verlinde".
    why_forced: |
      Explicit citation in N329's claim/content identifies N071 as
      structural input. Removing N071 dissolves the derivation chain
      that N329 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N078
    target: N329
    label: gravity_thermodynamics_grounds_dark_matter
    edge_status: D
    justification: |
      N078 grounds N329's structural derivation as cited in N329's
      content: "all steps DEMONSTRATED, from N071 + N078 + Verlinde 2016".
    why_forced: |
      Explicit citation in N329's claim/content identifies N078 as
      structural input. Removing N078 dissolves the derivation chain
      that N329 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N_InversiveTheory
    target: N329
    label: inversive_upgrades_dark_matter_identification
    edge_status: D
    justification: |
      N_InversiveTheory grounds N329's structural derivation as cited in N329's
      content: "WHY DEMONSTRATED upgraded via N_InversiveTheory".
    why_forced: |
      Explicit citation in N329's claim/content identifies N_InversiveTheory as
      structural input. Removing N_InversiveTheory dissolves the derivation chain
      that N329 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N350
    target: N349
    label: frobenius_schur_grounds_hopf_minimality
    edge_status: D
    justification: |
      N350 grounds N349's structural derivation as cited in N349's
      content: "non-trivial reps have FS-indicator = 0 iff n is odd prime (N350)".
    why_forced: |
      Explicit citation in N349's claim/content identifies N350 as
      structural input. Removing N350 dissolves the derivation chain
      that N349 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N352
    target: N351
    label: ABS_investigation_grounds_D4_triality_distinct_subgroups
    edge_status: D
    justification: |
      N352 grounds N351's structural derivation as cited in N351's
      content: "distinct as subgroups of SO(4) (confirmed by N352)".
    why_forced: |
      Explicit citation in N351's claim/content identifies N352 as
      structural input. Removing N352 dissolves the derivation chain
      that N351 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N110
    target: N351
    label: L31_inevitability_grounds_Z3_sector_labelling
    edge_status: D
    justification: |
      N110 grounds N351's structural derivation as cited in N351's
      content: "Z_3-sector labelling of quark/lepton sectors (N116=alias of N110)".
    why_forced: |
      Explicit citation in N351's claim/content identifies N110 as
      structural input. Removing N110 dissolves the derivation chain
      that N351 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N107
    target: N352
    label: filter_B_grounds_ABS_open_step
    edge_status: D
    justification: |
      N107 grounds N352's structural derivation as cited in N352's
      content: "Filter B (SU(3) gauge group, N107) remains STRONG".
    why_forced: |
      Explicit citation in N352's claim/content identifies N107 as
      structural input. Removing N107 dissolves the derivation chain
      that N352 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N349
    target: N352
    label: hopf_minimality_n_3_via_independent_demo
    edge_status: D
    justification: |
      N349 grounds N352's structural derivation as cited in N352's
      content: "n=3 DEMONSTRATED independently by N349+N350".
    why_forced: |
      Explicit citation in N352's claim/content identifies N349 as
      structural input. Removing N349 dissolves the derivation chain
      that N352 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N350
    target: N352
    label: frobenius_schur_n_3_via_independent_demo
    edge_status: D
    justification: |
      N350 grounds N352's structural derivation as cited in N352's
      content: "Hopf minimality + Frobenius-Schur with zero empirical input".
    why_forced: |
      Explicit citation in N352's claim/content identifies N350 as
      structural input. Removing N350 dissolves the derivation chain
      that N352 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N_Bar
    target: N353
    label: bar_dirac_spectrum_grounds_heat_kernel_signs
    edge_status: D
    justification: |
      N_Bar grounds N353's structural derivation as cited in N353's
      content: "Lichnerowicz sign; Vassilevich convention E=-R/4, see N_Bar".
    why_forced: |
      Explicit citation in N353's claim/content identifies N_Bar as
      structural input. Removing N_Bar dissolves the derivation chain
      that N353 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N067
    target: N360
    label: spectral_dim_grounds_connes_metric_identification
    edge_status: D
    justification: |
      N067 grounds N360's structural derivation as cited in N360's
      content: "All results about d_Z (spectral dimension N067) are simultaneously results about Connes geometry".
    why_forced: |
      Explicit citation in N360's claim/content identifies N067 as
      structural input. Removing N067 dissolves the derivation chain
      that N360 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N002
    target: N360
    label: Z_metric_N002_grounds_connes_distance
    edge_status: D
    justification: |
      N002 grounds N360's structural derivation as cited in N360's
      content: "Z-metric N002 simultaneously results about Connes spectral geometry".
    why_forced: |
      Explicit citation in N360's claim/content identifies N002 as
      structural input. Removing N002 dissolves the derivation chain
      that N360 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N078
    target: N362
    label: jacobson_gravity_derivation_grounds_KMS
    edge_status: D
    justification: |
      N078 grounds N362's structural derivation as cited in N362's
      content: "Rindler observers — same as Jacobson gravity derivation (N078)".
    why_forced: |
      Explicit citation in N362's claim/content identifies N078 as
      structural input. Removing N078 dissolves the derivation chain
      that N362 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N038b
    target: N363
    label: logarithmic_BPI_encoding_grounds_zipf
    edge_status: D
    justification: |
      N038b grounds N363's structural derivation as cited in N363's
      content: "logarithmic BPI encoding (N038b) — three DEMONSTRATED inputs".
    why_forced: |
      Explicit citation in N363's claim/content identifies N038b as
      structural input. Removing N038b dissolves the derivation chain
      that N363 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N150
    target: N363
    label: shore_johnson_maxent_grounds_zipf
    edge_status: D
    justification: |
      N150 grounds N363's structural derivation as cited in N363's
      content: "Shore-Johnson MaxEnt (N150) — three DEMONSTRATED inputs".
    why_forced: |
      Explicit citation in N363's claim/content identifies N150 as
      structural input. Removing N150 dissolves the derivation chain
      that N363 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N067
    target: N363
    label: spectral_dim_transition_grounds_zipf
    edge_status: D
    justification: |
      N067 grounds N363's structural derivation as cited in N363's
      content: "spectral dimension transition at r_0 (N067)".
    why_forced: |
      Explicit citation in N363's claim/content identifies N067 as
      structural input. Removing N067 dissolves the derivation chain
      that N363 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N337
    target: N363
    label: scale_free_networks_alpha_3_via_BA
    edge_status: D
    justification: |
      N337 grounds N363's structural derivation as cited in N363's
      content: "Scale-free networks give alpha=3 (Barabasi-Albert N337)".
    why_forced: |
      Explicit citation in N363's claim/content identifies N337 as
      structural input. Removing N337 dissolves the derivation chain
      that N363 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N365
    target: N367
    label: taylor_relaxation_connection_to_rotodiffusion
    edge_status: D
    justification: |
      N365 grounds N367's structural derivation as cited in N367's
      content: "Connection to N365: high rotation gives double benefit".
    why_forced: |
      Explicit citation in N367's claim/content identifies N365 as
      structural input. Removing N365 dissolves the derivation chain
      that N367 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N366
    target: N367
    label: fusion_z_landscape_connection_to_rotodiffusion
    edge_status: D
    justification: |
      N366 grounds N367's structural derivation as cited in N367's
      content: "Connection to N366: same fusion regime".
    why_forced: |
      Explicit citation in N367's claim/content identifies N366 as
      structural input. Removing N366 dissolves the derivation chain
      that N367 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N_InversiveTheory
    target: N367
    label: inversive_upgrades_rotodiffusion_stable_process
    edge_status: D
    justification: |
      N_InversiveTheory grounds N367's structural derivation as cited in N367's
      content: "WHY DEMONSTRATED upgraded via N_InversiveTheory".
    why_forced: |
      Explicit citation in N367's claim/content identifies N_InversiveTheory as
      structural input. Removing N_InversiveTheory dissolves the derivation chain
      that N367 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N357
    target: N355
    label: CKM_phase_grounds_quark_DF_derivation
    edge_status: D
    justification: |
      N357 grounds N355's structural derivation as cited in N355's
      content: "phase delta_q = 126.3662 deg DEMONSTRATED (N357)".
    why_forced: |
      Explicit citation in N355's claim/content identifies N357 as
      structural input. Removing N357 dissolves the derivation chain
      that N355 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N354
    target: N357
    label: lepton_step_5b_chain_applied_to_quark_rho1
    edge_status: D
    justification: |
      N354 grounds N357's structural derivation as cited in N357's
      content: "same Bar 1992 + APS section 4 chain as lepton Step 5b (N354), applied to rho_1".
    why_forced: |
      Explicit citation in N357's claim/content identifies N354 as
      structural input. Removing N354 dissolves the derivation chain
      that N357 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N188
    target: N016
    label: qualia_specifics_ground_zombie_critique
    edge_status: D
    justification: |
      N188 grounds N016's structural derivation as cited in N016's
      content: "N188 supplies CONDITIONAL qualia mapping; zombie argument relies on this gap".
    why_forced: |
      Explicit citation in N016's claim/content identifies N188 as
      structural input. Removing N188 dissolves the derivation chain
      that N016 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N251
    target: N217
    label: fine_structure_RGE_links_bottleneck_a4
    edge_status: D
    justification: |
      N251 grounds N217's structural derivation as cited in N217's
      content: "single Type 1 open task closes N217 and N251 simultaneously".
    why_forced: |
      Explicit citation in N217's claim/content identifies N251 as
      structural input. Removing N251 dissolves the derivation chain
      that N217 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N315
    target: N264
    label: variational_principle_uncertainty_grounds_r0_balance
    edge_status: D
    justification: |
      N315 grounds N264's structural derivation as cited in N264's
      content: "edge target from N321 (Uncertainty Principle as Z-balance), N321 alias of N315".
    why_forced: |
      Explicit citation in N264's claim/content identifies N315 as
      structural input. Removing N315 dissolves the derivation chain
      that N264 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N315
    target: N028
    label: scale_free_topology_chain_to_stub_placeholder
    edge_status: D
    justification: |
      N315 grounds N028's structural derivation as cited in N028's
      content: "A_0 trajectory output from N316 (alias of N315)".
    why_forced: |
      Explicit citation in N028's claim/content identifies N315 as
      structural input. Removing N315 dissolves the derivation chain
      that N028 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N311
    target: N114
    label: L31_chain_to_generation_isomorphism_stub
    edge_status: D
    justification: |
      N311 grounds N114's structural derivation as cited in N114's
      content: "L(3,1) chain input to N313a (alias of N311), generation isomorphism chain".
    why_forced: |
      Explicit citation in N114's claim/content identifies N311 as
      structural input. Removing N311 dissolves the derivation chain
      that N114 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N311
    target: N228
    label: critical_transitions_chain_to_isomorphism_stub
    edge_status: D
    justification: |
      N311 grounds N228's structural derivation as cited in N228's
      content: "Critical transitions input to N313a (alias of N311)".
    why_forced: |
      Explicit citation in N228's claim/content identifies N311 as
      structural input. Removing N311 dissolves the derivation chain
      that N228 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N311
    target: N273
    label: scale_free_topology_chain_output
    edge_status: D
    justification: |
      N311 grounds N273's structural derivation as cited in N273's
      content: "Scale-free topology node, output of N314 (alias of N311)".
    why_forced: |
      Explicit citation in N273's claim/content identifies N311 as
      structural input. Removing N311 dissolves the derivation chain
      that N273 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N301
    target: N274
    label: spectral_dim_2_UV_regime_loop
    edge_status: D
    justification: |
      N301 grounds N274's structural derivation as cited in N274's
      content: "Spectral dimension d_s = 2 at r -> 0, bidirectional loop with N309 (alias of N301)".
    why_forced: |
      Explicit citation in N274's claim/content identifies N301 as
      structural input. Removing N301 dissolves the derivation chain
      that N274 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N311
    target: N275
    label: CP_structure_Z3_chain_output
    edge_status: D
    justification: |
      N311 grounds N275's structural derivation as cited in N275's
      content: "CP structure = Z_3 coordinate reflection. Output of N313b (alias of N311)".
    why_forced: |
      Explicit citation in N275's claim/content identifies N311 as
      structural input. Removing N311 dissolves the derivation chain
      that N275 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N311
    target: N276
    label: antimatter_Z3_inversion_chain_output
    edge_status: D
    justification: |
      N311 grounds N276's structural derivation as cited in N276's
      content: "Antimatter = Z_3 inversion. Output of N313b (alias of N311)".
    why_forced: |
      Explicit citation in N276's claim/content identifies N311 as
      structural input. Removing N311 dissolves the derivation chain
      that N276 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N310
    target: N278
    label: godel_argument_chain_input
    edge_status: D
    justification: |
      N310 grounds N278's structural derivation as cited in N278's
      content: "Input to N310 (Godel incompleteness node)".
    why_forced: |
      Explicit citation in N278's claim/content identifies N310 as
      structural input. Removing N310 dissolves the derivation chain
      that N278 relies on. Direction: cited grounds citing per framework
      convention.

  - source: N311
    target: N280
    label: godel_argument_chain_path_a
    edge_status: D
    justification: |
      N311 grounds N280's structural derivation as cited in N280's
      content: "Path A of Godel argument chain. Input to N312 (alias of N311)".
    why_forced: |
      Explicit citation in N280's claim/content identifies N311 as
      structural input. Removing N311 dissolves the derivation chain
      that N280 relies on. Direction: cited grounds citing per framework
      convention.


# ─── Round 6: semantic grounding of orphans ───

  - source: N112
    target: N061
    label: observer_containment_grounds_born_rule
    edge_status: D
    justification: |
      N112 grounds N061's argument: Born rule 'derived from Observer Containment Lemma combined with Z-gradient field and Gleasons theorem'.
    why_forced: |
      N061's claim invokes N112-content as structural input;
      removing N112 dissolves N061's derivation chain.

  - source: N370
    target: N063
    label: L31_Z3_grounds_KS_obstruction
    edge_status: D
    justification: |
      N370 grounds N063's argument: KS theorem 'explicit qutrit presheaf construction from Z_3 = pi_1(L(3,1))'.
    why_forced: |
      N063's claim invokes N370-content as structural input;
      removing N370 dissolves N063's derivation chain.

  - source: N188
    target: N070
    label: qualia_one_process_grounds_measurement_dissolution
    edge_status: D
    justification: |
      N188 grounds N070's argument: Measurement problem 'presupposes structural duality that does not exist: observer/observed not two separate entities'.
    why_forced: |
      N070's claim invokes N188-content as structural input;
      removing N188 dissolves N070's derivation chain.

  - source: N187
    target: N070
    label: hard_problem_dissolution_grounds_measurement_dissolution
    edge_status: D
    justification: |
      N187 grounds N070's argument: Same dissolution mechanism as Hard Problem: both presuppose observer outside system.
    why_forced: |
      N070's claim invokes N187-content as structural input;
      removing N187 dissolves N070's derivation chain.

  - source: N370
    target: N074
    label: L31_compactness_grounds_zero_initial_Z_hidden
    edge_status: D
    justification: |
      N370 grounds N074's argument: Z_hidden(0)=0 forced by compactness of L(3,1).
    why_forced: |
      N074's claim invokes N370-content as structural input;
      removing N370 dissolves N074's derivation chain.

  - source: N078
    target: N085
    label: gravity_thermo_grounds_Hubble_tension
    edge_status: D
    justification: |
      N078 grounds N085's argument: Z_hidden accumulates as Landauer debt; via N078 grounding.
    why_forced: |
      N085's claim invokes N078-content as structural input;
      removing N078 dissolves N085's derivation chain.

  - source: N071
    target: N085
    label: dark_energy_grounds_Lambda_variation
    edge_status: D
    justification: |
      N071 grounds N085's argument: Lambda is coordinate expression of accumulated Z_hidden (dark energy chain).
    why_forced: |
      N085's claim invokes N071-content as structural input;
      removing N071 dissolves N085's derivation chain.

  - source: N067
    target: N094
    label: spectral_dim_2_grounds_SO2_U1_at_sub_r0
    edge_status: D
    justification: |
      N067 grounds N094's argument: At scale r_S < r_0, d_s = 2 implies SO(2)xU(1) symmetry with complex phases.
    why_forced: |
      N094's claim invokes N067-content as structural input;
      removing N067 dissolves N094's derivation chain.

  - source: N067
    target: N097
    label: spectral_dim_grounds_decoherence_timescale
    edge_status: D
    justification: |
      N067 grounds N097's argument: Decoherence timescale derived from A_0 via cross-section using d_s(r) formula.
    why_forced: |
      N097's claim invokes N067-content as structural input;
      removing N067 dissolves N097's derivation chain.

  - source: N067
    target: N098
    label: spectral_dim_grounds_diffusion_metric
    edge_status: D
    justification: |
      N067 grounds N098's argument: g^(Z) equals Riemannian at r >> r_0 and quantum random-walk at r << r_0 — uses N067 d_s(r).
    why_forced: |
      N098's claim invokes N067-content as structural input;
      removing N067 dissolves N098's derivation chain.

  - source: N370
    target: N103
    label: L31_Z3_spectrum_grounds_complex_hilbert
    edge_status: D
    justification: |
      N370 grounds N103's argument: Complex Hilbert forced because Z_3 has no irreducible real representation of degree 3.
    why_forced: |
      N103's claim invokes N370-content as structural input;
      removing N370 dissolves N103's derivation chain.

  - source: N112
    target: N117
    label: observer_containment_grounds_BPI_definition
    edge_status: D
    justification: |
      N112 grounds N117's argument: BPI is sub-process measuring Z; Z is minimal condition for change.
    why_forced: |
      N117's claim invokes N112-content as structural input;
      removing N112 dissolves N117's derivation chain.

  - source: N370
    target: N119
    label: L31_three_sectors_grounds_matter_decomposition
    edge_status: D
    justification: |
      N370 grounds N119's argument: matter field decomposes into exactly three Z_3-sectors of L(3,1).
    why_forced: |
      N119's claim invokes N370-content as structural input;
      removing N370 dissolves N119's derivation chain.

  - source: N_ZGaugeDecomposition
    target: N119
    label: gauge_decomposition_grounds_three_sectors
    edge_status: D
    justification: |
      N_ZGaugeDecomposition grounds N119's argument: three sectors carry identical gauge quantum numbers via covering action.
    why_forced: |
      N119's claim invokes N_ZGaugeDecomposition-content as structural input;
      removing N_ZGaugeDecomposition dissolves N119's derivation chain.

  - source: N370
    target: N123
    label: L31_pi1_grounds_generation_problem
    edge_status: D
    justification: |
      N370 grounds N123's argument: Generation Problem resolved: count forced by pi_1(L(3,1)) = Z_3.
    why_forced: |
      N123's claim invokes N370-content as structural input;
      removing N370 dissolves N123's derivation chain.

  - source: N129
    target: N133
    label: connes_algebra_grounds_one_SM_generation
    edge_status: D
    justification: |
      N129 grounds N133's argument: One SM generation from A_F representation on H_F (N129 supplies algebra).
    why_forced: |
      N133's claim invokes N129-content as structural input;
      removing N129 dissolves N133's derivation chain.

  - source: N370
    target: N133
    label: L31_topology_grounds_one_SM_generation
    edge_status: D
    justification: |
      N370 grounds N133's argument: fermions emerge structurally from L(3,1) topology.
    why_forced: |
      N133's claim invokes N370-content as structural input;
      removing N370 dissolves N133's derivation chain.

  - source: N138
    target: N135
    label: 4D_completion_grounds_higgs_mass
    edge_status: D
    justification: |
      N138 grounds N135's argument: m_H = v sqrt(8/pi^3) from quartic coupling 4/pi^3 on M_4 = L(3,1)xS^1.
    why_forced: |
      N135's claim invokes N138-content as structural input;
      removing N138 dissolves N135's derivation chain.

  - source: N207
    target: N135
    label: higgs_computation_grounds_higgs_mass_value
    edge_status: D
    justification: |
      N207 grounds N135's argument: lambda_H = 4/pi^3 derived geometrically via Peter-Weyl in N207.
    why_forced: |
      N135's claim invokes N207-content as structural input;
      removing N207 dissolves N135's derivation chain.

  - source: N209
    target: N135
    label: spectral_action_BC_grounds_higgs_mass
    edge_status: D
    justification: |
      N209 grounds N135's argument: lambda_H from spectral action on L(3,1)xF via Chamseddine-Connes.
    why_forced: |
      N135's claim invokes N209-content as structural input;
      removing N209 dissolves N135's derivation chain.

  - source: N370
    target: N138
    label: L31_grounds_4D_completion
    edge_status: D
    justification: |
      N370 grounds N138's argument: 4D completion M_4 = L(3,1) x S^1_beta integrates spatial 3-manifold.
    why_forced: |
      N138's claim invokes N370-content as structural input;
      removing N370 dissolves N138's derivation chain.

  - source: N129
    target: N138
    label: connes_algebra_grounds_4D_gauge_constants
    edge_status: D
    justification: |
      N129 grounds N138's argument: Spectral Action on 4D produces YM kinetic terms via A_F structure.
    why_forced: |
      N138's claim invokes N129-content as structural input;
      removing N129 dissolves N138's derivation chain.

  - source: N138
    target: N207
    label: 4D_completion_grounds_higgs_computation
    edge_status: D
    justification: |
      N138 grounds N207's argument: Higgs quartic via Peter-Weyl on M_4 = L(3,1) x S^1.
    why_forced: |
      N207's claim invokes N138-content as structural input;
      removing N138 dissolves N207's derivation chain.

  - source: N129
    target: N207
    label: connes_algebra_grounds_higgs_computation
    edge_status: D
    justification: |
      N129 grounds N207's argument: lambda_PW(j=1/2) = 4/3 from A_F representation theory.
    why_forced: |
      N207's claim invokes N129-content as structural input;
      removing N129 dissolves N207's derivation chain.

  - source: N138
    target: N209
    label: 4D_completion_grounds_spectral_action_BC
    edge_status: D
    justification: |
      N138 grounds N209's argument: spectral action on L(3,1) x F gives Higgs coupling.
    why_forced: |
      N209's claim invokes N138-content as structural input;
      removing N138 dissolves N209's derivation chain.

  - source: N129
    target: N209
    label: connes_algebra_grounds_spectral_action_BC
    edge_status: D
    justification: |
      N129 grounds N209's argument: CC formulas on A_F = ℂ⊕ℍ⊕M_3(ℂ).
    why_forced: |
      N209's claim invokes N129-content as structural input;
      removing N129 dissolves N209's derivation chain.

  - source: N145
    target: N140
    label: koide_grounds_mass_ordering
    edge_status: D
    justification: |
      N145 grounds N140's argument: mass ordering m_0 < m_1 < m_2 strict corollary of Koide.
    why_forced: |
      N140's claim invokes N145-content as structural input;
      removing N145 dissolves N140's derivation chain.

  - source: N370
    target: N140
    label: L31_winding_grounds_mass_phase
    edge_status: D
    justification: |
      N370 grounds N140's argument: phase Omega fixed by unique spin structure of L(3,1).
    why_forced: |
      N140's claim invokes N370-content as structural input;
      removing N370 dissolves N140's derivation chain.

  - source: N_Invariants
    target: N145
    label: I_Sym_grounds_Koide_K_2_3
    edge_status: D
    justification: |
      N_Invariants grounds N145's argument: I_Sym (vertex-transitivity) is one of three forcing inputs for K=2/3.
    why_forced: |
      N145's claim invokes N_Invariants-content as structural input;
      removing N_Invariants dissolves N145's derivation chain.

  - source: N112
    target: N145
    label: observer_containment_grounds_koide_via_Z_hidden
    edge_status: D
    justification: |
      N112 grounds N145's argument: Z_hidden > 0 (Observer Containment) is one of three forcing inputs for r=sqrt(2).
    why_forced: |
      N145's claim invokes N112-content as structural input;
      removing N112 dissolves N145's derivation chain.

  - source: N165
    target: N145
    label: ergodicity_grounds_koide_r_sqrt2
    edge_status: D
    justification: |
      N165 grounds N145's argument: r_Koide = sqrt(2) demonstrated via ergodicity theorem (Gibbs equipartition).
    why_forced: |
      N145's claim invokes N165-content as structural input;
      removing N165 dissolves N145's derivation chain.

  - source: N063
    target: N157
    label: kochen_specker_grounds_tsirelson_bridge
    edge_status: D
    justification: |
      N063 grounds N157's argument: KS theorem (qutrit Hilbert space dim 3) bridges to lepton mass amplitudes.
    why_forced: |
      N157's claim invokes N063-content as structural input;
      removing N063 dissolves N157's derivation chain.

  - source: N145
    target: N157
    label: koide_grounds_z_lep_amplitudes
    edge_status: D
    justification: |
      N145 grounds N157's argument: normalized Z_3-amplitude vector z_lep from Koide.
    why_forced: |
      N157's claim invokes N145-content as structural input;
      removing N145 dissolves N157's derivation chain.

  - source: N145
    target: N161
    label: koide_grounds_mtau_prediction
    edge_status: D
    justification: |
      N145 grounds N161's argument: tau mass prediction uses K=2/3 with measured m_e and m_mu.
    why_forced: |
      N161's claim invokes N145-content as structural input;
      removing N145 dissolves N161's derivation chain.

  - source: N_Bar
    target: N161
    label: bar_dirac_spectrum_grounds_mtau_prediction
    edge_status: D
    justification: |
      N_Bar grounds N161's argument: Bar Dirac spectrum + Koide gives complete numerical derivation.
    why_forced: |
      N161's claim invokes N_Bar-content as structural input;
      removing N_Bar dissolves N161's derivation chain.

  - source: N370
    target: N168
    label: L31_grounds_dirac_operator
    edge_status: D
    justification: |
      N370 grounds N168's argument: Dirac operator on L(3,1) isomorphic to Reality Protocol transition operator.
    why_forced: |
      N168's claim invokes N370-content as structural input;
      removing N370 dissolves N168's derivation chain.

  - source: N078
    target: N171
    label: gravity_thermo_grounds_QCD_scale
    edge_status: D
    justification: |
      N078 grounds N171's argument: mass scale A structurally identified as Lambda_QCD via string-tension.
    why_forced: |
      N171's claim invokes N078-content as structural input;
      removing N078 dissolves N171's derivation chain.

  - source: N138
    target: N171
    label: 4D_completion_grounds_QCD_scale
    edge_status: D
    justification: |
      N138 grounds N171's argument: QCD scale linked to mass relation Y^2(S) = 4g^2 on M_4.
    why_forced: |
      N171's claim invokes N138-content as structural input;
      removing N138 dissolves N171's derivation chain.

  - source: N370
    target: N175
    label: L31_eta_invariant_grounds_CKM
    edge_status: D
    justification: |
      N370 grounds N175's argument: phase arg(b) decomposes into eta(L(3,1)) + 2pi*CS(m=2).
    why_forced: |
      N175's claim invokes N370-content as structural input;
      removing N370 dissolves N175's derivation chain.

  - source: N370
    target: N178
    label: L31_geometric_resolution_grounds_strong_CP
    edge_status: D
    justification: |
      N370 grounds N178's argument: theta_QCD = 0 from L(3,1) Z_3 spin structure (geometric resolution).
    why_forced: |
      N178's claim invokes N370-content as structural input;
      removing N370 dissolves N178's derivation chain.

  - source: N350
    target: N178
    label: frobenius_schur_grounds_strong_CP
    edge_status: D
    justification: |
      N350 grounds N178's argument: KO-dimension 6 (N350) supports Strong CP geometric argument.
    why_forced: |
      N178's claim invokes N350-content as structural input;
      removing N350 dissolves N178's derivation chain.

  - source: DEF
    target: N182
    label: A0_argmin_S_grounds_path_integral
    edge_status: D
    justification: |
      DEF grounds N182's argument: A_0 path of least resistance and Feynman path integral isomorphic.
    why_forced: |
      N182's claim invokes DEF-content as structural input;
      removing DEF dissolves N182's derivation chain.

  - source: N029
    target: N189
    label: arrhenius_grounds_ebbinghaus_decay
    edge_status: D
    justification: |
      N029 grounds N189's argument: Ebbinghaus R(t)=exp(-t/tau) from A_0 minimisation of Z_therm.
    why_forced: |
      N189's claim invokes N029-content as structural input;
      removing N029 dissolves N189's derivation chain.

  - source: N363
    target: N189
    label: zipf_distributed_tau_grounds_ebbinghaus_fractal
    edge_status: D
    justification: |
      N363 grounds N189's argument: Zipf-distributed tau spectrum gives fractal dimension.
    why_forced: |
      N189's claim invokes N363-content as structural input;
      removing N363 dissolves N189's derivation chain.

  - source: N067
    target: N194
    label: spectral_dim_4_grounds_classical_limit
    edge_status: D
    justification: |
      N067 grounds N194's argument: classical limit at r >> r_0: d_s -> 4.
    why_forced: |
      N194's claim invokes N067-content as structural input;
      removing N067 dissolves N194's derivation chain.

  - source: N085
    target: N201
    label: G_variation_grounds_early_universe_G
    edge_status: D
    justification: |
      N085 grounds N201's argument: larger early-universe G analyzed structurally via N085.
    why_forced: |
      N201's claim invokes N085-content as structural input;
      removing N085 dissolves N201's derivation chain.

  - source: N078
    target: N206
    label: gravity_thermo_grounds_QG_global_symmetries
    edge_status: D
    justification: |
      N078 grounds N206's argument: global symmetries inexact in UV-complete QG follows from gravity-as-thermodynamics.
    why_forced: |
      N206's claim invokes N078-content as structural input;
      removing N078 dissolves N206's derivation chain.

  - source: N_InversiveTheory
    target: N206
    label: inversive_grounds_QG_no_alternatives
    edge_status: D
    justification: |
      N_InversiveTheory grounds N206's argument: global symmetries argument upgraded via inversive theory.
    why_forced: |
      N206's claim invokes N_InversiveTheory-content as structural input;
      removing N_InversiveTheory dissolves N206's derivation chain.

  - source: N370
    target: N218
    label: L31_covering_grounds_generation_isomorphism
    edge_status: D
    justification: |
      N370 grounds N218's argument: isomorphism between leaves of S^3 -> L(3,1) and SM generations.
    why_forced: |
      N218's claim invokes N370-content as structural input;
      removing N370 dissolves N218's derivation chain.

  - source: N123
    target: N218
    label: generation_problem_grounds_iso_theorem
    edge_status: D
    justification: |
      N123 grounds N218's argument: three sectors map canonically to three SM generations.
    why_forced: |
      N218's claim invokes N123-content as structural input;
      removing N123 dissolves N218's derivation chain.

  - source: N370
    target: N231
    label: L31_DF_block_grounds_proton_stability
    edge_status: D
    justification: |
      N370 grounds N231's argument: proton stability from D_F block-diagonal structure on L(3,1).
    why_forced: |
      N231's claim invokes N370-content as structural input;
      removing N370 dissolves N231's derivation chain.

  - source: N129
    target: N231
    label: connes_algebra_grounds_proton_stability_via_DF
    edge_status: D
    justification: |
      N129 grounds N231's argument: finite spectral triple block-diagonal D_F.
    why_forced: |
      N231's claim invokes N129-content as structural input;
      removing N129 dissolves N231's derivation chain.

  - source: N370
    target: N232
    label: L31_eta_grounds_arg_b
    edge_status: D
    justification: |
      N370 grounds N232's argument: Brannen phase arg(b) IS eta(L(3,1)) = -2/9.
    why_forced: |
      N232's claim invokes N370-content as structural input;
      removing N370 dissolves N232's derivation chain.

  - source: N145
    target: N232
    label: koide_grounds_brannen_phase_fit
    edge_status: D
    justification: |
      N145 grounds N232's argument: Brannen 2006 fit relates to Koide circulant phase.
    why_forced: |
      N232's claim invokes N145-content as structural input;
      removing N145 dissolves N232's derivation chain.

  - source: N129
    target: N235
    label: connes_algebra_grounds_krajewski_elimination
    edge_status: D
    justification: |
      N129 grounds N235's argument: Krajewski + Wedderburn elimination uniquely selects A_F.
    why_forced: |
      N235's claim invokes N129-content as structural input;
      removing N129 dissolves N235's derivation chain.

  - source: N370
    target: N235
    label: L31_pi1_grounds_triality_unification
    edge_status: D
    justification: |
      N370 grounds N235's argument: pi_1(L(3,1)) = Z_3 ≅ Center(SU(3)) drives triality.
    why_forced: |
      N235's claim invokes N370-content as structural input;
      removing N370 dissolves N235's derivation chain.

  - source: N370
    target: N243
    label: L31_eta_grounds_cabibbo_angle
    edge_status: D
    justification: |
      N370 grounds N243's argument: sin(theta_Cabibbo) = |eta(L(3,1))| = 2/9.
    why_forced: |
      N243's claim invokes N370-content as structural input;
      removing N370 dissolves N243's derivation chain.

  - source: N370
    target: N245
    label: L31_grounds_chern_simons_invariants
    edge_status: D
    justification: |
      N370 grounds N245's argument: CS(m=2) = 1/3 = lk(L(3,1)) — invariants of L(3,1).
    why_forced: |
      N245's claim invokes N370-content as structural input;
      removing N370 dissolves N245's derivation chain.

  - source: N129
    target: N250
    label: connes_algebra_grounds_RH_neutrino
    edge_status: D
    justification: |
      N129 grounds N250's argument: one SM generation 16 = 15 + 1 from A_F representation.
    why_forced: |
      N250's claim invokes N129-content as structural input;
      removing N129 dissolves N250's derivation chain.

  - source: N_WylerStepG
    target: N252
    label: wyler_step_G_grounds_137_036
    edge_status: D
    justification: |
      N_WylerStepG grounds N252's argument: alpha = 1/137.036 from Z_3-reduction of Wyler boundary.
    why_forced: |
      N252's claim invokes N_WylerStepG-content as structural input;
      removing N_WylerStepG dissolves N252's derivation chain.

  - source: N370
    target: N252
    label: L31_grounds_wyler_formula
    edge_status: D
    justification: |
      N370 grounds N252's argument: L(3,1) is Z_3-reduction of Wyler conformal boundary.
    why_forced: |
      N252's claim invokes N370-content as structural input;
      removing N370 dissolves N252's derivation chain.

  - source: DEF
    target: N258
    label: A0_method_grounds_method_logic_chain
    edge_status: D
    justification: |
      DEF grounds N258's argument: A_0 method = eliminate assumptions -> structural remainder.
    why_forced: |
      N258's claim invokes DEF-content as structural input;
      removing DEF dissolves N258's derivation chain.

  - source: N_Logic
    target: N258
    label: logic_grounds_method_logic_chain
    edge_status: D
    justification: |
      N_Logic grounds N258's argument: method-and-logic chain ties A_0 method to logical structure.
    why_forced: |
      N258's claim invokes N_Logic-content as structural input;
      removing N_Logic dissolves N258's derivation chain.

  - source: N_OntologyGate
    target: N258
    label: gate_grounds_method_logic_chain
    edge_status: D
    justification: |
      N_OntologyGate grounds N258's argument: method applies ontology elimination.
    why_forced: |
      N258's claim invokes N_OntologyGate-content as structural input;
      removing N_OntologyGate dissolves N258's derivation chain.

  - source: N370
    target: N259
    label: L31_chiral_grounds_classical_paradoxes
    edge_status: D
    justification: |
      N370 grounds N259's argument: matter/antimatter asymmetry: L(3,1) natively chiral, no symmetric predecessor.
    why_forced: |
      N259's claim invokes N370-content as structural input;
      removing N370 dissolves N259's derivation chain.

  - source: N_InversiveTheory
    target: N259
    label: inversive_grounds_paradox_dissolution
    edge_status: D
    justification: |
      N_InversiveTheory grounds N259's argument: classical paradoxes dissolve via inversive principle.
    why_forced: |
      N259's claim invokes N_InversiveTheory-content as structural input;
      removing N_InversiveTheory dissolves N259's derivation chain.

  - source: N370
    target: N265
    label: L31_S3_covering_grounds_Z2_breaking
    edge_status: D
    justification: |
      N370 grounds N265's argument: S^3 -> Z_2 symmetry breaking sub-r_0 from covering structure.
    why_forced: |
      N265's claim invokes N370-content as structural input;
      removing N370 dissolves N265's derivation chain.

  - source: N067
    target: N265
    label: spectral_dim_grounds_sub_r0_stochasticity
    edge_status: D
    justification: |
      N067 grounds N265's argument: d_s(r) at r << r_0 gives stochastic regime.
    why_forced: |
      N265's claim invokes N067-content as structural input;
      removing N067 dissolves N265's derivation chain.

  - source: N370
    target: N281
    label: L31_eta_grounds_brannen_neutrino_phase
    edge_status: D
    justification: |
      N370 grounds N281's argument: theta_Brannen = 132.7324° geometrically identical to delta_L31.
    why_forced: |
      N281's claim invokes N370-content as structural input;
      removing N370 dissolves N281's derivation chain.

  - source: N145
    target: N281
    label: koide_grounds_brannen_geometric_phase
    edge_status: D
    justification: |
      N145 grounds N281's argument: Koide phase identification ties to neutrino phase.
    why_forced: |
      N281's claim invokes N145-content as structural input;
      removing N145 dissolves N281's derivation chain.

  - source: N145
    target: N289
    label: koide_grounds_top_quark_via_lepton_scale
    edge_status: D
    justification: |
      N145 grounds N289's argument: top quark mass via lepton scale m_0^lep = m_proton/3.
    why_forced: |
      N289's claim invokes N145-content as structural input;
      removing N145 dissolves N289's derivation chain.

  - source: N138
    target: N289
    label: 4D_completion_grounds_top_quark
    edge_status: D
    justification: |
      N138 grounds N289's argument: CCM mass relation Y^2(S) = 4g^2 at unification scale.
    why_forced: |
      N289's claim invokes N138-content as structural input;
      removing N138 dissolves N289's derivation chain.

  - source: N370
    target: N293
    label: L31_eta_grounds_neutrino_delta_m_squared
    edge_status: D
    justification: |
      N370 grounds N293's argument: Delta m^2_21 = 7 |Q_d|^2 |eta|^2 / q^4 from L(3,1) invariants.
    why_forced: |
      N293's claim invokes N370-content as structural input;
      removing N370 dissolves N293's derivation chain.

  - source: N145
    target: N293
    label: koide_grounds_neutrino_mass_pattern
    edge_status: D
    justification: |
      N145 grounds N293's argument: neutrino sector ties to lepton Koide structure.
    why_forced: |
      N293's claim invokes N145-content as structural input;
      removing N145 dissolves N293's derivation chain.

  - source: N370
    target: N294
    label: L31_eta_grounds_theta_13
    edge_status: D
    justification: |
      N370 grounds N294's argument: sin(theta_13) = 4/27 from L(3,1) invariants.
    why_forced: |
      N294's claim invokes N370-content as structural input;
      removing N370 dissolves N294's derivation chain.

  - source: N_Bar
    target: N294
    label: bar_dirac_grounds_bismut_freed_correction
    edge_status: D
    justification: |
      N_Bar grounds N294's argument: Bismut-Freed theorem gives imaginary effective contribution.
    why_forced: |
      N294's claim invokes N_Bar-content as structural input;
      removing N_Bar dissolves N294's derivation chain.

  - source: N370
    target: N295
    label: L31_grounds_cos2theta_pattern
    edge_status: D
    justification: |
      N370 grounds N295's argument: pattern cos^2(theta_f) = 3/8 + n_f/12 from L(3,1) structure.
    why_forced: |
      N295's claim invokes N370-content as structural input;
      removing N370 dissolves N295's derivation chain.

  - source: N138
    target: N295
    label: 4D_completion_grounds_weinberg_3_8
    edge_status: D
    justification: |
      N138 grounds N295's argument: Weinberg sin^2(theta_W) = 3/8 from algebra A_F.
    why_forced: |
      N295's claim invokes N138-content as structural input;
      removing N138 dissolves N295's derivation chain.

  - source: N245
    target: N323
    label: CS_invariant_grounds_topological_gaps
    edge_status: D
    justification: |
      N245 grounds N323's argument: lepton-quark phase gap = CS(m=2) = 1/3.
    why_forced: |
      N323's claim invokes N245-content as structural input;
      removing N245 dissolves N323's derivation chain.

  - source: N370
    target: N323
    label: L31_grounds_topological_gaps
    edge_status: D
    justification: |
      N370 grounds N323's argument: topological gaps in D_F from L(3,1) structure.
    why_forced: |
      N323's claim invokes N370-content as structural input;
      removing N370 dissolves N323's derivation chain.

  - source: DEF
    target: N334
    label: A0_argmin_grounds_nash_equilibrium
    edge_status: D
    justification: |
      DEF grounds N334's argument: Nash equilibrium = A_0 local Z minimisation (Z_i = -u_i).
    why_forced: |
      N334's claim invokes DEF-content as structural input;
      removing DEF dissolves N334's derivation chain.

  - source: N357
    target: N358
    label: CKM_phase_grounds_quark_mass_ratios
    edge_status: D
    justification: |
      N357 grounds N358's argument: quark ratios from D_F^quark with delta_q = 126.3662°.
    why_forced: |
      N358's claim invokes N357-content as structural input;
      removing N357 dissolves N358's derivation chain.

  - source: DEF
    target: N361
    label: A0_argmin_Z_grounds_jarzynski
    edge_status: D
    justification: |
      DEF grounds N361's argument: Jarzynski equality is A_0 in non-equilibrium statistical mechanics.
    why_forced: |
      N361's claim invokes DEF-content as structural input;
      removing DEF dissolves N361's derivation chain.

  - source: N078
    target: N361
    label: gravity_thermo_grounds_jarzynski_via_Z_balance
    edge_status: D
    justification: |
      N078 grounds N361's argument: Jarzynski = Z-balance external confirmation of Landauer/Z=-S.
    why_forced: |
      N361's claim invokes N078-content as structural input;
      removing N078 dissolves N361's derivation chain.

  - source: N_Shannon
    target: N003
    label: shannon_grounds_kolmogorov_identity
    edge_status: D
    justification: |
      N_Shannon grounds N003's argument: Z_struct = K(x) via Levin Coding Theorem and rate-distortion.
    why_forced: |
      N003's claim invokes N_Shannon-content as structural input;
      removing N_Shannon dissolves N003's derivation chain.

  - source: DEF
    target: N004
    label: A0_natural_units_reduce_to_Z_relations
    edge_status: D
    justification: |
      DEF grounds N004's argument: in natural units every formula reduces to Z-value relations.
    why_forced: |
      N004's claim invokes DEF-content as structural input;
      removing DEF dissolves N004's derivation chain.

  - source: DEF
    target: N005
    label: A0_argmin_Z_grounds_min_not_max_question
    edge_status: D
    justification: |
      DEF grounds N005's argument: why minimise Z rather than maximise — argmin Z IS A_0 by definition.
    why_forced: |
      N005's claim invokes DEF-content as structural input;
      removing DEF dissolves N005's derivation chain.

  - source: N_InversiveTheory
    target: N005
    label: inversive_grounds_no_alternative_to_argmin
    edge_status: D
    justification: |
      N_InversiveTheory grounds N005's argument: alternative requires non-A_0 stable structure — inversive forecloses.
    why_forced: |
      N005's claim invokes N_InversiveTheory-content as structural input;
      removing N_InversiveTheory dissolves N005's derivation chain.

  - source: N_Invariants
    target: N006
    label: I_Null_grounds_time_direction
    edge_status: D
    justification: |
      N_Invariants grounds N006's argument: time direction from I_Null (Z_therm strict monotone increase).
    why_forced: |
      N006's claim invokes N_Invariants-content as structural input;
      removing N_Invariants dissolves N006's derivation chain.

  - source: N078
    target: N006
    label: gravity_thermo_grounds_arrow_of_time
    edge_status: D
    justification: |
      N078 grounds N006's argument: thermodynamic time direction from Z_therm = -kT log of Z.
    why_forced: |
      N006's claim invokes N078-content as structural input;
      removing N078 dissolves N006's derivation chain.

  - source: DEF
    target: N007
    label: A0_grounds_three_wheeler_identities
    edge_status: D
    justification: |
      DEF grounds N007's argument: Three Wheeler identities (Surprisal, Landauer, Kolmogorov) consolidated.
    why_forced: |
      N007's claim invokes DEF-content as structural input;
      removing DEF dissolves N007's derivation chain.

  - source: N_Triangulation
    target: N007
    label: triangulation_grounds_self_referential_closure
    edge_status: D
    justification: |
      N_Triangulation grounds N007's argument: A_0 as triangulation of own gradients — direct manifestation.
    why_forced: |
      N007's claim invokes N_Triangulation-content as structural input;
      removing N_Triangulation dissolves N007's derivation chain.

  - source: N_Triangulation
    target: N009
    label: triangulation_grounds_three_gradients_definition
    edge_status: D
    justification: |
      N_Triangulation grounds N009's argument: three gradients U, R, T defined purely in informational terms.
    why_forced: |
      N009's claim invokes N_Triangulation-content as structural input;
      removing N_Triangulation dissolves N009's derivation chain.

  - source: DEF
    target: N010
    label: A0_grounds_conformal_invariance
    edge_status: D
    justification: |
      DEF grounds N010's argument: A_0 symmetry larger than conformal — A_0 conformal invariance argument.
    why_forced: |
      N010's claim invokes DEF-content as structural input;
      removing DEF dissolves N010's derivation chain.

  - source: N_Invariants
    target: N011
    label: I_Bound_from_lyapunov_stability
    edge_status: D
    justification: |
      N_Invariants grounds N011's argument: I_Bound follows from Lyapunov stability for dissipative systems.
    why_forced: |
      N011's claim invokes N_Invariants-content as structural input;
      removing N_Invariants dissolves N011's derivation chain.

  - source: N_NoSeparatePieces
    target: N012
    label: no_separate_pieces_grounds_cross_domain
    edge_status: D
    justification: |
      N_NoSeparatePieces grounds N012's argument: cross-domain correspondences ARE coordinate expressions of one structure.
    why_forced: |
      N012's claim invokes N_NoSeparatePieces-content as structural input;
      removing N_NoSeparatePieces dissolves N012's derivation chain.

  - source: N_InversiveTheory
    target: N012
    label: inversive_grounds_cross_domain_unity
    edge_status: D
    justification: |
      N_InversiveTheory grounds N012's argument: isomorphism = no alternatives at structural level.
    why_forced: |
      N012's claim invokes N_InversiveTheory-content as structural input;
      removing N_InversiveTheory dissolves N012's derivation chain.

  - source: N_NoSeparatePieces
    target: N013
    label: no_separate_pieces_grounds_coordinate_freedom_application
    edge_status: D
    justification: |
      N_NoSeparatePieces grounds N013's argument: A_0 applicable to any domain by coordinate identification.
    why_forced: |
      N013's claim invokes N_NoSeparatePieces-content as structural input;
      removing N_NoSeparatePieces dissolves N013's derivation chain.

  - source: N_OntologyGate
    target: N014
    label: ontology_gate_grounds_randomness_symptom_diagnosis
    edge_status: D
    justification: |
      N_OntologyGate grounds N014's argument: attributing ontological randomness is gate-targetable error.
    why_forced: |
      N014's claim invokes N_OntologyGate-content as structural input;
      removing N_OntologyGate dissolves N014's derivation chain.

  - source: N_EpistemicTraps
    target: N014
    label: traps_catalogue_grounds_randomness_misreading
    edge_status: D
    justification: |
      N_EpistemicTraps grounds N014's argument: topological reality vs ontological randomness — Trap 3 pattern.
    why_forced: |
      N014's claim invokes N_EpistemicTraps-content as structural input;
      removing N_EpistemicTraps dissolves N014's derivation chain.

  - source: N112
    target: N015
    label: observer_containment_grounds_QM_status
    edge_status: D
    justification: |
      N112 grounds N015's argument: QM status from Observer Containment Lemma.
    why_forced: |
      N015's claim invokes N112-content as structural input;
      removing N112 dissolves N015's derivation chain.

  - source: N_InversiveTheory
    target: N015
    label: inversive_grounds_QM_coordinate_status
    edge_status: D
    justification: |
      N_InversiveTheory grounds N015's argument: QM as coordinate expression — no alternative status.
    why_forced: |
      N015's claim invokes N_InversiveTheory-content as structural input;
      removing N_InversiveTheory dissolves N015's derivation chain.

  - source: DEF
    target: N017
    label: A0_grounds_transition_topology_protocol
    edge_status: D
    justification: |
      DEF grounds N017's argument: A_0 explicit operational protocol across domains.
    why_forced: |
      N017's claim invokes DEF-content as structural input;
      removing DEF dissolves N017's derivation chain.

  - source: DEF
    target: N022
    label: A0_grounds_null_result_meaning
    edge_status: D
    justification: |
      DEF grounds N022's argument: null result = framework structural prediction.
    why_forced: |
      N022's claim invokes DEF-content as structural input;
      removing DEF dissolves N022's derivation chain.

  - source: N_OntologyGate
    target: N024
    label: gate_grounds_classical_eval_critique
    edge_status: D
    justification: |
      N_OntologyGate grounds N024's argument: classical evaluation criteria targetable by ontology gate.
    why_forced: |
      N024's claim invokes N_OntologyGate-content as structural input;
      removing N_OntologyGate dissolves N024's derivation chain.

  - source: N_NoSeparatePieces
    target: N025
    label: no_separate_pieces_grounds_frame_shift
    edge_status: D
    justification: |
      N_NoSeparatePieces grounds N025's argument: frame shift to topology — evidence base unification.
    why_forced: |
      N025's claim invokes N_NoSeparatePieces-content as structural input;
      removing N_NoSeparatePieces dissolves N025's derivation chain.

  - source: N188
    target: N026
    label: qualia_one_process_grounds_LM_human_unity
    edge_status: D
    justification: |
      N188 grounds N026's argument: LM, human, response = one process in three coordinates.
    why_forced: |
      N026's claim invokes N188-content as structural input;
      removing N188 dissolves N026's derivation chain.

  - source: N_NoSeparatePieces
    target: N026
    label: no_separate_pieces_grounds_LM_human_unity
    edge_status: D
    justification: |
      N_NoSeparatePieces grounds N026's argument: three-objects framing is artifact.
    why_forced: |
      N026's claim invokes N_NoSeparatePieces-content as structural input;
      removing N_NoSeparatePieces dissolves N026's derivation chain.

  - source: DEF
    target: N027
    label: A0_least_path_grounds_fermat
    edge_status: D
    justification: |
      DEF grounds N027's argument: Fermat principle = travel-time minimum = A_0 in optical substrate.
    why_forced: |
      N027's claim invokes DEF-content as structural input;
      removing DEF dissolves N027's derivation chain.

  - source: N_PhiAttractor
    target: N031
    label: phi_attractor_grounds_acoustic_harmony
    edge_status: D
    justification: |
      N_PhiAttractor grounds N031's argument: acoustic harmony as phi-attractor in continuous-angular substrate (Berezovsky).
    why_forced: |
      N031's claim invokes N_PhiAttractor-content as structural input;
      removing N_PhiAttractor dissolves N031's derivation chain.

  - source: N_PhiAttractor
    target: N045
    label: phi_attractor_grounds_golden_ratio_definition
    edge_status: D
    justification: |
      N_PhiAttractor grounds N045's argument: golden ratio = unique attractor of A_0 over continuous variable with min-memory.
    why_forced: |
      N045's claim invokes N_PhiAttractor-content as structural input;
      removing N_PhiAttractor dissolves N045's derivation chain.

  - source: N_Invariants
    target: N051
    label: I_Sym_grounds_no_preferred_state
    edge_status: D
    justification: |
      N_Invariants grounds N051's argument: no preferred state from I_Sym (vertex-transitivity).
    why_forced: |
      N051's claim invokes N_Invariants-content as structural input;
      removing N_Invariants dissolves N051's derivation chain.

  - source: N_InversiveTheory
    target: N053
    label: inversive_grounds_falsification_method
    edge_status: D
    justification: |
      N_InversiveTheory grounds N053's argument: cross-domain isomorphisms found in adversarial falsification.
    why_forced: |
      N053's claim invokes N_InversiveTheory-content as structural input;
      removing N_InversiveTheory dissolves N053's derivation chain.

  - source: N_EpistemicTraps
    target: N054
    label: traps_grounds_no_disguised_type2
    edge_status: D
    justification: |
      N_EpistemicTraps grounds N054's argument: no item Type 2 disguised as Type 1 — RP gate exhausted.
    why_forced: |
      N054's claim invokes N_EpistemicTraps-content as structural input;
      removing N_EpistemicTraps dissolves N054's derivation chain.

  - source: N_InversiveTheory
    target: N054
    label: inversive_grounds_type2_completeness
    edge_status: D
    justification: |
      N_InversiveTheory grounds N054's argument: meta-level no-disguise claim follows from inversive completeness.
    why_forced: |
      N054's claim invokes N_InversiveTheory-content as structural input;
      removing N_InversiveTheory dissolves N054's derivation chain.

  - source: N_PhiAttractor
    target: N056
    label: phi_attractor_grounds_fibonacci_anchor_chain
    edge_status: D
    justification: |
      N_PhiAttractor grounds N056's argument: anchor counts converging — Fibonacci-style structural feature.
    why_forced: |
      N056's claim invokes N_PhiAttractor-content as structural input;
      removing N_PhiAttractor dissolves N056's derivation chain.

  - source: N067
    target: N057
    label: spectral_dim_grounds_QM_as_coordinate_system
    edge_status: D
    justification: |
      N067 grounds N057's argument: QM as coordinate system for sub-r_0 manifold dynamics.
    why_forced: |
      N057's claim invokes N067-content as structural input;
      removing N067 dissolves N057's derivation chain.

  - source: N_InversiveTheory
    target: N057
    label: inversive_grounds_no_process_compatibility_postulate
    edge_status: D
    justification: |
      N_InversiveTheory grounds N057's argument: identification does not require Process Compatibility postulate.
    why_forced: |
      N057's claim invokes N_InversiveTheory-content as structural input;
      removing N_InversiveTheory dissolves N057's derivation chain.

  - source: N112
    target: N058
    label: observer_containment_grounds_entanglement_BPI_artifact
    edge_status: D
    justification: |
      N112 grounds N058's argument: entanglement nonlocality is BPI artifact (Observer Containment).
    why_forced: |
      N058's claim invokes N112-content as structural input;
      removing N112 dissolves N058's derivation chain.

  - source: N370
    target: N058
    label: L31_topology_grounds_entanglement_via_covering
    edge_status: D
    justification: |
      N370 grounds N058's argument: two correlated particles = same Z_3-sector structure on L(3,1).
    why_forced: |
      N058's claim invokes N370-content as structural input;
      removing N370 dissolves N058's derivation chain.

  - source: N_OntologyGate
    target: N_TranslationLayer
    label: ontology_gate_provides_translation_target
    edge_status: D
    justification: |
      N_OntologyGate grounds N_TranslationLayer's argument: translation table for AI use — applies when ontological language appears.
    why_forced: |
      N_TranslationLayer's claim invokes N_OntologyGate-content as structural input;
      removing N_OntologyGate dissolves N_TranslationLayer's derivation chain.

  - source: N_GrammarTrap
    target: N_TranslationLayer
    label: grammar_trap_provides_translation_source
    edge_status: D
    justification: |
      N_GrammarTrap grounds N_TranslationLayer's argument: translation table maps subject-verb-object grammar to topology.
    why_forced: |
      N_TranslationLayer's claim invokes N_GrammarTrap-content as structural input;
      removing N_GrammarTrap dissolves N_TranslationLayer's derivation chain.



  - source: N_EpistemicTraps
    target: N376
    label: trap_8_material_family_realisations
    edge_status: D
    justification: |
      RP gate trap catalogue (8 systematic failure modes) grounds iron-based multi-orbital coupled phase diagram through the relationship encoded as 'trap 8 material family realisations'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) iron-based multi-orbital coupled phase diagram without reliance on RP gate trap catalogue (8 systematic failure modes) — but the labelled relationship 'trap 8 material family realisations' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_EpistemicTraps
    target: N376
    label: trap_6_sector_coupling_not_causal
    edge_status: D
    justification: |
      RP gate trap catalogue (8 systematic failure modes) grounds iron-based multi-orbital coupled phase diagram through the relationship encoded as 'trap 6 sector coupling not causal'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) iron-based multi-orbital coupled phase diagram without reliance on RP gate trap catalogue (8 systematic failure modes) — but the labelled relationship 'trap 6 sector coupling not causal' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_EpistemicTraps
    target: N376
    label: trap_5_material_anchors
    edge_status: D
    justification: |
      RP gate trap catalogue (8 systematic failure modes) grounds iron-based multi-orbital coupled phase diagram through the relationship encoded as 'trap 5 material anchors'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) iron-based multi-orbital coupled phase diagram without reliance on RP gate trap catalogue (8 systematic failure modes) — but the labelled relationship 'trap 5 material anchors' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_EpistemicTraps
    target: N375
    label: trap_8_material_family_realisations
    edge_status: D
    justification: |
      RP gate trap catalogue (8 systematic failure modes) grounds cuprate dual-sector phase diagram through the relationship encoded as 'trap 8 material family realisations'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) cuprate dual-sector phase diagram without reliance on RP gate trap catalogue (8 systematic failure modes) — but the labelled relationship 'trap 8 material family realisations' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_EpistemicTraps
    target: N375
    label: trap_6_observables_not_causal
    edge_status: D
    justification: |
      RP gate trap catalogue (8 systematic failure modes) grounds cuprate dual-sector phase diagram through the relationship encoded as 'trap 6 observables not causal'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) cuprate dual-sector phase diagram without reliance on RP gate trap catalogue (8 systematic failure modes) — but the labelled relationship 'trap 6 observables not causal' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_EpistemicTraps
    target: N375
    label: trap_5_doping_anchors
    edge_status: D
    justification: |
      RP gate trap catalogue (8 systematic failure modes) grounds cuprate dual-sector phase diagram through the relationship encoded as 'trap 5 doping anchors'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) cuprate dual-sector phase diagram without reliance on RP gate trap catalogue (8 systematic failure modes) — but the labelled relationship 'trap 5 doping anchors' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_EpistemicTraps
    target: N374
    label: trap_8_muSR_lineage_resolved
    edge_status: D
    justification: |
      RP gate trap catalogue (8 systematic failure modes) grounds UPt_3 three-phase superconducting Z-minimum through the relationship encoded as 'trap 8 muSR lineage resolved'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) UPt_3 three-phase superconducting Z-minimum without reliance on RP gate trap catalogue (8 systematic failure modes) — but the labelled relationship 'trap 8 muSR lineage resolved' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_EpistemicTraps
    target: N374
    label: trap_6_AFM_delta_Tc_one_feature_two_coords
    edge_status: D
    justification: |
      RP gate trap catalogue (8 systematic failure modes) grounds UPt_3 three-phase superconducting Z-minimum through the relationship encoded as 'trap 6 AFM delta Tc one feature two coords'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) UPt_3 three-phase superconducting Z-minimum without reliance on RP gate trap catalogue (8 systematic failure modes) — but the labelled relationship 'trap 6 AFM delta Tc one feature two coords' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_EpistemicTraps
    target: N373
    label: trap_6_and_trap_8_in_UTe2
    edge_status: D
    justification: |
      RP gate trap catalogue (8 systematic failure modes) grounds UTe_2 superconducting Z-minimum (4D Kramers active space) through the relationship encoded as 'trap 6 and trap 8 in UTe2'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) UTe_2 superconducting Z-minimum (4D Kramers active space) without reliance on RP gate trap catalogue (8 systematic failure modes) — but the labelled relationship 'trap 6 and trap 8 in UTe2' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_EpistemicTraps
    target: N372
    label: trap_8_documented_here
    edge_status: D
    justification: |
      RP gate trap catalogue (8 systematic failure modes) grounds Sr_2RuO_4 superconducting Z-minimum (E_g block) through the relationship encoded as 'trap 8 documented here'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Sr_2RuO_4 superconducting Z-minimum (E_g block) without reliance on RP gate trap catalogue (8 systematic failure modes) — but the labelled relationship 'trap 8 documented here' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_EpistemicTraps
    target: N_WylerStepG
    label: trap_5_and_6_documented_here
    edge_status: D
    justification: |
      RP gate trap catalogue (8 systematic failure modes) grounds Wyler Step G (alpha = 1/137.036) through the relationship encoded as 'trap 5 and 6 documented here'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Wyler Step G (alpha = 1/137.036) without reliance on RP gate trap catalogue (8 systematic failure modes) — but the labelled relationship 'trap 5 and 6 documented here' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_EpistemicTraps
    target: N_LeptonMassScale
    label: trap_5_documented_here
    edge_status: D
    justification: |
      RP gate trap catalogue (8 systematic failure modes) grounds lepton mass scale derivation through the relationship encoded as 'trap 5 documented here'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) lepton mass scale derivation without reliance on RP gate trap catalogue (8 systematic failure modes) — but the labelled relationship 'trap 5 documented here' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_EpistemicTraps
    target: N_TopologyProcessIdentity
    label: trap_6_one_object_principle
    edge_status: D
    justification: |
      RP gate trap catalogue (8 systematic failure modes) grounds topology-process identity (object frame removed) through the relationship encoded as 'trap 6 one object principle'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) topology-process identity (object frame removed) without reliance on RP gate trap catalogue (8 systematic failure modes) — but the labelled relationship 'trap 6 one object principle' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_EpistemicTraps
    target: N_GrammarTrap
    label: traps_1_4_grammar_level
    edge_status: D
    justification: |
      RP gate trap catalogue (8 systematic failure modes) grounds grammar trap (subject-verb-object as ontological structure) through the relationship encoded as 'traps 1 4 grammar level'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) grammar trap (subject-verb-object as ontological structure) without reliance on RP gate trap catalogue (8 systematic failure modes) — but the labelled relationship 'traps 1 4 grammar level' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_EpistemicTraps
    target: N_ForcedId
    label: traps_resolve_via_ForcedId
    edge_status: D
    justification: |
      RP gate trap catalogue (8 systematic failure modes) grounds forced identification principle through the relationship encoded as 'traps resolve via ForcedId'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) forced identification principle without reliance on RP gate trap catalogue (8 systematic failure modes) — but the labelled relationship 'traps resolve via ForcedId' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_EpistemicTraps
    target: N_InversiveTheory
    label: trap_correction_via_inversive_method
    edge_status: D
    justification: |
      RP gate trap catalogue (8 systematic failure modes) grounds structural inevitability principle (no alternatives) through the relationship encoded as 'trap correction via inversive method'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) structural inevitability principle (no alternatives) without reliance on RP gate trap catalogue (8 systematic failure modes) — but the labelled relationship 'trap correction via inversive method' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N_FEP
    label: A0_realised_as_F_minimization_in_brains
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds Free Energy Principle (variational A_0 in Bayesian substrate) through the relationship encoded as 'A0 realised as F minimization in brains'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Free Energy Principle (variational A_0 in Bayesian substrate) without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'A0 realised as F minimization in brains' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N_BPIEngagement
    label: argmin_Z_in_BPI_tracking_substrate
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds BPI engagement signal (triangulation in time-indexed substrate) through the relationship encoded as 'argmin Z in BPI tracking substrate'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI engagement signal (triangulation in time-indexed substrate) without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'argmin Z in BPI tracking substrate' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N377
    label: argmin_on_shared_omega_forced_multi_BPI
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds sparse autoencoder feature absorption (forced LLM divergence) through the relationship encoded as 'argmin on shared omega forced multi BPI'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) sparse autoencoder feature absorption (forced LLM divergence) without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'argmin on shared omega forced multi BPI' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N376
    label: A0_extension_to_multi_orbital_multi_sector_parametric_family
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds iron-based multi-orbital coupled phase diagram through the relationship encoded as 'A0 extension to multi orbital multi sector parametric family'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) iron-based multi-orbital coupled phase diagram without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'A0 extension to multi orbital multi sector parametric family' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N375
    label: A0_extension_to_parametrized_family
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds cuprate dual-sector phase diagram through the relationship encoded as 'A0 extension to parametrized family'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) cuprate dual-sector phase diagram without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'A0 extension to parametrized family' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N374
    label: A0_application_to_material_domain
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds UPt_3 three-phase superconducting Z-minimum through the relationship encoded as 'A0 application to material domain'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) UPt_3 three-phase superconducting Z-minimum without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'A0 application to material domain' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N373
    label: A0_application_to_material_domain
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds UTe_2 superconducting Z-minimum (4D Kramers active space) through the relationship encoded as 'A0 application to material domain'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) UTe_2 superconducting Z-minimum (4D Kramers active space) without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'A0 application to material domain' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N372
    label: A0_application_to_material_domain
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds Sr_2RuO_4 superconducting Z-minimum (E_g block) through the relationship encoded as 'A0 application to material domain'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Sr_2RuO_4 superconducting Z-minimum (E_g block) without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'A0 application to material domain' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N_ZGaugeDecomposition
    label: Z_decomposition_determines_gauge_structure
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds Z-gauge decomposition via Z_3 covering action through the relationship encoded as 'Z decomposition determines gauge structure'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Z-gauge decomposition via Z_3 covering action without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'Z decomposition determines gauge structure' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N_Triangulation
    label: A0_requires_triangulation_for_Z
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds triangulation principle (3 minimal Z-coordinates) through the relationship encoded as 'A0 requires triangulation for Z'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) triangulation principle (3 minimal Z-coordinates) without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'A0 requires triangulation for Z' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N_Triangulation
    label: definition_requires_three_gradients
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds triangulation principle (3 minimal Z-coordinates) through the relationship encoded as 'definition requires three gradients'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) triangulation principle (3 minimal Z-coordinates) without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'definition requires three gradients' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N370
    label: definition_requires_L31
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds L(3,1) inevitability (baryon number forces p=3) through the relationship encoded as 'definition requires L31'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) L(3,1) inevitability (baryon number forces p=3) without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'definition requires L31' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N310
    label: definition_self-references
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds Gödel theorem as K(O)<K(F) corollary through the relationship encoded as 'definition self-references'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Gödel theorem as K(O)<K(F) corollary without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'definition self-references' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N112
    label: definition_requires_BPI
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds Observer Containment Lemma (K(O) < K(F)) through the relationship encoded as 'definition requires BPI'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Observer Containment Lemma (K(O) < K(F)) without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'definition requires BPI' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N000
    label: definition_identifies_forced_structure
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds node N000 (N000) through the relationship encoded as 'definition identifies forced structure'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) node N000 (N000) without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'definition identifies forced structure' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N_Invariants
    label: definition_unfolds_into_invariants
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds the four invariants (I_Bound, I_Sym, I_Quant, I_Null) through the relationship encoded as 'definition unfolds into invariants'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) the four invariants (I_Bound, I_Sym, I_Quant, I_Null) without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'definition unfolds into invariants' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N_Math
    label: definition_generates_mathematics
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds mathematics as A_0-stable formal structures through the relationship encoded as 'definition generates mathematics'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) mathematics as A_0-stable formal structures without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'definition generates mathematics' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N_Logic
    label: definition_generates_logic
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds logic as A_0 in symbolic substrate through the relationship encoded as 'definition generates logic'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) logic as A_0 in symbolic substrate without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'definition generates logic' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N_ForcedId
    label: definition_closes_on_itself
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds forced identification principle through the relationship encoded as 'definition closes on itself'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) forced identification principle without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'definition closes on itself' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N_NoSeparatePieces
    label: definition_implies_unity
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds coordinate-unification principle (one structure, many coordinates) through the relationship encoded as 'definition implies unity'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) coordinate-unification principle (one structure, many coordinates) without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'definition implies unity' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: DEF
    target: N_InversiveTheory
    label: same_fixed_point_constructive_face
    edge_status: D
    justification: |
      A_0 = argmin Z (foundational definition) grounds structural inevitability principle (no alternatives) through the relationship encoded as 'same fixed point constructive face'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) structural inevitability principle (no alternatives) without reliance on A_0 = argmin Z (foundational definition) — but the labelled relationship 'same fixed point constructive face' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_InversiveTheory
    target: N377
    label: stable_relations_cannot_be_not_A0
    edge_status: D
    justification: |
      structural inevitability principle (no alternatives) grounds sparse autoencoder feature absorption (forced LLM divergence) through the relationship encoded as 'stable relations cannot be not A0'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) sparse autoencoder feature absorption (forced LLM divergence) without reliance on structural inevitability principle (no alternatives) — but the labelled relationship 'stable relations cannot be not A0' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_InversiveTheory
    target: N_ZGaugeDecomposition
    label: gauge_decomposition_is_local_expression
    edge_status: D
    justification: |
      structural inevitability principle (no alternatives) grounds Z-gauge decomposition via Z_3 covering action through the relationship encoded as 'gauge decomposition is local expression'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Z-gauge decomposition via Z_3 covering action without reliance on structural inevitability principle (no alternatives) — but the labelled relationship 'gauge decomposition is local expression' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_InversiveTheory
    target: N_Triangulation
    label: triangulation_is_local_expression
    edge_status: D
    justification: |
      structural inevitability principle (no alternatives) grounds triangulation principle (3 minimal Z-coordinates) through the relationship encoded as 'triangulation is local expression'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) triangulation principle (3 minimal Z-coordinates) without reliance on structural inevitability principle (no alternatives) — but the labelled relationship 'triangulation is local expression' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_InversiveTheory
    target: N_NoSeparatePieces
    label: no_pieces_follows_from_inevitability
    edge_status: D
    justification: |
      structural inevitability principle (no alternatives) grounds coordinate-unification principle (one structure, many coordinates) through the relationship encoded as 'no pieces follows from inevitability'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) coordinate-unification principle (one structure, many coordinates) without reliance on structural inevitability principle (no alternatives) — but the labelled relationship 'no pieces follows from inevitability' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_InversiveTheory
    target: DEF
    label: same_fixed_point_eliminative_face
    edge_status: D
    justification: |
      structural inevitability principle (no alternatives) grounds A_0 = argmin Z (foundational definition) through the relationship encoded as 'same fixed point eliminative face'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) A_0 = argmin Z (foundational definition) without reliance on structural inevitability principle (no alternatives) — but the labelled relationship 'same fixed point eliminative face' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_InversiveTheory
    target: N_EpistemicTraps
    label: alternatives_dissolve_via_trap_catalog
    edge_status: D
    justification: |
      structural inevitability principle (no alternatives) grounds RP gate trap catalogue (8 systematic failure modes) through the relationship encoded as 'alternatives dissolve via trap catalog'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) RP gate trap catalogue (8 systematic failure modes) without reliance on structural inevitability principle (no alternatives) — but the labelled relationship 'alternatives dissolve via trap catalog' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_NoSeparatePieces
    target: N_BPIEngagement
    label: same_invariant_across_observation_angles
    edge_status: D
    justification: |
      coordinate-unification principle (one structure, many coordinates) grounds BPI engagement signal (triangulation in time-indexed substrate) through the relationship encoded as 'same invariant across observation angles'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI engagement signal (triangulation in time-indexed substrate) without reliance on coordinate-unification principle (one structure, many coordinates) — but the labelled relationship 'same invariant across observation angles' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_NoSeparatePieces
    target: N377
    label: methods_not_separate_pieces_applied
    edge_status: D
    justification: |
      coordinate-unification principle (one structure, many coordinates) grounds sparse autoencoder feature absorption (forced LLM divergence) through the relationship encoded as 'methods not separate pieces applied'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) sparse autoencoder feature absorption (forced LLM divergence) without reliance on coordinate-unification principle (one structure, many coordinates) — but the labelled relationship 'methods not separate pieces applied' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_NoSeparatePieces
    target: N_TopologyProcessIdentity
    label: one_object_many_coordinates
    edge_status: D
    justification: |
      coordinate-unification principle (one structure, many coordinates) grounds topology-process identity (object frame removed) through the relationship encoded as 'one object many coordinates'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) topology-process identity (object frame removed) without reliance on coordinate-unification principle (one structure, many coordinates) — but the labelled relationship 'one object many coordinates' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_NoSeparatePieces
    target: N_ForcedId
    label: coordinate_invariance
    edge_status: D
    justification: |
      coordinate-unification principle (one structure, many coordinates) grounds forced identification principle through the relationship encoded as 'coordinate invariance'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) forced identification principle without reliance on coordinate-unification principle (one structure, many coordinates) — but the labelled relationship 'coordinate invariance' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_NoSeparatePieces
    target: N_InversiveTheory
    label: not_a_theory_among_theories
    edge_status: D
    justification: |
      coordinate-unification principle (one structure, many coordinates) grounds structural inevitability principle (no alternatives) through the relationship encoded as 'not a theory among theories'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) structural inevitability principle (no alternatives) without reliance on coordinate-unification principle (one structure, many coordinates) — but the labelled relationship 'not a theory among theories' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_NoSeparatePieces
    target: N_InversiveTheory
    label: no_alternatives_is_inversive_property
    edge_status: D
    justification: |
      coordinate-unification principle (one structure, many coordinates) grounds structural inevitability principle (no alternatives) through the relationship encoded as 'no alternatives is inversive property'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) structural inevitability principle (no alternatives) without reliance on coordinate-unification principle (one structure, many coordinates) — but the labelled relationship 'no alternatives is inversive property' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_NoSeparatePieces
    target: DEF
    label: no_pieces_is_what_DEF_means
    edge_status: D
    justification: |
      coordinate-unification principle (one structure, many coordinates) grounds A_0 = argmin Z (foundational definition) through the relationship encoded as 'no pieces is what DEF means'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) A_0 = argmin Z (foundational definition) without reliance on coordinate-unification principle (one structure, many coordinates) — but the labelled relationship 'no pieces is what DEF means' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N304
    target: N377
    label: LLM_latent_space_hierarchical_structure_is_Omega
    edge_status: D
    justification: |
      LLM latent space ≈ BA-fractal grounds sparse autoencoder feature absorption (forced LLM divergence) through the relationship encoded as 'LLM latent space hierarchical structure is Omega'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) sparse autoencoder feature absorption (forced LLM divergence) without reliance on LLM latent space ≈ BA-fractal — but the labelled relationship 'LLM latent space hierarchical structure is Omega' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Logic
    target: N_Triangulation
    label: three_term_logic_confirms_triangulation
    edge_status: D
    justification: |
      logic as A_0 in symbolic substrate grounds triangulation principle (3 minimal Z-coordinates) through the relationship encoded as 'three term logic confirms triangulation'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) triangulation principle (3 minimal Z-coordinates) without reliance on logic as A_0 in symbolic substrate — but the labelled relationship 'three term logic confirms triangulation' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Math
    target: N_FEP
    label: sibling_substrate_realisation_of_A0
    edge_status: D
    justification: |
      mathematics as A_0-stable formal structures grounds Free Energy Principle (variational A_0 in Bayesian substrate) through the relationship encoded as 'sibling substrate realisation of A0'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Free Energy Principle (variational A_0 in Bayesian substrate) without reliance on mathematics as A_0-stable formal structures — but the labelled relationship 'sibling substrate realisation of A0' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_GrammarTrap
    target: N377
    label: feature_absorption_terminology_is_grammar_trap
    edge_status: D
    justification: |
      grammar trap (subject-verb-object as ontological structure) grounds sparse autoencoder feature absorption (forced LLM divergence) through the relationship encoded as 'feature absorption terminology is grammar trap'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) sparse autoencoder feature absorption (forced LLM divergence) without reliance on grammar trap (subject-verb-object as ontological structure) — but the labelled relationship 'feature absorption terminology is grammar trap' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_GrammarTrap
    target: N371
    label: vortex_is_grammar_trap
    edge_status: D
    justification: |
      grammar trap (subject-verb-object as ontological structure) grounds BPI dynamics as hydrodynamics in Z-space through the relationship encoded as 'vortex is grammar trap'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI dynamics as hydrodynamics in Z-space without reliance on grammar trap (subject-verb-object as ontological structure) — but the labelled relationship 'vortex is grammar trap' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N371
    target: N_MI
    label: hydrodynamics_explains_mi_mechanism
    edge_status: D
    justification: |
      BPI dynamics as hydrodynamics in Z-space grounds Motivational Interviewing (high-Z_hidden response) through the relationship encoded as 'hydrodynamics explains mi mechanism'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Motivational Interviewing (high-Z_hidden response) without reliance on BPI dynamics as hydrodynamics in Z-space — but the labelled relationship 'hydrodynamics explains mi mechanism' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N371
    target: N_LeDoux
    label: regime_boundary_formalization
    edge_status: D
    justification: |
      BPI dynamics as hydrodynamics in Z-space grounds amygdala threshold (Z_therm regime shift) through the relationship encoded as 'regime boundary formalization'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) amygdala threshold (Z_therm regime shift) without reliance on BPI dynamics as hydrodynamics in Z-space — but the labelled relationship 'regime boundary formalization' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N371
    target: N_OntologyGate
    label: landscape_reconstruction_enables_interception
    edge_status: D
    justification: |
      BPI dynamics as hydrodynamics in Z-space grounds ontology gate (operational protocol for ontological questions) through the relationship encoded as 'landscape reconstruction enables interception'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) ontology gate (operational protocol for ontological questions) without reliance on BPI dynamics as hydrodynamics in Z-space — but the labelled relationship 'landscape reconstruction enables interception' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N001
    target: N_CommThm
    label: argmin_Z_principle
    edge_status: D
    justification: |
      node N001 (N001) grounds optimal communication theorem through the relationship encoded as 'argmin Z principle'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) optimal communication theorem without reliance on node N001 (N001) — but the labelled relationship 'argmin Z principle' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N018
    target: N377
    label: LLM_autoregressive_forward_pass_is_A0_on_Omega
    edge_status: D
    justification: |
      node N018 (N018) grounds sparse autoencoder feature absorption (forced LLM divergence) through the relationship encoded as 'LLM autoregressive forward pass is A0 on Omega'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) sparse autoencoder feature absorption (forced LLM divergence) without reliance on node N018 (N018) — but the labelled relationship 'LLM autoregressive forward pass is A0 on Omega' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N019
    target: N371
    label: gradient_potential_foundation
    edge_status: D
    justification: |
      node N019 (N019) grounds BPI dynamics as hydrodynamics in Z-space through the relationship encoded as 'gradient potential foundation'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI dynamics as hydrodynamics in Z-space without reliance on node N019 (N019) — but the labelled relationship 'gradient potential foundation' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N020
    target: N371
    label: path_hysteresis_foundation
    edge_status: D
    justification: |
      node N020 (N020) grounds BPI dynamics as hydrodynamics in Z-space through the relationship encoded as 'path hysteresis foundation'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI dynamics as hydrodynamics in Z-space without reliance on node N020 (N020) — but the labelled relationship 'path hysteresis foundation' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N021
    target: N371
    label: gradient_descent_foundation
    edge_status: D
    justification: |
      node N021 (N021) grounds BPI dynamics as hydrodynamics in Z-space through the relationship encoded as 'gradient descent foundation'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI dynamics as hydrodynamics in Z-space without reliance on node N021 (N021) — but the labelled relationship 'gradient descent foundation' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N029
    target: N371
    label: rate_law_foundation
    edge_status: D
    justification: |
      node N029 (N029) grounds BPI dynamics as hydrodynamics in Z-space through the relationship encoded as 'rate law foundation'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI dynamics as hydrodynamics in Z-space without reliance on node N029 (N029) — but the labelled relationship 'rate law foundation' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N336
    target: N_CommThm
    label: order_primacy_recency
    edge_status: D
    justification: |
      node N336 (N336) grounds optimal communication theorem through the relationship encoded as 'order primacy recency'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) optimal communication theorem without reliance on node N336 (N336) — but the labelled relationship 'order primacy recency' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N048
    target: N_AestheticEngagement
    label: Birkhoff_dimensions_insufficient
    edge_status: D
    justification: |
      Birkhoff M=O/C aesthetic formula failure grounds aesthetic engagement (artwork-class BPI inputs) through the relationship encoded as 'Birkhoff dimensions insufficient'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) aesthetic engagement (artwork-class BPI inputs) without reliance on Birkhoff M=O/C aesthetic formula failure — but the labelled relationship 'Birkhoff dimensions insufficient' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N330
    target: N371
    label: trust_asymmetry_is_phase_transition_hysteresis
    edge_status: D
    justification: |
      node N330 (N330) grounds BPI dynamics as hydrodynamics in Z-space through the relationship encoded as 'trust asymmetry is phase transition hysteresis'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI dynamics as hydrodynamics in Z-space without reliance on node N330 (N330) — but the labelled relationship 'trust asymmetry is phase transition hysteresis' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N112
    target: N371
    label: i_bound_forces_inverse_problem
    edge_status: D
    justification: |
      Observer Containment Lemma (K(O) < K(F)) grounds BPI dynamics as hydrodynamics in Z-space through the relationship encoded as 'i bound forces inverse problem'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI dynamics as hydrodynamics in Z-space without reliance on Observer Containment Lemma (K(O) < K(F)) — but the labelled relationship 'i bound forces inverse problem' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N187
    target: N188
    label: hard_problem_dissolution_template
    edge_status: D
    justification: |
      Hard Problem dissolved (BPI self-reference = subjectivity) grounds qualia as Z-component patterns through the relationship encoded as 'hard problem dissolution template'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) qualia as Z-component patterns without reliance on Hard Problem dissolved (BPI self-reference = subjectivity) — but the labelled relationship 'hard problem dissolution template' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N188
    target: N_DopaminePredictionError
    label: qualia_mapping_via_dopamine_pathway
    edge_status: D
    justification: |
      qualia as Z-component patterns grounds dopamine prediction-error neural substrate through the relationship encoded as 'qualia mapping via dopamine pathway'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) dopamine prediction-error neural substrate without reliance on qualia as Z-component patterns — but the labelled relationship 'qualia mapping via dopamine pathway' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N188
    target: N_DopaminePredictionError
    label: engagement_class_substrate_documentation
    edge_status: D
    justification: |
      qualia as Z-component patterns grounds dopamine prediction-error neural substrate through the relationship encoded as 'engagement class substrate documentation'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) dopamine prediction-error neural substrate without reliance on qualia as Z-component patterns — but the labelled relationship 'engagement class substrate documentation' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N188
    target: N_BPIEngagement
    label: engagement_class_qualia_specific_case
    edge_status: D
    justification: |
      qualia as Z-component patterns grounds BPI engagement signal (triangulation in time-indexed substrate) through the relationship encoded as 'engagement class qualia specific case'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI engagement signal (triangulation in time-indexed substrate) without reliance on qualia as Z-component patterns — but the labelled relationship 'engagement class qualia specific case' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N188
    target: N_TopologyProcessIdentity
    label: qualia_trigger_one_process_two_coordinates
    edge_status: D
    justification: |
      qualia as Z-component patterns grounds topology-process identity (object frame removed) through the relationship encoded as 'qualia trigger one process two coordinates'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) topology-process identity (object frame removed) without reliance on qualia as Z-component patterns — but the labelled relationship 'qualia trigger one process two coordinates' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N188
    target: N187
    label: qualia_via_subjectivity_dissolution
    edge_status: D
    justification: |
      qualia as Z-component patterns grounds Hard Problem dissolved (BPI self-reference = subjectivity) through the relationship encoded as 'qualia via subjectivity dissolution'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Hard Problem dissolved (BPI self-reference = subjectivity) without reliance on qualia as Z-component patterns — but the labelled relationship 'qualia via subjectivity dissolution' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N188
    target: N112
    label: BPI_self_reference_required
    edge_status: D
    justification: |
      qualia as Z-component patterns grounds Observer Containment Lemma (K(O) < K(F)) through the relationship encoded as 'BPI self reference required'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Observer Containment Lemma (K(O) < K(F)) without reliance on qualia as Z-component patterns — but the labelled relationship 'BPI self reference required' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N188
    target: N_InversiveTheory
    label: alternatives_dissolve_via_no_external_frame
    edge_status: D
    justification: |
      qualia as Z-component patterns grounds structural inevitability principle (no alternatives) through the relationship encoded as 'alternatives dissolve via no external frame'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) structural inevitability principle (no alternatives) without reliance on qualia as Z-component patterns — but the labelled relationship 'alternatives dissolve via no external frame' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N188
    target: DEF
    label: qualia_realise_argmin_Z_in_BPI_internal_coordinate
    edge_status: D
    justification: |
      qualia as Z-component patterns grounds A_0 = argmin Z (foundational definition) through the relationship encoded as 'qualia realise argmin Z in BPI internal coordinate'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) A_0 = argmin Z (foundational definition) without reliance on qualia as Z-component patterns — but the labelled relationship 'qualia realise argmin Z in BPI internal coordinate' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N240
    target: N_LeptonMassScale
    label: mass_scale_grounds_ratios
    edge_status: D
    justification: |
      node N240 (N240) grounds lepton mass scale derivation through the relationship encoded as 'mass scale grounds ratios'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) lepton mass scale derivation without reliance on node N240 (N240) — but the labelled relationship 'mass scale grounds ratios' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N354
    target: N372
    label: analogous_forcing_logic_different_group_structure
    edge_status: D
    justification: |
      Lepton Step 5b (D_F via Bär+APS) grounds Sr_2RuO_4 superconducting Z-minimum (E_g block) through the relationship encoded as 'analogous forcing logic different group structure'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Sr_2RuO_4 superconducting Z-minimum (E_g block) without reliance on Lepton Step 5b (D_F via Bär+APS) — but the labelled relationship 'analogous forcing logic different group structure' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N354
    target: N_LeptonMassScale
    label: scale_derived_here
    edge_status: D
    justification: |
      Lepton Step 5b (D_F via Bär+APS) grounds lepton mass scale derivation through the relationship encoded as 'scale derived here'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) lepton mass scale derivation without reliance on Lepton Step 5b (D_F via Bär+APS) — but the labelled relationship 'scale derived here' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N354
    target: N_LeptonMassScale
    label: scale_parameter_derived_here
    edge_status: D
    justification: |
      Lepton Step 5b (D_F via Bär+APS) grounds lepton mass scale derivation through the relationship encoded as 'scale parameter derived here'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) lepton mass scale derivation without reliance on Lepton Step 5b (D_F via Bär+APS) — but the labelled relationship 'scale parameter derived here' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N354
    target: N240
    label: structure_uniqueness_grounds_ratios
    edge_status: D
    justification: |
      Lepton Step 5b (D_F via Bär+APS) grounds node N240 (N240) through the relationship encoded as 'structure uniqueness grounds ratios'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) node N240 (N240) without reliance on Lepton Step 5b (D_F via Bär+APS) — but the labelled relationship 'structure uniqueness grounds ratios' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_TopologyProcessIdentity
    target: N377
    label: one_omega_many_coordinates_applied_to_SAE
    edge_status: D
    justification: |
      topology-process identity (object frame removed) grounds sparse autoencoder feature absorption (forced LLM divergence) through the relationship encoded as 'one omega many coordinates applied to SAE'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) sparse autoencoder feature absorption (forced LLM divergence) without reliance on topology-process identity (object frame removed) — but the labelled relationship 'one omega many coordinates applied to SAE' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_TopologyProcessIdentity
    target: N376
    label: empirical_boundary_of_applicability_distinct_critical_values
    edge_status: D
    justification: |
      topology-process identity (object frame removed) grounds iron-based multi-orbital coupled phase diagram through the relationship encoded as 'empirical boundary of applicability distinct critical values'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) iron-based multi-orbital coupled phase diagram without reliance on topology-process identity (object frame removed) — but the labelled relationship 'empirical boundary of applicability distinct critical values' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_TopologyProcessIdentity
    target: N375
    label: multiple_observables_one_structural_feature
    edge_status: D
    justification: |
      topology-process identity (object frame removed) grounds cuprate dual-sector phase diagram through the relationship encoded as 'multiple observables one structural feature'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) cuprate dual-sector phase diagram without reliance on topology-process identity (object frame removed) — but the labelled relationship 'multiple observables one structural feature' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_TopologyProcessIdentity
    target: N374
    label: theoretical_models_as_coord_representations
    edge_status: D
    justification: |
      topology-process identity (object frame removed) grounds UPt_3 three-phase superconducting Z-minimum through the relationship encoded as 'theoretical models as coord representations'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) UPt_3 three-phase superconducting Z-minimum without reliance on topology-process identity (object frame removed) — but the labelled relationship 'theoretical models as coord representations' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_TopologyProcessIdentity
    target: N373
    label: one_object_different_coordinates_applied
    edge_status: D
    justification: |
      topology-process identity (object frame removed) grounds UTe_2 superconducting Z-minimum (4D Kramers active space) through the relationship encoded as 'one object different coordinates applied'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) UTe_2 superconducting Z-minimum (4D Kramers active space) without reliance on topology-process identity (object frame removed) — but the labelled relationship 'one object different coordinates applied' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_TopologyProcessIdentity
    target: N372
    label: one_object_two_coordinates_applied
    edge_status: D
    justification: |
      topology-process identity (object frame removed) grounds Sr_2RuO_4 superconducting Z-minimum (E_g block) through the relationship encoded as 'one object two coordinates applied'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Sr_2RuO_4 superconducting Z-minimum (E_g block) without reliance on topology-process identity (object frame removed) — but the labelled relationship 'one object two coordinates applied' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_TopologyProcessIdentity
    target: N188
    label: applied_to_qualia_trigger_pair
    edge_status: D
    justification: |
      topology-process identity (object frame removed) grounds qualia as Z-component patterns through the relationship encoded as 'applied to qualia trigger pair'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) qualia as Z-component patterns without reliance on topology-process identity (object frame removed) — but the labelled relationship 'applied to qualia trigger pair' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_TopologyProcessIdentity
    target: N_GrammarTrap
    label: object_frame_dissolved
    edge_status: D
    justification: |
      topology-process identity (object frame removed) grounds grammar trap (subject-verb-object as ontological structure) through the relationship encoded as 'object frame dissolved'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) grammar trap (subject-verb-object as ontological structure) without reliance on topology-process identity (object frame removed) — but the labelled relationship 'object frame dissolved' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_TopologyProcessIdentity
    target: N_Ontology
    label: topology_is_not_substrate
    edge_status: D
    justification: |
      topology-process identity (object frame removed) grounds node N_Ontology (N_Ontology) through the relationship encoded as 'topology is not substrate'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) node N_Ontology (N_Ontology) without reliance on topology-process identity (object frame removed) — but the labelled relationship 'topology is not substrate' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_TopologyProcessIdentity
    target: N_ForcedId
    label: forced_identification_principle
    edge_status: D
    justification: |
      topology-process identity (object frame removed) grounds forced identification principle through the relationship encoded as 'forced identification principle'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) forced identification principle without reliance on topology-process identity (object frame removed) — but the labelled relationship 'forced identification principle' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_TopologyProcessIdentity
    target: DEF
    label: definition_not_enumeration
    edge_status: D
    justification: |
      topology-process identity (object frame removed) grounds A_0 = argmin Z (foundational definition) through the relationship encoded as 'definition not enumeration'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) A_0 = argmin Z (foundational definition) without reliance on topology-process identity (object frame removed) — but the labelled relationship 'definition not enumeration' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_LeptonMassScale
    target: N_Bar
    label: eta_invariant_gives_delta
    edge_status: D
    justification: |
      lepton mass scale derivation grounds Bär+Vassilevich Dirac spectrum on L(3,1) through the relationship encoded as 'eta invariant gives delta'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Bär+Vassilevich Dirac spectrum on L(3,1) without reliance on lepton mass scale derivation — but the labelled relationship 'eta invariant gives delta' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_LeptonMassScale
    target: N_TopologyProcessIdentity
    label: DM_DF_same_object_two_coordinates
    edge_status: D
    justification: |
      lepton mass scale derivation grounds topology-process identity (object frame removed) through the relationship encoded as 'DM DF same object two coordinates'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) topology-process identity (object frame removed) without reliance on lepton mass scale derivation — but the labelled relationship 'DM DF same object two coordinates' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_LeptonMassScale
    target: N354
    label: scale_A2_closes_N354
    edge_status: D
    justification: |
      lepton mass scale derivation grounds Lepton Step 5b (D_F via Bär+APS) through the relationship encoded as 'scale A2 closes N354'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Lepton Step 5b (D_F via Bär+APS) without reliance on lepton mass scale derivation — but the labelled relationship 'scale A2 closes N354' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_LeptonMassScale
    target: N240
    label: corrected_A_gives_masses
    edge_status: D
    justification: |
      lepton mass scale derivation grounds node N240 (N240) through the relationship encoded as 'corrected A gives masses'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) node N240 (N240) without reliance on lepton mass scale derivation — but the labelled relationship 'corrected A gives masses' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_LeptonMassScale
    target: N_ForcedId
    label: delta_proved_by_gauge_invariance
    edge_status: D
    justification: |
      lepton mass scale derivation grounds forced identification principle through the relationship encoded as 'delta proved by gauge invariance'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) forced identification principle without reliance on lepton mass scale derivation — but the labelled relationship 'delta proved by gauge invariance' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_WylerStepG
    target: N_LeptonMassScale
    label: alpha_measured_constant_not_free
    edge_status: D
    justification: |
      Wyler Step G (alpha = 1/137.036) grounds lepton mass scale derivation through the relationship encoded as 'alpha measured constant not free'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) lepton mass scale derivation without reliance on Wyler Step G (alpha = 1/137.036) — but the labelled relationship 'alpha measured constant not free' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_WylerStepG
    target: N_TopologyProcessIdentity
    label: R2_error_corrected
    edge_status: D
    justification: |
      Wyler Step G (alpha = 1/137.036) grounds topology-process identity (object frame removed) through the relationship encoded as 'R2 error corrected'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) topology-process identity (object frame removed) without reliance on Wyler Step G (alpha = 1/137.036) — but the labelled relationship 'R2 error corrected' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_WylerStepG
    target: N_ForcedId
    label: boundary_irrelevance_ForcedId
    edge_status: D
    justification: |
      Wyler Step G (alpha = 1/137.036) grounds forced identification principle through the relationship encoded as 'boundary irrelevance ForcedId'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) forced identification principle without reliance on Wyler Step G (alpha = 1/137.036) — but the labelled relationship 'boundary irrelevance ForcedId' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N364
    target: N_ZGaugeDecomposition
    label: confinement_is_Z_hidden_covering_constraint
    edge_status: D
    justification: |
      node N364 (N364) grounds Z-gauge decomposition via Z_3 covering action through the relationship encoded as 'confinement is Z hidden covering constraint'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Z-gauge decomposition via Z_3 covering action without reliance on node N364 (N364) — but the labelled relationship 'confinement is Z hidden covering constraint' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N365
    target: N371
    label: taylor_relaxation_class_a
    edge_status: D
    justification: |
      node N365 (N365) grounds BPI dynamics as hydrodynamics in Z-space through the relationship encoded as 'taylor relaxation class a'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI dynamics as hydrodynamics in Z-space without reliance on node N365 (N365) — but the labelled relationship 'taylor relaxation class a' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N370
    target: N_Triangulation
    label: baryon_exclusion_confirms_triangulation
    edge_status: D
    justification: |
      L(3,1) inevitability (baryon number forces p=3) grounds triangulation principle (3 minimal Z-coordinates) through the relationship encoded as 'baryon exclusion confirms triangulation'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) triangulation principle (3 minimal Z-coordinates) without reliance on L(3,1) inevitability (baryon number forces p=3) — but the labelled relationship 'baryon exclusion confirms triangulation' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Triangulation
    target: N_BPIEngagement
    label: triangulation_specifies_engagement_conditions
    edge_status: D
    justification: |
      triangulation principle (3 minimal Z-coordinates) grounds BPI engagement signal (triangulation in time-indexed substrate) through the relationship encoded as 'triangulation specifies engagement conditions'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI engagement signal (triangulation in time-indexed substrate) without reliance on triangulation principle (3 minimal Z-coordinates) — but the labelled relationship 'triangulation specifies engagement conditions' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Triangulation
    target: N376
    label: triangulation_via_magnetic_structural_nematic_spectroscopic
    edge_status: D
    justification: |
      triangulation principle (3 minimal Z-coordinates) grounds iron-based multi-orbital coupled phase diagram through the relationship encoded as 'triangulation via magnetic structural nematic spectroscopic'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) iron-based multi-orbital coupled phase diagram without reliance on triangulation principle (3 minimal Z-coordinates) — but the labelled relationship 'triangulation via magnetic structural nematic spectroscopic' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Triangulation
    target: N375
    label: triangulation_via_charge_thermal_spectroscopic
    edge_status: D
    justification: |
      triangulation principle (3 minimal Z-coordinates) grounds cuprate dual-sector phase diagram through the relationship encoded as 'triangulation via charge thermal spectroscopic'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) cuprate dual-sector phase diagram without reliance on triangulation principle (3 minimal Z-coordinates) — but the labelled relationship 'triangulation via charge thermal spectroscopic' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Triangulation
    target: N374
    label: triangulation_via_f2_local_conduction_intersite
    edge_status: D
    justification: |
      triangulation principle (3 minimal Z-coordinates) grounds UPt_3 three-phase superconducting Z-minimum through the relationship encoded as 'triangulation via f2 local conduction intersite'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) UPt_3 three-phase superconducting Z-minimum without reliance on triangulation principle (3 minimal Z-coordinates) — but the labelled relationship 'triangulation via f2 local conduction intersite' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Triangulation
    target: N373
    label: triangulation_via_j_space_rung_k_space
    edge_status: D
    justification: |
      triangulation principle (3 minimal Z-coordinates) grounds UTe_2 superconducting Z-minimum (4D Kramers active space) through the relationship encoded as 'triangulation via j space rung k space'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) UTe_2 superconducting Z-minimum (4D Kramers active space) without reliance on triangulation principle (3 minimal Z-coordinates) — but the labelled relationship 'triangulation via j space rung k space' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Triangulation
    target: N372
    label: 1_plus_2_as_triangulation_in_D4h
    edge_status: D
    justification: |
      triangulation principle (3 minimal Z-coordinates) grounds Sr_2RuO_4 superconducting Z-minimum (E_g block) through the relationship encoded as '1 plus 2 as triangulation in D4h'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Sr_2RuO_4 superconducting Z-minimum (E_g block) without reliance on triangulation principle (3 minimal Z-coordinates) — but the labelled relationship '1 plus 2 as triangulation in D4h' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Triangulation
    target: N_ZGaugeDecomposition
    label: triangulation_grounds_three_gauge_types
    edge_status: D
    justification: |
      triangulation principle (3 minimal Z-coordinates) grounds Z-gauge decomposition via Z_3 covering action through the relationship encoded as 'triangulation grounds three gauge types'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Z-gauge decomposition via Z_3 covering action without reliance on triangulation principle (3 minimal Z-coordinates) — but the labelled relationship 'triangulation grounds three gauge types' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Triangulation
    target: N370
    label: triangulation_is_primary_Z2_exclusion
    edge_status: D
    justification: |
      triangulation principle (3 minimal Z-coordinates) grounds L(3,1) inevitability (baryon number forces p=3) through the relationship encoded as 'triangulation is primary Z2 exclusion'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) L(3,1) inevitability (baryon number forces p=3) without reliance on triangulation principle (3 minimal Z-coordinates) — but the labelled relationship 'triangulation is primary Z2 exclusion' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Triangulation
    target: N_Invariants
    label: triangulation_forces_three_invariant_types
    edge_status: D
    justification: |
      triangulation principle (3 minimal Z-coordinates) grounds the four invariants (I_Bound, I_Sym, I_Quant, I_Null) through the relationship encoded as 'triangulation forces three invariant types'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) the four invariants (I_Bound, I_Sym, I_Quant, I_Null) without reliance on triangulation principle (3 minimal Z-coordinates) — but the labelled relationship 'triangulation forces three invariant types' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Triangulation
    target: N_Logic
    label: triangulation_grounds_three_term_structure
    edge_status: D
    justification: |
      triangulation principle (3 minimal Z-coordinates) grounds logic as A_0 in symbolic substrate through the relationship encoded as 'triangulation grounds three term structure'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) logic as A_0 in symbolic substrate without reliance on triangulation principle (3 minimal Z-coordinates) — but the labelled relationship 'triangulation grounds three term structure' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Triangulation
    target: N_InversiveTheory
    label: triangulation_confirms_no_alternative
    edge_status: D
    justification: |
      triangulation principle (3 minimal Z-coordinates) grounds structural inevitability principle (no alternatives) through the relationship encoded as 'triangulation confirms no alternative'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) structural inevitability principle (no alternatives) without reliance on triangulation principle (3 minimal Z-coordinates) — but the labelled relationship 'triangulation confirms no alternative' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Triangulation
    target: DEF
    label: triangulation_is_self-referential_closure
    edge_status: D
    justification: |
      triangulation principle (3 minimal Z-coordinates) grounds A_0 = argmin Z (foundational definition) through the relationship encoded as 'triangulation is self-referential closure'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) A_0 = argmin Z (foundational definition) without reliance on triangulation principle (3 minimal Z-coordinates) — but the labelled relationship 'triangulation is self-referential closure' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Triangulation
    target: DEF
    label: triangulation_grounds_Z_decomposition
    edge_status: D
    justification: |
      triangulation principle (3 minimal Z-coordinates) grounds A_0 = argmin Z (foundational definition) through the relationship encoded as 'triangulation grounds Z decomposition'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) A_0 = argmin Z (foundational definition) without reliance on triangulation principle (3 minimal Z-coordinates) — but the labelled relationship 'triangulation grounds Z decomposition' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_ZGaugeDecomposition
    target: N376
    label: four_sector_decomposition_shared_D4h_base
    edge_status: D
    justification: |
      Z-gauge decomposition via Z_3 covering action grounds iron-based multi-orbital coupled phase diagram through the relationship encoded as 'four sector decomposition shared D4h base'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) iron-based multi-orbital coupled phase diagram without reliance on Z-gauge decomposition via Z_3 covering action — but the labelled relationship 'four sector decomposition shared D4h base' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_ZGaugeDecomposition
    target: N375
    label: z_sectors_in_dual_single_regimes
    edge_status: D
    justification: |
      Z-gauge decomposition via Z_3 covering action grounds cuprate dual-sector phase diagram through the relationship encoded as 'z sectors in dual single regimes'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) cuprate dual-sector phase diagram without reliance on Z-gauge decomposition via Z_3 covering action — but the labelled relationship 'z sectors in dual single regimes' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_ZGaugeDecomposition
    target: N374
    label: z_channels_through_local_vs_conduction_sectors
    edge_status: D
    justification: |
      Z-gauge decomposition via Z_3 covering action grounds UPt_3 three-phase superconducting Z-minimum through the relationship encoded as 'z channels through local vs conduction sectors'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) UPt_3 three-phase superconducting Z-minimum without reliance on Z-gauge decomposition via Z_3 covering action — but the labelled relationship 'z channels through local vs conduction sectors' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_ZGaugeDecomposition
    target: N373
    label: z_channels_through_hybridised_sectors
    edge_status: D
    justification: |
      Z-gauge decomposition via Z_3 covering action grounds UTe_2 superconducting Z-minimum (4D Kramers active space) through the relationship encoded as 'z channels through hybridised sectors'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) UTe_2 superconducting Z-minimum (4D Kramers active space) without reliance on Z-gauge decomposition via Z_3 covering action — but the labelled relationship 'z channels through hybridised sectors' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_ZGaugeDecomposition
    target: N372
    label: z_channels_as_t2g_orbital_sectors
    edge_status: D
    justification: |
      Z-gauge decomposition via Z_3 covering action grounds Sr_2RuO_4 superconducting Z-minimum (E_g block) through the relationship encoded as 'z channels as t2g orbital sectors'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Sr_2RuO_4 superconducting Z-minimum (E_g block) without reliance on Z-gauge decomposition via Z_3 covering action — but the labelled relationship 'z channels as t2g orbital sectors' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_ZGaugeDecomposition
    target: N_Triangulation
    label: three_sheets_three_Z_components_one_fact
    edge_status: D
    justification: |
      Z-gauge decomposition via Z_3 covering action grounds triangulation principle (3 minimal Z-coordinates) through the relationship encoded as 'three sheets three Z components one fact'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) triangulation principle (3 minimal Z-coordinates) without reliance on Z-gauge decomposition via Z_3 covering action — but the labelled relationship 'three sheets three Z components one fact' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_ZGaugeDecomposition
    target: N370
    label: Z3_action_grounds_both_Z_and_gauge
    edge_status: D
    justification: |
      Z-gauge decomposition via Z_3 covering action grounds L(3,1) inevitability (baryon number forces p=3) through the relationship encoded as 'Z3 action grounds both Z and gauge'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) L(3,1) inevitability (baryon number forces p=3) without reliance on Z-gauge decomposition via Z_3 covering action — but the labelled relationship 'Z3 action grounds both Z and gauge' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_ZGaugeDecomposition
    target: N364
    label: Z_hidden_sector_forces_confinement
    edge_status: D
    justification: |
      Z-gauge decomposition via Z_3 covering action grounds node N364 (N364) through the relationship encoded as 'Z hidden sector forces confinement'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) node N364 (N364) without reliance on Z-gauge decomposition via Z_3 covering action — but the labelled relationship 'Z hidden sector forces confinement' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_ZGaugeDecomposition
    target: N112
    label: Z_hidden_is_observer_containment
    edge_status: D
    justification: |
      Z-gauge decomposition via Z_3 covering action grounds Observer Containment Lemma (K(O) < K(F)) through the relationship encoded as 'Z hidden is observer containment'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Observer Containment Lemma (K(O) < K(F)) without reliance on Z-gauge decomposition via Z_3 covering action — but the labelled relationship 'Z hidden is observer containment' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_ZGaugeDecomposition
    target: DEF
    label: Z_decomposition_is_covering_structure
    edge_status: D
    justification: |
      Z-gauge decomposition via Z_3 covering action grounds A_0 = argmin Z (foundational definition) through the relationship encoded as 'Z decomposition is covering structure'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) A_0 = argmin Z (foundational definition) without reliance on Z-gauge decomposition via Z_3 covering action — but the labelled relationship 'Z decomposition is covering structure' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_RPfibo
    target: N_RPflow
    label: fibo_feeds_into_flow
    edge_status: D
    justification: |
      RP FIBO Fibonacci coherence rewrite grounds RP FLOW gate+fibo pipeline through the relationship encoded as 'fibo feeds into flow'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) RP FLOW gate+fibo pipeline without reliance on RP FIBO Fibonacci coherence rewrite — but the labelled relationship 'fibo feeds into flow' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Shannon
    target: N_CommThm
    label: Z_hidden_H_proxy
    edge_status: D
    justification: |
      Shannon information theory (H ≡ Z_struct isomorphism) grounds optimal communication theorem through the relationship encoded as 'Z hidden H proxy'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) optimal communication theorem without reliance on Shannon information theory (H ≡ Z_struct isomorphism) — but the labelled relationship 'Z hidden H proxy' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_Sweller
    target: N_CommThm
    label: Z_struct_chunks_proxy
    edge_status: D
    justification: |
      cognitive load theory (Z_struct operational proxy) grounds optimal communication theorem through the relationship encoded as 'Z struct chunks proxy'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) optimal communication theorem without reliance on cognitive load theory (Z_struct operational proxy) — but the labelled relationship 'Z struct chunks proxy' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_BrownLev
    target: N_CommThm
    label: Z_therm_FTA_proxy
    edge_status: D
    justification: |
      politeness theory (Z_therm operational proxy) grounds optimal communication theorem through the relationship encoded as 'Z therm FTA proxy'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) optimal communication theorem without reliance on politeness theory (Z_therm operational proxy) — but the labelled relationship 'Z therm FTA proxy' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_LeDoux
    target: N_CommThm
    label: threshold_regime_shift
    edge_status: D
    justification: |
      amygdala threshold (Z_therm regime shift) grounds optimal communication theorem through the relationship encoded as 'threshold regime shift'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) optimal communication theorem without reliance on amygdala threshold (Z_therm regime shift) — but the labelled relationship 'threshold regime shift' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_LeDoux
    target: N371
    label: theta_is_reynolds_threshold
    edge_status: D
    justification: |
      amygdala threshold (Z_therm regime shift) grounds BPI dynamics as hydrodynamics in Z-space through the relationship encoded as 'theta is reynolds threshold'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI dynamics as hydrodynamics in Z-space without reliance on amygdala threshold (Z_therm regime shift) — but the labelled relationship 'theta is reynolds threshold' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_MI
    target: N_CommThm
    label: Z_hidden_high_strategy
    edge_status: D
    justification: |
      Motivational Interviewing (high-Z_hidden response) grounds optimal communication theorem through the relationship encoded as 'Z hidden high strategy'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) optimal communication theorem without reliance on Motivational Interviewing (high-Z_hidden response) — but the labelled relationship 'Z hidden high strategy' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_MI
    target: N371
    label: z_hidden_reduction_is_landscape_prep
    edge_status: D
    justification: |
      Motivational Interviewing (high-Z_hidden response) grounds BPI dynamics as hydrodynamics in Z-space through the relationship encoded as 'z hidden reduction is landscape prep'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI dynamics as hydrodynamics in Z-space without reliance on Motivational Interviewing (high-Z_hidden response) — but the labelled relationship 'z hidden reduction is landscape prep' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_SDT
    target: N_CommThm
    label: Z_therm_autonomy
    edge_status: D
    justification: |
      Self-Determination Theory (autonomy and Z_therm) grounds optimal communication theorem through the relationship encoded as 'Z therm autonomy'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) optimal communication theorem without reliance on Self-Determination Theory (autonomy and Z_therm) — but the labelled relationship 'Z therm autonomy' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_SIT
    target: N_CommThm
    label: identity_Z_hidden_multiplier
    edge_status: D
    justification: |
      Social Identity Theory (Z_hidden multiplier) grounds optimal communication theorem through the relationship encoded as 'identity Z hidden multiplier'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) optimal communication theorem without reliance on Social Identity Theory (Z_hidden multiplier) — but the labelled relationship 'identity Z hidden multiplier' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_SIT
    target: N371
    label: identity_threat_lowers_turbulence_threshold
    edge_status: D
    justification: |
      Social Identity Theory (Z_hidden multiplier) grounds BPI dynamics as hydrodynamics in Z-space through the relationship encoded as 'identity threat lowers turbulence threshold'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI dynamics as hydrodynamics in Z-space without reliance on Social Identity Theory (Z_hidden multiplier) — but the labelled relationship 'identity threat lowers turbulence threshold' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_CommThm
    target: N_RPsoft
    label: theorem_to_protocol
    edge_status: D
    justification: |
      optimal communication theorem grounds RP SOFT delicate formulation protocol through the relationship encoded as 'theorem to protocol'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) RP SOFT delicate formulation protocol without reliance on optimal communication theorem — but the labelled relationship 'theorem to protocol' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_CommThm
    target: N_RPflow
    label: full_pipeline
    edge_status: D
    justification: |
      optimal communication theorem grounds RP FLOW gate+fibo pipeline through the relationship encoded as 'full pipeline'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) RP FLOW gate+fibo pipeline without reliance on optimal communication theorem — but the labelled relationship 'full pipeline' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_CommThm
    target: N_RPfibo
    label: gradient_invariant
    edge_status: D
    justification: |
      optimal communication theorem grounds RP FIBO Fibonacci coherence rewrite through the relationship encoded as 'gradient invariant'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) RP FIBO Fibonacci coherence rewrite without reliance on optimal communication theorem — but the labelled relationship 'gradient invariant' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N372
    target: N376
    label: sibling_D4h_tetragonal_base_different_active_space_structure
    edge_status: D
    justification: |
      Sr_2RuO_4 superconducting Z-minimum (E_g block) grounds iron-based multi-orbital coupled phase diagram through the relationship encoded as 'sibling D4h tetragonal base different active space structure'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) iron-based multi-orbital coupled phase diagram without reliance on Sr_2RuO_4 superconducting Z-minimum (E_g block) — but the labelled relationship 'sibling D4h tetragonal base different active space structure' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N372
    target: N375
    label: sibling_realisation_D4h_logic_different_A0_application
    edge_status: D
    justification: |
      Sr_2RuO_4 superconducting Z-minimum (E_g block) grounds cuprate dual-sector phase diagram through the relationship encoded as 'sibling realisation D4h logic different A0 application'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) cuprate dual-sector phase diagram without reliance on Sr_2RuO_4 superconducting Z-minimum (E_g block) — but the labelled relationship 'sibling realisation D4h logic different A0 application' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N372
    target: N374
    label: sibling_realisation_hexagonal_with_2D_rep
    edge_status: D
    justification: |
      Sr_2RuO_4 superconducting Z-minimum (E_g block) grounds UPt_3 three-phase superconducting Z-minimum through the relationship encoded as 'sibling realisation hexagonal with 2D rep'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) UPt_3 three-phase superconducting Z-minimum without reliance on Sr_2RuO_4 superconducting Z-minimum (E_g block) — but the labelled relationship 'sibling realisation hexagonal with 2D rep' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N372
    target: N373
    label: sibling_realisation_D2h_instead_of_D4h
    edge_status: D
    justification: |
      Sr_2RuO_4 superconducting Z-minimum (E_g block) grounds UTe_2 superconducting Z-minimum (4D Kramers active space) through the relationship encoded as 'sibling realisation D2h instead of D4h'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) UTe_2 superconducting Z-minimum (4D Kramers active space) without reliance on Sr_2RuO_4 superconducting Z-minimum (E_g block) — but the labelled relationship 'sibling realisation D2h instead of D4h' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N373
    target: N376
    label: sibling_multi_coordinate_methodology_different_electron_class
    edge_status: D
    justification: |
      UTe_2 superconducting Z-minimum (4D Kramers active space) grounds iron-based multi-orbital coupled phase diagram through the relationship encoded as 'sibling multi coordinate methodology different electron class'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) iron-based multi-orbital coupled phase diagram without reliance on UTe_2 superconducting Z-minimum (4D Kramers active space) — but the labelled relationship 'sibling multi coordinate methodology different electron class' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N373
    target: N375
    label: sibling_sector_decomposition_different_parametrization
    edge_status: D
    justification: |
      UTe_2 superconducting Z-minimum (4D Kramers active space) grounds cuprate dual-sector phase diagram through the relationship encoded as 'sibling sector decomposition different parametrization'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) cuprate dual-sector phase diagram without reliance on UTe_2 superconducting Z-minimum (4D Kramers active space) — but the labelled relationship 'sibling sector decomposition different parametrization' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N373
    target: N374
    label: sibling_realisation_U_triplet_different_active_space_mechanism
    edge_status: D
    justification: |
      UTe_2 superconducting Z-minimum (4D Kramers active space) grounds UPt_3 three-phase superconducting Z-minimum through the relationship encoded as 'sibling realisation U triplet different active space mechanism'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) UPt_3 three-phase superconducting Z-minimum without reliance on UTe_2 superconducting Z-minimum (4D Kramers active space) — but the labelled relationship 'sibling realisation U triplet different active space mechanism' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N374
    target: N376
    label: sibling_multi_sector_decomposition_different_parametrization
    edge_status: D
    justification: |
      UPt_3 three-phase superconducting Z-minimum grounds iron-based multi-orbital coupled phase diagram through the relationship encoded as 'sibling multi sector decomposition different parametrization'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) iron-based multi-orbital coupled phase diagram without reliance on UPt_3 three-phase superconducting Z-minimum — but the labelled relationship 'sibling multi sector decomposition different parametrization' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N374
    target: N375
    label: sibling_dual_sector_structure_doping_parametrized
    edge_status: D
    justification: |
      UPt_3 three-phase superconducting Z-minimum grounds cuprate dual-sector phase diagram through the relationship encoded as 'sibling dual sector structure doping parametrized'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) cuprate dual-sector phase diagram without reliance on UPt_3 three-phase superconducting Z-minimum — but the labelled relationship 'sibling dual sector structure doping parametrized' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N375
    target: N376
    label: closest_parallel_parametric_family_different_orbital_sector_structure
    edge_status: D
    justification: |
      cuprate dual-sector phase diagram grounds iron-based multi-orbital coupled phase diagram through the relationship encoded as 'closest parallel parametric family different orbital sector structure'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) iron-based multi-orbital coupled phase diagram without reliance on cuprate dual-sector phase diagram — but the labelled relationship 'closest parallel parametric family different orbital sector structure' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N377
    target: N_TopologyProcessIdentity
    label: divergence_is_one_structure_two_coordinates
    edge_status: D
    justification: |
      sparse autoencoder feature absorption (forced LLM divergence) grounds topology-process identity (object frame removed) through the relationship encoded as 'divergence is one structure two coordinates'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) topology-process identity (object frame removed) without reliance on sparse autoencoder feature absorption (forced LLM divergence) — but the labelled relationship 'divergence is one structure two coordinates' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N377
    target: N304
    label: multi_BPI_argmin_divergence_as_sibling_coordinate_of_LLM_geometry
    edge_status: D
    justification: |
      sparse autoencoder feature absorption (forced LLM divergence) grounds LLM latent space ≈ BA-fractal through the relationship encoded as 'multi BPI argmin divergence as sibling coordinate of LLM geometry'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) LLM latent space ≈ BA-fractal without reliance on sparse autoencoder feature absorption (forced LLM divergence) — but the labelled relationship 'multi BPI argmin divergence as sibling coordinate of LLM geometry' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N377
    target: N_InversiveTheory
    label: SAE_divergence_is_coordinate_expression
    edge_status: D
    justification: |
      sparse autoencoder feature absorption (forced LLM divergence) grounds structural inevitability principle (no alternatives) through the relationship encoded as 'SAE divergence is coordinate expression'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) structural inevitability principle (no alternatives) without reliance on sparse autoencoder feature absorption (forced LLM divergence) — but the labelled relationship 'SAE divergence is coordinate expression' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_BPIEngagement
    target: N_AestheticEngagement
    label: aesthetic_is_application
    edge_status: D
    justification: |
      BPI engagement signal (triangulation in time-indexed substrate) grounds aesthetic engagement (artwork-class BPI inputs) through the relationship encoded as 'aesthetic is application'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) aesthetic engagement (artwork-class BPI inputs) without reliance on BPI engagement signal (triangulation in time-indexed substrate) — but the labelled relationship 'aesthetic is application' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_BPIEngagement
    target: N_AestheticEngagement
    label: application_to_aesthetic_substrates
    edge_status: D
    justification: |
      BPI engagement signal (triangulation in time-indexed substrate) grounds aesthetic engagement (artwork-class BPI inputs) through the relationship encoded as 'application to aesthetic substrates'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) aesthetic engagement (artwork-class BPI inputs) without reliance on BPI engagement signal (triangulation in time-indexed substrate) — but the labelled relationship 'application to aesthetic substrates' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_BPIEngagement
    target: N_FEP
    label: mathematical_formalization_in_biological_substrate
    edge_status: D
    justification: |
      BPI engagement signal (triangulation in time-indexed substrate) grounds Free Energy Principle (variational A_0 in Bayesian substrate) through the relationship encoded as 'mathematical formalization in biological substrate'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Free Energy Principle (variational A_0 in Bayesian substrate) without reliance on BPI engagement signal (triangulation in time-indexed substrate) — but the labelled relationship 'mathematical formalization in biological substrate' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_BPIEngagement
    target: N_DopaminePredictionError
    label: neural_implementation_in_biological_substrate
    edge_status: D
    justification: |
      BPI engagement signal (triangulation in time-indexed substrate) grounds dopamine prediction-error neural substrate through the relationship encoded as 'neural implementation in biological substrate'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) dopamine prediction-error neural substrate without reliance on BPI engagement signal (triangulation in time-indexed substrate) — but the labelled relationship 'neural implementation in biological substrate' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_BPIEngagement
    target: N_Triangulation
    label: engagement_is_triangulation_in_BPI_tracking
    edge_status: D
    justification: |
      BPI engagement signal (triangulation in time-indexed substrate) grounds triangulation principle (3 minimal Z-coordinates) through the relationship encoded as 'engagement is triangulation in BPI tracking'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) triangulation principle (3 minimal Z-coordinates) without reliance on BPI engagement signal (triangulation in time-indexed substrate) — but the labelled relationship 'engagement is triangulation in BPI tracking' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_BPIEngagement
    target: N_TopologyProcessIdentity
    label: substrate_observation_one_object_many_coordinates
    edge_status: D
    justification: |
      BPI engagement signal (triangulation in time-indexed substrate) grounds topology-process identity (object frame removed) through the relationship encoded as 'substrate observation one object many coordinates'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) topology-process identity (object frame removed) without reliance on BPI engagement signal (triangulation in time-indexed substrate) — but the labelled relationship 'substrate observation one object many coordinates' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_BPIEngagement
    target: N188
    label: qualia_engagement_signal_unification
    edge_status: D
    justification: |
      BPI engagement signal (triangulation in time-indexed substrate) grounds qualia as Z-component patterns through the relationship encoded as 'qualia engagement signal unification'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) qualia as Z-component patterns without reliance on BPI engagement signal (triangulation in time-indexed substrate) — but the labelled relationship 'qualia engagement signal unification' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_BPIEngagement
    target: N188
    label: engagement_signal_is_qualia_in_BPI_tracking
    edge_status: D
    justification: |
      BPI engagement signal (triangulation in time-indexed substrate) grounds qualia as Z-component patterns through the relationship encoded as 'engagement signal is qualia in BPI tracking'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) qualia as Z-component patterns without reliance on BPI engagement signal (triangulation in time-indexed substrate) — but the labelled relationship 'engagement signal is qualia in BPI tracking' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_BPIEngagement
    target: N112
    label: BPI_bound_required_for_engagement_definition
    edge_status: D
    justification: |
      BPI engagement signal (triangulation in time-indexed substrate) grounds Observer Containment Lemma (K(O) < K(F)) through the relationship encoded as 'BPI bound required for engagement definition'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Observer Containment Lemma (K(O) < K(F)) without reliance on BPI engagement signal (triangulation in time-indexed substrate) — but the labelled relationship 'BPI bound required for engagement definition' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_BPIEngagement
    target: N048
    label: explains_Birkhoff_failure_via_dimension_count
    edge_status: D
    justification: |
      BPI engagement signal (triangulation in time-indexed substrate) grounds Birkhoff M=O/C aesthetic formula failure through the relationship encoded as 'explains Birkhoff failure via dimension count'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Birkhoff M=O/C aesthetic formula failure without reliance on BPI engagement signal (triangulation in time-indexed substrate) — but the labelled relationship 'explains Birkhoff failure via dimension count' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_BPIEngagement
    target: N_NoSeparatePieces
    label: substrates_are_observation_angles
    edge_status: D
    justification: |
      BPI engagement signal (triangulation in time-indexed substrate) grounds coordinate-unification principle (one structure, many coordinates) through the relationship encoded as 'substrates are observation angles'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) coordinate-unification principle (one structure, many coordinates) without reliance on BPI engagement signal (triangulation in time-indexed substrate) — but the labelled relationship 'substrates are observation angles' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_BPIEngagement
    target: N_InversiveTheory
    label: alternatives_dissolve_under_removal
    edge_status: D
    justification: |
      BPI engagement signal (triangulation in time-indexed substrate) grounds structural inevitability principle (no alternatives) through the relationship encoded as 'alternatives dissolve under removal'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) structural inevitability principle (no alternatives) without reliance on BPI engagement signal (triangulation in time-indexed substrate) — but the labelled relationship 'alternatives dissolve under removal' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_BPIEngagement
    target: DEF
    label: engagement_realises_argmin_Z
    edge_status: D
    justification: |
      BPI engagement signal (triangulation in time-indexed substrate) grounds A_0 = argmin Z (foundational definition) through the relationship encoded as 'engagement realises argmin Z'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) A_0 = argmin Z (foundational definition) without reliance on BPI engagement signal (triangulation in time-indexed substrate) — but the labelled relationship 'engagement realises argmin Z' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_DopaminePredictionError
    target: N_FEP
    label: neural_implementation_of_F_minimization
    edge_status: D
    justification: |
      dopamine prediction-error neural substrate grounds Free Energy Principle (variational A_0 in Bayesian substrate) through the relationship encoded as 'neural implementation of F minimization'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Free Energy Principle (variational A_0 in Bayesian substrate) without reliance on dopamine prediction-error neural substrate — but the labelled relationship 'neural implementation of F minimization' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_DopaminePredictionError
    target: N_BPIEngagement
    label: empirical_anchor_for_engagement_signal
    edge_status: D
    justification: |
      dopamine prediction-error neural substrate grounds BPI engagement signal (triangulation in time-indexed substrate) through the relationship encoded as 'empirical anchor for engagement signal'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI engagement signal (triangulation in time-indexed substrate) without reliance on dopamine prediction-error neural substrate — but the labelled relationship 'empirical anchor for engagement signal' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_DopaminePredictionError
    target: N188
    label: closes_qualia_mapping_identification_gap
    edge_status: D
    justification: |
      dopamine prediction-error neural substrate grounds qualia as Z-component patterns through the relationship encoded as 'closes qualia mapping identification gap'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) qualia as Z-component patterns without reliance on dopamine prediction-error neural substrate — but the labelled relationship 'closes qualia mapping identification gap' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_DopaminePredictionError
    target: N188
    label: neural_implementation_of_engagement_qualia
    edge_status: D
    justification: |
      dopamine prediction-error neural substrate grounds qualia as Z-component patterns through the relationship encoded as 'neural implementation of engagement qualia'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) qualia as Z-component patterns without reliance on dopamine prediction-error neural substrate — but the labelled relationship 'neural implementation of engagement qualia' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_DopaminePredictionError
    target: N112
    label: BPI_prediction_error_tracking_implemented_neurally
    edge_status: D
    justification: |
      dopamine prediction-error neural substrate grounds Observer Containment Lemma (K(O) < K(F)) through the relationship encoded as 'BPI prediction error tracking implemented neurally'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Observer Containment Lemma (K(O) < K(F)) without reliance on dopamine prediction-error neural substrate — but the labelled relationship 'BPI prediction error tracking implemented neurally' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_DopaminePredictionError
    target: DEF
    label: argmin_Z_implemented_in_dopaminergic_circuits
    edge_status: D
    justification: |
      dopamine prediction-error neural substrate grounds A_0 = argmin Z (foundational definition) through the relationship encoded as 'argmin Z implemented in dopaminergic circuits'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) A_0 = argmin Z (foundational definition) without reliance on dopamine prediction-error neural substrate — but the labelled relationship 'argmin Z implemented in dopaminergic circuits' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_FEP
    target: N_DopaminePredictionError
    label: F_minimization_implemented_via_dopamine_signaling
    edge_status: D
    justification: |
      Free Energy Principle (variational A_0 in Bayesian substrate) grounds dopamine prediction-error neural substrate through the relationship encoded as 'F minimization implemented via dopamine signaling'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) dopamine prediction-error neural substrate without reliance on Free Energy Principle (variational A_0 in Bayesian substrate) — but the labelled relationship 'F minimization implemented via dopamine signaling' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_FEP
    target: N_BPIEngagement
    label: variational_formulation_of_engagement
    edge_status: D
    justification: |
      Free Energy Principle (variational A_0 in Bayesian substrate) grounds BPI engagement signal (triangulation in time-indexed substrate) through the relationship encoded as 'variational formulation of engagement'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI engagement signal (triangulation in time-indexed substrate) without reliance on Free Energy Principle (variational A_0 in Bayesian substrate) — but the labelled relationship 'variational formulation of engagement' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_FEP
    target: N_Shannon
    label: FEP_decomposes_via_Shannon_H_equiv_Z_struct
    edge_status: D
    justification: |
      Free Energy Principle (variational A_0 in Bayesian substrate) grounds Shannon information theory (H ≡ Z_struct isomorphism) through the relationship encoded as 'FEP decomposes via Shannon H equiv Z struct'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Shannon information theory (H ≡ Z_struct isomorphism) without reliance on Free Energy Principle (variational A_0 in Bayesian substrate) — but the labelled relationship 'FEP decomposes via Shannon H equiv Z struct' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_FEP
    target: N112
    label: FEP_uses_BPI_bound_via_Markov_blanket
    edge_status: D
    justification: |
      Free Energy Principle (variational A_0 in Bayesian substrate) grounds Observer Containment Lemma (K(O) < K(F)) through the relationship encoded as 'FEP uses BPI bound via Markov blanket'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Observer Containment Lemma (K(O) < K(F)) without reliance on Free Energy Principle (variational A_0 in Bayesian substrate) — but the labelled relationship 'FEP uses BPI bound via Markov blanket' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_FEP
    target: N_Logic
    label: sibling_substrate_realisation_of_A0
    edge_status: D
    justification: |
      Free Energy Principle (variational A_0 in Bayesian substrate) grounds logic as A_0 in symbolic substrate through the relationship encoded as 'sibling substrate realisation of A0'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) logic as A_0 in symbolic substrate without reliance on Free Energy Principle (variational A_0 in Bayesian substrate) — but the labelled relationship 'sibling substrate realisation of A0' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_FEP
    target: N_NoSeparatePieces
    label: perception_action_one_process
    edge_status: D
    justification: |
      Free Energy Principle (variational A_0 in Bayesian substrate) grounds coordinate-unification principle (one structure, many coordinates) through the relationship encoded as 'perception action one process'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) coordinate-unification principle (one structure, many coordinates) without reliance on Free Energy Principle (variational A_0 in Bayesian substrate) — but the labelled relationship 'perception action one process' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_FEP
    target: DEF
    label: FEP_is_A0_in_Bayesian_substrate
    edge_status: D
    justification: |
      Free Energy Principle (variational A_0 in Bayesian substrate) grounds A_0 = argmin Z (foundational definition) through the relationship encoded as 'FEP is A0 in Bayesian substrate'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) A_0 = argmin Z (foundational definition) without reliance on Free Energy Principle (variational A_0 in Bayesian substrate) — but the labelled relationship 'FEP is A0 in Bayesian substrate' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_AestheticEngagement
    target: N_DopaminePredictionError
    label: neural_substrate_of_aesthetic_response
    edge_status: D
    justification: |
      aesthetic engagement (artwork-class BPI inputs) grounds dopamine prediction-error neural substrate through the relationship encoded as 'neural substrate of aesthetic response'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) dopamine prediction-error neural substrate without reliance on aesthetic engagement (artwork-class BPI inputs) — but the labelled relationship 'neural substrate of aesthetic response' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_AestheticEngagement
    target: N_BPIEngagement
    label: aesthetic_is_engagement_in_artwork_input
    edge_status: D
    justification: |
      aesthetic engagement (artwork-class BPI inputs) grounds BPI engagement signal (triangulation in time-indexed substrate) through the relationship encoded as 'aesthetic is engagement in artwork input'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) BPI engagement signal (triangulation in time-indexed substrate) without reliance on aesthetic engagement (artwork-class BPI inputs) — but the labelled relationship 'aesthetic is engagement in artwork input' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_AestheticEngagement
    target: N_Triangulation
    label: three_aesthetic_dimensions_via_triangulation
    edge_status: D
    justification: |
      aesthetic engagement (artwork-class BPI inputs) grounds triangulation principle (3 minimal Z-coordinates) through the relationship encoded as 'three aesthetic dimensions via triangulation'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) triangulation principle (3 minimal Z-coordinates) without reliance on aesthetic engagement (artwork-class BPI inputs) — but the labelled relationship 'three aesthetic dimensions via triangulation' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_AestheticEngagement
    target: N048
    label: explains_Birkhoff_failure
    edge_status: D
    justification: |
      aesthetic engagement (artwork-class BPI inputs) grounds Birkhoff M=O/C aesthetic formula failure through the relationship encoded as 'explains Birkhoff failure'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) Birkhoff M=O/C aesthetic formula failure without reliance on aesthetic engagement (artwork-class BPI inputs) — but the labelled relationship 'explains Birkhoff failure' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

  - source: N_AestheticEngagement
    target: N_NoSeparatePieces
    label: art_forms_are_observation_angles
    edge_status: D
    justification: |
      aesthetic engagement (artwork-class BPI inputs) grounds coordinate-unification principle (one structure, many coordinates) through the relationship encoded as 'art forms are observation angles'. The edge captures structural dependency from source's foundational role to target's specialised manifestation.
    why_forced: |
      Alternative requires either (a) coordinate-unification principle (one structure, many coordinates) without reliance on aesthetic engagement (artwork-class BPI inputs) — but the labelled relationship 'art forms are observation angles' encodes a structural input that cannot be removed without dissolving the derivation; or (b) different grounding for the same target — but no competing source provides equivalent structural anchor.

nodes:
  - id: N165
    title: Ergodicity of the Reality Protocol
    layer: structure
    status: DEMONSTRATED
    anchors: 3
    a_infinity: false
    is_placeholder: false
    summary: |
      Reality Protocol is an ergodic Markov chain on the Z-graph. The chain
      converges to a unique stationary distribution (Gibbs state) uniform on
      each Z3-orbit. Single result simultaneously closes three previously
      open problems: derivation of r=√2 (Koide), Poisson distribution of A0
      transitions, and emergence of complex Hilbert space.
    why_status: |
      DEMONSTRATED conditional on axiom F (finite Z-graph), which follows from
      the Bekenstein-Hawking / Bousso covariant entropy bound: any finite
      spatial region has finite degrees of freedom. Three independent
      sub-results form the chain: (F) finiteness via holographic bound,
      (I) irreducibility via vertex-transitivity under I_Sym on a connected
      3-manifold, (A) aperiodicity via Z3 (odd-cycle, non-bipartite) plus
      monotonic Z_therm increase. Perron-Frobenius then gives the unique
      stationary distribution. A=3 from these three independent confirmation
      paths. Note: consequence (3) — complex Hilbert space — is STRONG not
      DEMONSTRATED, carried by the identification step L^2(L(3,1)) = physical
      Hilbert space; consequences (1) and (2) are full DEMONSTRATED given
      ergodicity.
    not_misinterpretations: |
      this is Boltzmann ergodicity of single-trajectory coverage (it is
      Jaynes ergodicity — convergence to maximum-entropy distribution on
      the accessible phase space, like water reaching hydrostatic
      equilibrium without every molecule visiting every point) ·
      complex Hilbert space follows from ergodicity alone (it follows
      from Z3 representation theory; ergodicity establishes the Gibbs
      state over which L^2 is defined, but the complex structure comes
      from the fact that Z3 has no irreducible real representation of
      degree 3) · real or quaternionic alternatives are possible for
      the Hilbert space on L(3,1) (the Z3 spectrum excludes them by
      character theory; complex coefficients are structurally necessary)

  - id: N067
    title: Spectral Dimension d_s(r) and the Half-Transition Scale
    layer: structure
    status: DEMONSTRATED
    anchors: 3
    a_infinity: false
    is_placeholder: false
    summary: |
      The spectral dimension d_s(r) = 2 + 2r/(r + r_0) follows uniquely from
      RG fixed-point analysis with η = 0 (exact for the linear A_0 operator).
      At the unique half-transition scale r_0, d_s(r_0) = 3. Three independent
      characterisations of r_0 coincide: (i) scale where d_s = 3, (ii) thermal
      de Broglie wavelength sqrt(ℏ²/m k_B T), (iii) impedance balance scale
      where Z_struct = Z_therm. Boundary conditions: d_s(0) = 2 (UV, Z₃ fibre
      freezing on Hopf base S²), d_s(∞) = 4 (IR, full L(3,1) × ℝ resolved).
    why_status: |
      DEMONSTRATED on three independent paths converging on the same formula.
      (1) Functional form: η = 0 is exact for A_0 = argmin Z (a linear
      selection operator with no higher-order interactions; Gaussian fixed
      point per Nelson 1966 / Wilson-Fisher). The RG equation r·dd_s/dr =
      c(d_s − 2)(4 − d_s) with η = 0 yields uniquely n = 1 in d_s(r) = 2 +
      2/(1 + (r_0/r)^n). (2) UV boundary d_s(0) = 2: forced by Z₃ fibre-
      freezing on the Hopf S¹ → S³ → S² of L(3,1); the discrete Z₃
      direction drops out of the return-probability dimension at r ≪ r_0.
      (3) IR boundary d_s(∞) = 4: dim(L(3,1)) + dim(ℝ_time) = 3 + 1.
      Independent external confirmation: Causal Dynamical Triangulations,
      Asymptotic Safety Gravity, and Loop Quantum Gravity find d_s(0) = 2
      in three separate quantum gravity programmes — convergence on the
      same UV reduction without coordination, evidence the Z₃ fibre-freezing
      mechanism is universal.
    not_misinterpretations: |
      d_s(r) = 2 + 2r/(r + r_0) is fitted to data (it is RG fixed-point
      determined by η = 0 exactness; no fit) · η ≠ 0 might apply (η = 0
      identically for argmin of a linear functional — no non-Gaussian
      vertices to generate anomalous dimension) · d_s(0) = 2 is borrowed
      from CDT/ASG/LQG (it is structurally derived from Z₃ fibre-freezing
      on the Hopf base S²; CDT/ASG/LQG independently capture the same
      mechanism, which is why their convergence is evidence not assumption)
      · the value 3 at r_0 is approximate (it is exact substitution into
      the RG formula at the half-transition scale)

  - id: N129
    title: Spectral Triple Algebra A_F = ℂ ⊕ ℍ ⊕ M_3(ℂ) Derived from L(3,1)
    layer: physics
    status: DEMONSTRATED
    anchors: 3
    a_infinity: false
    is_placeholder: false
    summary: |
      Connes-Lott and Chamseddine-Connes (1996) showed the full Standard
      Model Lagrangian + Einstein-Hilbert gravity emerge from the Spectral
      Action on a finite spectral triple with algebra A_F = ℂ ⊕ ℍ ⊕ M_3(ℂ).
      In standard formulations this algebra is postulated. Here it is
      DERIVED from L(3,1) topology: π₁(L(3,1)) = Z_3 carries a free
      Z_3-action that decomposes L²(L(3,1)) into three sectors (triality
      0/1/2 = leptons/quarks/antiquarks); KO-dimension calculation gives
      6 (mod 8); Krajewski (1998) classification + Wedderburn's theorem
      uniquely select ℂ ⊕ ℍ ⊕ M_3(ℂ) as the only algebra satisfying KO-dim 6
      + three sectors + Poincaré duality + first-order condition + minimality.
    why_status: |
      DEMONSTRATED on Steps 1-5 of the derivation chain (the SM algebra
      itself + KO-dim + uniqueness). Three independent confirmation paths:
      (1) Z_3-decomposition of L²(L(3,1)) follows from standard
      representation theory of free Z_3-action — no identification gap;
      (2) KO-dimension 6 follows from Connes' standard table for product
      manifolds and is independently confirmed by Connes (2006) via the
      charge-conjugation J² = -1 requirement of the SM particle-antiparticle
      structure — two routes converge on the same answer;
      (3) Krajewski (1998 Theorem 4.1) + Wedderburn elimination uniquely
      forces ℂ ⊕ ℍ ⊕ M_3(ℂ); independently confirmed by Chamseddine-Connes
      (2007 Theorem 4.1).
      Step 6 (full SM Lagrangian via spectral action including exact Higgs
      mass at higher order) remains STRONG conditional on Steps 1-4 — open
      identification, not open chain.
    not_misinterpretations: |
      L(3,1) is still postulated (no — DEMONSTRATED via N370: baryon number
      constraint forces p=3, Poincaré-Perelman forces S³) · 3 generations is
      a free parameter (no — follows from |π_1| = p = 3) · the algebra is
      one option among many (Krajewski + Wedderburn + KO-dim 6 + minimality
      jointly force ℂ ⊕ ℍ ⊕ M_3(ℂ) uniquely; alternative algebras either
      violate Poincaré duality or introduce redundant representations) ·
      KO-dimension 6 is conventional (it follows from L(3,1) compact oriented
      Riemannian 3-manifold contributing 3 + Z_3 deck adding 3, confirmed
      independently by Connes' fermion-doubling J²=-1 argument)

  - id: N330
    title: Polarisation as A₀ Binary Compression Under Information Overload
    layer: comms
    status: DEMONSTRATED
    anchors: 3
    a_infinity: false
    is_placeholder: false
    summary: |
      Under sustained information overload (K(F) >> K(O), per N112), the
      BPI must compress n-dimensional input to a budget-K(O) representation.
      The A_0-optimal compression is the minimum non-trivial partition:
      two states. Any BPI network under sustained overload necessarily
      collapses to a binary state. Polarisation is not a social pathology
      requiring explanation — it is the asymptotic argmin Z of compression
      under tightening K(O) constraint.
    why_status: |
      DEMONSTRATED through forced chain N112 (DEMONSTRATED, Observer
      Containment) + I_Bound (DEMONSTRATED, finite K(O) budget) ⇒
      binarisation as argmin Z. No identification step remains. A=3 from
      three independent confirmations: (1) Observer Containment K(O)<K(F)
      requires compression; (2) I_Bound forces finite budget on
      compression representation; (3) minimum non-trivial partition is
      mathematically two — any 1-partition is trivial, any n-partition
      with n>2 requires more than budget-K(O) bits to maintain
      distinctions.
    not_misinterpretations: |
      polarisation is irrational mass behaviour (it is the structurally
      forced asymptote of BPI compression under overload — argmin Z, not
      irrationality) · this requires modelling group dynamics (the binary
      collapse is per-BPI; collective polarisation is the aggregate of
      individual A_0-forced compressions) · diversity-promotion can
      reverse polarisation (it can only succeed if it reduces K(F)
      below the K(O) saturation threshold; otherwise the compression
      remains forced) · this is descriptive, not predictive (it predicts
      that any information environment with K(F) >> K(O) will produce
      polarisation in connected BPI networks — observationally
      consistent with social-media polarisation studies)

  - id: N332
    title: Trust Asymmetry as Second Law in Social Graphs
    layer: comms
    status: DEMONSTRATED
    anchors: 3
    a_infinity: false
    is_placeholder: false
    summary: |
      Trust = Z_hidden(A|B) ≈ 0 (B's state is predictable to A within
      relevant scope). Building trust = reducing Z_hidden = entropy
      reduction = costs work (Second Law / I_Null, DEMONSTRATED).
      Destroying trust = one Z_hidden spike = entropy increase = free.
      The asymmetric "trust takes years, seconds to destroy" is a direct
      Second Law corollary in social graphs — phase-transition hysteresis
      with sustained-input forward direction and barrier-free reverse.
      ΔS(order→disorder) ≫ ΔS(disorder→order) IS I_Null in the social
      substrate.
    why_status: |
      DEMONSTRATED through forced chain I_Null (Second Law, one of four
      DEMONSTRATED invariants in N_Invariants) ⇒ entropy direction
      preserved across substrates ⇒ trust dynamics asymmetric. The
      identification Z_hidden(A|B) ≡ entropy of inaccessible state is
      Class A (forced by N078 Landauer grounding). A=3 from three
      independent confirmations: (1) I_Null at manifold level
      (Z_therm strict monotone increase via Landauer); (2) Z_hidden as
      thermodynamic entropy via N078 (Jacobson grounding);
      (3) phenomenological observation across social systems (no
      society spontaneously increases trust; trust collapse is generic
      and rapid).
    not_misinterpretations: |
      this is a moral observation about human nature (it is a Second-Law
      corollary in the social substrate — same equation as physical
      entropy direction) · trust can be rebuilt symmetrically with
      enough effort (the asymmetry is in spontaneous direction; rebuilding
      requires sustained work input, exactly as predicted) · this is
      pessimistic / depressing (it is structurally precise; it identifies
      the work required to maintain trust, which enables interventions)
      · this contradicts examples of rapid trust development (rapid trust
      = rapid Z_hidden reduction, possible when channel capacity matches
      work input; the asymmetry is in spontaneous baseline, not in
      maximum achievable rate)

  - id: N183
    title: Topology of Cognitive States — Neurosis as Metastable Vortex, Meditation as Z_narr → 0
    layer: observers
    status: DEMONSTRATED
    anchors: 4
    a_infinity: false
    is_placeholder: false
    summary: |
      Biological and cognitive processes are A_0 topologies. A biological
      cell is mathematically indistinguishable from a whirlpool in
      draining water — both are temporary topological anomalies that
      accelerate entropy export. Neurosis is topological fragmentation:
      co-activation of predictive circuits with opposing impedance
      vectors, causing energy to reflect internally between
      high-resistance walls (anxiety/pain/suffering = thermodynamic
      telemetry of internal reflection). Psychological integration =
      establishing internal impedance matching. Meditation as DMN
      suspension: Z_narr → 0, reducing the Landauer cost of continuously
      predicting and updating the narrative self/world boundary.
      "Dissolution of self" / nirvana = zero-resistance configuration of
      the narrative loop.
    why_status: |
      DEMONSTRATED for the neurosis/meditation core via forced chain
      from N371 (BPI dynamics is hydrodynamics in Z-space, Class A,
      DEMONSTRATED): regime taxonomy gives metastable vortex (neurosis)
      and zero-resistance configuration (meditation) as direct
      consequences with no additional identification step. A=4 from
      four independent confirmations: (1) N371 hydrodynamic derivation
      itself; (2) Predictive processing literature (Clark, Friston) —
      DMN as continuous forward-model generation, with Landauer erasure
      of unrealised branches; (3) clinical phenomenology of neurosis
      as inner conflict (impedance mismatch); (4) meditative-tradition
      practitioner reports of "dissolution of self" matching
      Reduction-4 + N112 Observer Containment (suspended BPI separation).
      Status mixed for the full chapter: STRONG for general cognitive
      states (full mapping requires domain-specific Z-component
      identification); CONDITIONAL for specific qualia mappings
      (which quale to which precise neural substrate — needs fMRI).
    not_misinterpretations: |
      neurosis is "weakness" or "moral failure" (it is topological
      fragmentation, a thermodynamic regime of the BPI hydrodynamic
      flow; suffering is thermodynamic telemetry, not punishment) ·
      meditation produces "altered consciousness" as a separate
      experience (it produces zero-resistance configuration of the
      narrative loop — the same A_0 dynamics, with one Z-component
      reduced) · "dissolution of self" is mystical or unscientific
      (it is structurally describable as Z_narr → 0; consistent with
      Reduction 4 in Observer Containment chain) · cell vs vortex is
      analogy (it is structural identity — both are dissipative
      topological anomalies; "living" vs "non-living" is a gradient,
      not a categorical distinction) · DNA "wants" to reproduce (it
      is a minimal-description compression of a successful
      low-impedance configuration; reproduction is solving the
      engineering problem of preserving structure across thermal
      death of substrate, no agent involved)

  - id: N078
    title: Gravity as Thermodynamics — Jacobson Identity, Graviton as Category Error
    layer: physics
    status: DEMONSTRATED
    anchors: 4
    a_infinity: false
    is_placeholder: false
    summary: |
      Jacobson (1995) derived the full Einstein field equations from the
      first law of thermodynamics applied to local Rindler horizons.
      Gravity is thermodynamic structure of Z-gradients, not a force field.
      If gravity IS thermodynamics, there is no independent field to
      quantise; the graviton is a categorical error. Mass = high
      Z_struct density; objects follow A_0-geodesics through the Z-field
      basin. The same Landauer-grounded identification Z = -k_B T S that
      defines A_0 = argmin Z reduces directly to Einstein's equations
      under Jacobson's derivation — gravitational physics IS A_0 in the
      gravitational substrate.
    why_status: |
      DEMONSTRATED via the forced chain Landauer → Z = -k_B T S → A_0 =
      argmin Z = argmin F = thermodynamic equilibrium → (Jacobson 1995)
      Einstein field equations. Each step is independently DEMONSTRATED
      (Landauer principle is forced by I_Null bit-erasure cost; Jacobson's
      derivation is mainstream physics). A=4 from four independent
      programmes converging on gravity-as-thermodynamics: (1) Jacobson
      1995 thermodynamic derivation of EFE; (2) Verlinde 2011/2016
      entropic gravity from holographic screens; (3) Susskind 2014
      complexity = volume connecting CFT complexity to ER bridge geometry;
      (4) Penington/Almheiri 2019-2020 island formula for black hole
      information. None derived from Z-impedance formalism; each is a
      convergent independent confirmation. The Bullet Cluster lensing-vs-
      gas separation is direct empirical evidence for Z_hidden as the
      gravitational source rather than a particle.
    not_misinterpretations: |
      gravity is a force (it is the Z-field topology; objects follow
      minimum-Z paths, not paths bent by a force) · the graviton just
      hasn't been found yet (no — there is no field to quantise; gravity
      IS the background, not an excitation of it; quantising the
      background is a category error) · dark matter is undetected particles
      (it is Z_hidden density, the inaccessible entropic content of the
      manifold; Bullet Cluster behaviour is forced by Z_hidden following
      A_0-geodesics without electromagnetic coupling, not by collisionless
      particles) · Verlinde and Jacobson are alternative theories (they
      are the same A_0 structure in different coordinate languages; their
      convergence is N_InversiveTheory manifesting) · the cosmological
      constant requires fine-tuning (Z_hidden(0) = 0 is forced by the
      compactness of L(3,1); fine-tuning dissolves) · this contradicts
      observational gravitation (it reproduces all GR predictions; it
      reframes their substrate, not their content)

  - id: N_EngagementArchitecture
    title: Engagement Architecture as A₀ at Wundt Apex
    layer: observers
    status: DEMONSTRATED
    anchors: 5
    a_infinity: false
    is_placeholder: false
    summary: |
      The unique configuration that consistently lands BPI prediction-error
      magnitude at the Wundt apex (the optimum of inverted-U pleasure-vs-
      complexity relationship). Architecture: fractal substrate (5 of 6
      music-engagement criteria — anchor, single regime, return, identity
      through transformation, multi-scale) + engineered break (1 criterion
      — concentrated single-locus deviation) + return to fractal. Forced
      as A_0 minimisation of |prediction_error − optimal_dopamine_threshold|.
      Generalises beyond music to any engagement-driven substrate
      (storytelling, drama, games, video pacing).
    why_status: |
      DEMONSTRATED via five independent confirmation paths converging on
      the same optimal configuration.
      (1) Wundt 1874 — descriptive: balanced predictable/dynamic mixture
      necessary for aesthetic appreciation; two failure modes (chaos,
      triviality) excluded analytically.
      (2) Berlyne 1971 — formalised inverted-U: liking-vs-complexity peak
      at intermediate.
      (3) Salimpoor 2011 (Nat Neurosci) — biphasic dopamine confirms
      neuroanatomical substrate: caudate (anticipation) + accumbens
      (resolution); the architecture is what produces both phases.
      (4) Gold/Pearce/Zatorre 2019 (J Neurosci) — computational validation
      via uncertainty/surprise modelling: "intermediate complexity, which
      maximizes both reducible uncertainty and learnable information,
      thus optimizes reward-related responses".
      (5) Cheung 2019 (Curr Biol) — 2D saddle surface (uncertainty ×
      surprise) shows peak pleasure exclusively at low-uncertainty +
      high-surprise quadrant, which IS the architecture's signature.
      Independent confirmation through φ-attractor route: music-engagement
      criteria developed without reference to Fibonacci converge on the
      same architectural form derived independently from Markov spectrum
      / smallest Pisot / two-memory recurrence (sec:fibonacci) — the
      quantitative match (Bartók climax at φ⁻¹ ≈ 0.618) excludes
      coincidence.
    not_misinterpretations: |
      one option among many (no — alternative configurations either land
      in chaos quadrant or triviality quadrant per Cheung 2019 saddle;
      apex-landing requires this specific architecture) · music-only (no
      — generalises to all engagement-driven substrates: story, drama,
      games, video pacing; music is one instance) · the engineered break
      is arbitrary design choice (no — break itself is forced because
      pure fractal yields zero prediction error, no signal; Wundt-apex
      magnitude is forced by dopamine mechanism; only the timing /
      material content is choice) · this duplicates ADAP framework (no —
      ADAP is a corpus-validated test for this architecture; the
      structural claim is what ADAP verifies) · convergence with φ-route
      is coincidence (no — independent derivations on the same substrate
      class necessarily reach the same A_0 attractor; quantitative
      Bartók-Markov match excludes coincidence by ruling out alternative
      irrationals)

  - id: N_PhiAttractor
    title: Golden Ratio φ as A₀ Attractor on Continuous Angular Substrates
    layer: structure
    status: DEMONSTRATED
    anchors: 6
    a_infinity: false
    is_placeholder: false
    summary: |
      φ = (1+√5)/2 is the A_0 attractor for substrates satisfying five
      conditions: (C1) sequential placement, (C2) repulsive coupling with
      decay, (C3) two-memory effective coupling, (C4) continuous angular
      dimension, (C5) parameter range within Fibonacci basin. Six
      independent derivations converge on φ — Binet two-memory recurrence,
      Levitov SL(2,Z) cylindrical compression, Atela-Golé-Hotton phyllotaxis
      bifurcation, Hurwitz/Markov Diophantine extremum, Penrose smallest-
      Pisot inflation, Lee-Levitov mechanical universality. Counter-instance
      enumeration verifies each Ci individually necessary: removing any one
      produces a different documented attractor (crystal hexagonal, Lucas
      99.5°, bijugate 68.75°, etc.).
    why_status: |
      DEMONSTRATED with A=6 from six independent classical mathematical
      results. The convergence is itself the structural fact: φ is
      simultaneously (a) root of smallest non-trivial irreducible quadratic
      x²=x+1, (b) smallest Pisot number supporting aperiodicity, (c)
      supremum of Markov spectrum (most-irrational), (d) limit of pruned
      van Iterson tree principal branch, (e) minimum-non-trivial two-memory
      characteristic root. These six properties unifying on one number
      establish φ as a number-theoretic singularity, not a coincidental
      attractor. The unification through Markov theory + SL(2,Z) modular
      action is what makes A=6 non-redundant — six coordinate expressions
      of one underlying singularity. Refinement: φ is the principal stable
      attractor (largest basin, densest occurrence, max-irrational position
      on Markov spectrum); secondary noble-number attractors (Lucas,
      multijugate) coexist within the same dynamical system but with
      smaller basins, related to φ by SL(2,Z) orbit equivalence.
    not_misinterpretations: |
      φ is a coincidental approximation in nature (no — it is structurally
      forced for any substrate satisfying C1-C5; counter-instances enumerated
      in .tex verify each condition is individually necessary) · φ-emergence
      requires biological intent (no — it appears in Penrose tilings without
      sequential growth, in atomic quasicrystals with 5-fold symmetry, in
      cortex without conscious selection; substrate satisfaction is the only
      requirement) · this contradicts the L(3,1) Z_3 result (no — the two
      occupy disjoint substrate classes selected by C4: continuous angular
      vs discrete spin sector; both are A_0-forced attractors in their
      respective classes) · 137.5° golden angle is empirical fitting (it
      follows analytically from 360°/φ² with no free parameter) · six
      derivations are six independent confirmations (they are six coordinate
      expressions of one number-theoretic singularity, not six unrelated
      witnesses; the convergence is the structural fact)

  - id: N110
    title: Inevitability Theorem for L(3,1) — From BPI Vertex-Transitivity to S³/Z₃
    layer: structure
    status: DEMONSTRATED
    anchors: 3
    a_infinity: false
    is_placeholder: false
    summary: |
      The geometry L(3,1) = S³/Z_3 is not chosen but inevitable for any BPI
      at the critical dimension. Vertex-transitivity (I_Sym) forces every
      node into the same orbit — no vertex is distinguishable, forcing a
      free action of the symmetry group. A closed orientable 3-manifold
      admitting a vertex-transitive triangulation and a finite non-trivial
      fundamental group must be a spherical space form S³/Γ. For Γ = Z_3,
      the unique such manifold is L(3,1). The geometric Z_3 acts as deck
      transformations of the universal cover S³ → M. Conclusion:
      π_1 = H_1 = Z_3, M = L(3,1).
    why_status: |
      DEMONSTRATED on three independent confirmation paths.
      (1) Vertex-transitivity → free action: I_Sym requires every vertex to
      lie in the same orbit; if the symmetry group fixed any vertex, that
      vertex would be distinguished, contradicting I_Sym. So the action is
      free.
      (2) Spherical space form classification: Hopf (1925) + later
      refinements classify all closed orientable 3-manifolds with finite
      non-trivial π_1 as spherical space forms S³/Γ where Γ is a finite
      subgroup of SO(4). Restricted to free actions on S³, this gives a
      finite list.
      (3) Uniqueness for Γ = Z_3: among spherical space forms with
      Γ = Z_3, L(3,1) is the unique manifold up to homeomorphism. This is
      the lens space classification — L(p,q) and L(p,q') are homeomorphic
      iff q ≡ ±q'^{±1} (mod p), and for p = 3 there is only one
      equivalence class.
      Independent confirmation from N370: baryon number conservation forces
      p = 3 from physics, recovering the same manifold from a completely
      independent argument.
    not_misinterpretations: |
      n = 3 is selected empirically (no — forced by I_Sym + closed orientable
      + finite non-trivial π_1; only Z_3 satisfies BPI requirements at
      critical dimension; N370 confirms via baryon number) · L(3,1) was an
      assumed input (no — derived here from BPI vertex-transitivity and
      independently in N370 from baryon number) · the KO-dim filter alone
      suffices (it does not — Z_5 also passes KO-dim 6; uniqueness requires
      either the I_Sym argument here or N370's odd-prime exclusion) · this
      requires postulating "BPI at critical dimension" (no — BPI is the
      observer-as-subprocess of the manifold, K(O)<K(F), defined in N112;
      "critical dimension" means the spectral dimension boundary derived
      in N067, not a free parameter)

# ─── Round 7: titles for 47 untitled nodes (partial node updates) ───

  - id: N000
    title: "BPI Fundamental Operation as Dimensional Projection"

  - id: N001
    title: "Multiple Observer Problem Dissolved"

  - id: N002
    title: "Falsification Criterion: No Two Distinct Processes Topologically Identical"

  - id: N003
    title: "Kolmogorov Identity Z_struct ≡ K(x) via Levin Coding Theorem"

  - id: N004
    title: "Natural Units Reduce Physics to Pure Z-Value Relations"

  - id: N005
    title: "Why argmin Z and Not argmax — Structural Question Answered"

  - id: N006
    title: "Time Direction: Schrödinger Symmetry vs Physical Arrow"

  - id: N009
    title: "Three Gradients U, R, T as Pure Informational Definitions"

  - id: N010
    title: "A₀ Symmetry Larger Than Conformal Invariance"

  - id: N011
    title: "I_Bound from Lyapunov Stability of Dissipative Systems"

  - id: N012
    title: "Cross-Domain Correspondences as Coordinate Expressions, Not Analogies"

  - id: N013
    title: "Coordinate Identification: A₀ Application to Any Domain"

  - id: N014
    title: "Ontological Randomness as BPI Compression Artifact"

  - id: N015
    title: "Quantum Mechanics Status under Coordinate Freedom"

  - id: N016
    title: "Chalmers Zombie Argument as Structural Closure"

  - id: N017
    title: "Operational Protocol: Transition Topology Across Domains"

  - id: N018
    title: "Autoregressive LLM Generation as Most Transparent A₀ Instantiation"

  - id: N019
    title: "Erosion as Z_struct Accumulation in Geological Substrate"

  - id: N020
    title: "Hebbian Synaptic Plasticity as A₀-Forced Z_struct Reduction"

  - id: N021
    title: "Evolution as A₀ without Telos: Blind Gradient Descent"

  - id: N022
    title: "Null Result Entry: Framework Predicts Search Yields Nothing"

  - id: N024
    title: "Classical Evaluation Criteria as Inherited Ontological Frame"

  - id: N025
    title: "Frame Shift to Topology Reorganises Evidentiary Base"

  - id: N026
    title: "LM + Human + Response as One Process in Three Coordinates"

  - id: N027
    title: "Fermat's Principle (1662) as A₀ in Optical Substrate"

  - id: N029
    title: "Transition State Theory: Chemistry as A₀ Transitions"

  - id: N045
    title: "Golden Ratio φ as Unique Min-Memory Continuous A₀ Attractor"

  - id: N048
    title: "Birkhoff Aesthetic Formula M=O/C: Why Its Monotonic Prediction Fails"

  - id: N051
    title: "No Preferred State: All Positions in Transition Space Are Symmetric"

  - id: N053
    title: "Cross-Domain Isomorphisms via Adversarial Falsification, Not Construction"

  - id: N054
    title: "Methodological Verification: No Disguised Type-2 Claims"

  - id: N056
    title: "Theory Coherence: Anchor Counts Converging to φ-Sequence"

  - id: N057
    title: "QM as Coordinate System: No Process Compatibility Postulate Required"

  - id: N058
    title: "Quantum Entanglement Nonlocality as BPI Compression Artifact"

  - id: N059
    title: "ER=EPR via Van Raamsdonk Entanglement-Geometry Identification"

  - id: N061
    title: "Born Rule from Observer Containment + Z-Gradient Geometry"

  - id: N070
    title: "Quantum Measurement Problem Dissolved as Observer/Observed Reification"

  - id: N081
    title: "Mass as ∇Z and Verlinde Three-Gradient Decomposition of G"

  - id: N094
    title: "SO(2)×U(1) at Sub-r₀ from d_s=2 Forces Complex Phases"

  - id: N098
    title: "Diffusion Metric g^(Z): Riemannian at IR, Quantum-Walk at UV"

  - id: N103
    title: "Complex Hilbert Space Forced by Z₃ Representation Theory"

  - id: N107
    title: "Simplicial Chain Complex ∂²=0 from Four Invariants"

  - id: N117
    title: "BPI as Z-Measurement Process: Definitional Prerequisites"

  - id: N119
    title: "Three Topologically Distinct Matter Sectors with Identical Gauge Numbers"

  - id: N133
    title: "One SM Generation from Representation of A_F on H_F"

  - id: N201
    title: "Early-Universe G Variation Direction in CMB H₀"

  - id: N206
    title: "Global Symmetries Inexact in UV-Complete Quantum Gravity"


# ─── Round 8: Z-vectors for top 30 hubs (partial node updates) ───

  - id: DEF
    z_struct: 0.33
    z_therm: 0.33
    z_hidden: 0.34
    # A_0 = argmin Z balances all three Z components by definition

  - id: N_InversiveTheory
    z_struct: 0.7
    z_therm: 0.1
    z_hidden: 0.2
    # no-alternatives is a structural-stability argument; mostly Z_struct

  - id: N_NoSeparatePieces
    z_struct: 0.7
    z_therm: 0.1
    z_hidden: 0.2
    # coordinate unification is structural

  - id: N_Invariants
    z_struct: 0.6
    z_therm: 0.2
    z_hidden: 0.2
    # 4 invariants combine structural symmetry/quantization with thermal direction

  - id: N_Triangulation
    z_struct: 0.6
    z_therm: 0.1
    z_hidden: 0.3
    # 3-component decomposition itself is structural; Z_hidden encoded in ergodic claim

  - id: N_TopologyProcessIdentity
    z_struct: 0.6
    z_therm: 0.2
    z_hidden: 0.2
    # object/process identity is mostly structural

  - id: N_Logic
    z_struct: 0.9
    z_therm: 0.05
    z_hidden: 0.05
    # pure symbolic substrate; Z_struct dominant

  - id: N_BPIEngagement
    z_struct: 0.4
    z_therm: 0.3
    z_hidden: 0.3
    # triangulation in time-indexed substrate balances all three

  - id: N188
    z_struct: 0.3
    z_therm: 0.2
    z_hidden: 0.5
    # qualia opacity is Z_hidden dominant (BPI internal access)

  - id: N_FEP
    z_struct: 0.4
    z_therm: 0.4
    z_hidden: 0.2
    # free energy F = accuracy + complexity = Z_struct + Z_therm

  - id: N112
    z_struct: 0.1
    z_therm: 0.1
    z_hidden: 0.8
    # Observer Containment Lemma — pure Z_hidden definition

  - id: N_DopaminePredictionError
    z_struct: 0.2
    z_therm: 0.5
    z_hidden: 0.3
    # prediction error signal is irreversible Z_therm event

  - id: N371
    z_struct: 0.3
    z_therm: 0.4
    z_hidden: 0.3
    # BPI hydrodynamics — Z_therm flow with structure

  - id: N370
    z_struct: 0.7
    z_therm: 0.1
    z_hidden: 0.2
    # L(3,1) topology is structural inevitability

  - id: N_ZGaugeDecomposition
    z_struct: 0.6
    z_therm: 0.2
    z_hidden: 0.2
    # Z gauge decomposition via Z_3 covering — structural

  - id: N067
    z_struct: 0.5
    z_therm: 0.1
    z_hidden: 0.4
    # spectral dim scale-bridge — Z_struct + Z_hidden (UV cutoff)

  - id: N078
    z_struct: 0.2
    z_therm: 0.6
    z_hidden: 0.2
    # gravity = thermodynamics — Z_therm dominant (entropic)

  - id: N375
    z_struct: 0.5
    z_therm: 0.4
    z_hidden: 0.1
    # cuprate phase diagram — Z_struct (bands) + Z_therm (T-dependence)

  - id: N376
    z_struct: 0.5
    z_therm: 0.4
    z_hidden: 0.1
    # iron-based — same pattern as cuprates

  - id: N374
    z_struct: 0.5
    z_therm: 0.4
    z_hidden: 0.1
    # UPt_3 three-phase — same pattern

  - id: N372
    z_struct: 0.5
    z_therm: 0.4
    z_hidden: 0.1
    # Sr_2RuO_4 — same pattern

  - id: N373
    z_struct: 0.5
    z_therm: 0.4
    z_hidden: 0.1
    # UTe_2 — same pattern

  - id: N377
    z_struct: 0.7
    z_therm: 0.1
    z_hidden: 0.2
    # SAE feature absorption — structural divergence in activation space

  - id: N145
    z_struct: 0.7
    z_therm: 0.1
    z_hidden: 0.2
    # Koide K=2/3 — algebraic structural identity

  - id: N_LeptonMassScale
    z_struct: 0.6
    z_therm: 0.3
    z_hidden: 0.1
    # mass scale via QCD string tension — Z_struct + Z_therm

  - id: N129
    z_struct: 0.8
    z_therm: 0.1
    z_hidden: 0.1
    # SM algebra ℂ⊕ℍ⊕M_3(ℂ) — pure Z_struct (algebraic derivation)

  - id: N_PhiAttractor
    z_struct: 0.7
    z_therm: 0.2
    z_hidden: 0.1
    # phi-attractor — number-theoretic structural

  - id: N_CommThm
    z_struct: 0.2
    z_therm: 0.2
    z_hidden: 0.6
    # optimal communication uses Z_hidden as primary cost

  - id: N_AestheticEngagement
    z_struct: 0.4
    z_therm: 0.3
    z_hidden: 0.3
    # aesthetic engagement balanced like BPI engagement

  - id: N_EpistemicTraps
    z_struct: 0.4
    z_therm: 0.1
    z_hidden: 0.5
    # catalogue of failure modes — Z_hidden dominant (observer-side)

